OpenAI Codex v0.111.0 (research preview)
--------
workdir: /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_web
model: gpt-5.3-codex
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR, /Users/mq20185996/.codex/memories]
reasoning effort: medium
reasoning summaries: none
session id: 019ccad1-a838-7711-88b9-0b627e36f06a
--------
user
Process only this section from change requests and make code changes in the current repository.
Source file: change_requests.md
Section title: Nernst Equilibrium for One Ion

Requirements:
- Implement all bullet requests in this section only.
- Do not modify requests from other sections.
- Run relevant checks/build before finishing if possible.
- Provide a concise final summary of files changed and why.

Section content:
# Nernst Equilibrium for One Ion

- Use prettier math for V = S ln(C_out / C_in). ---
  something like latex / mathjax

- The charge walll in the middle of the cell wall in this
  simulation makes no sense. Remove it. We want it just like
  the preivou sims with one ion and one pore. The inside of
  the "cell" is the left side of the plot, the outside is
  the right side. The inside has a far wal that is charged
  to attract or reply ions of different charges.

mcp startup: no servers
codex
I’ll locate the “Nernst Equilibrium for One Ion” change-request section in the repo, then identify the corresponding simulation/UI files to implement only those bullets and run checks.
exec
/bin/zsh -lc 'rg --files' in /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_webexec
/bin/zsh -lc "rg -n \"Nernst Equilibrium for One Ion|Nernst|one ion|charge wall|cell wall|MathJax|latex|ln\\(\" change_requests.md src ." in /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_web succeeded in 51ms:
src/pages/resting_two_ion_common.ts
src/pages/inspect_resting_potential_nernst.ts
src/pages/inspect_electrochemical_3.ts
src/pages/sim_shared.ts
src/pages/inspect_diffusion_1.ts
src/pages/inspect_resting_potential_4.ts
src/pages/inspect_electrochemical_2.ts
src/pages/inspect_diffusion_4.ts
src/pages/inspect_action_potential_1.ts
src/pages/inspect_resting_potential_1.ts
src/pages/inspect_nak_pump.ts
src/pages/inspect_diffusion_3.ts
src/pages/action_potential_common.ts
src/pages/inspect_electrochemical_1.ts
src/pages/inspect_action_potential_2.ts
src/pages/inspect_resting_potential_2.ts
src/pages/inspect_action_potential_3.ts
src/pages/inspect_resting_potential_3.ts
src/pages/inspect_electrochemical_4.ts
src/pages/inspect_diffusion_2.ts
 succeeded in 51ms:
change_requests.md:27:# Nernst Equilibrium for One Ion
change_requests.md:28:- Use prettier math for V = S ln(C_out / C_in). ---
change_requests.md:29:  something like latex / mathjax
change_requests.md:31:- The charge walll in the middle of the cell wall in this
change_requests.md:33:  the preivou sims with one ion and one pore. The inside of
change_requests.md:46:- Remove from the legend Negative charge wall Positive
change_requests.md:47:  charge wall Ion A pore Ion B pore Ion A solid = left,
src/pages/resting_two_ion_common.ts:758:            `Goldman proxy = ln((P_A·A_out + P_B·B_in)/(P_A·A_in + P_B·B_out)) × ${simParams.goldmanScale.toFixed(0)}`,
src/pages/resting_two_ion_common.ts:763:            'The charge wall provides a membrane-scale electrical field instead of a point attractor',
src/pages/resting_two_ion_common.ts:777:      ...(variant.fieldMode === 'wall' ? ['The dashed charge wall sits just inside the left boundary and also reflects particles.'] : []),
./src/pages/resting_two_ion_common.ts:758:            `Goldman proxy = ln((P_A·A_out + P_B·B_in)/(P_A·A_in + P_B·B_out)) × ${simParams.goldmanScale.toFixed(0)}`,
./src/pages/resting_two_ion_common.ts:763:            'The charge wall provides a membrane-scale electrical field instead of a point attractor',
./src/pages/resting_two_ion_common.ts:777:      ...(variant.fieldMode === 'wall' ? ['The dashed charge wall sits just inside the left boundary and also reflects particles.'] : []),
src/pages/inspect_resting_potential_nernst.ts:52:  vNernst: number;
src/pages/inspect_resting_potential_nernst.ts:160:  state.vNernst = params.nernstScale * Math.log((state.rightCount + 0.5) / (state.leftCount + 0.5));
src/pages/inspect_resting_potential_nernst.ts:162:  // teaching trace and Nernst prediction are directly comparable.
src/pages/inspect_resting_potential_nernst.ts:163:  state.vProxy = state.vNernst;
src/pages/inspect_resting_potential_nernst.ts:196:    vNernst: 0
src/pages/inspect_resting_potential_nernst.ts:236:  trace.nernst.push(state.vNernst);
src/pages/inspect_resting_potential_nernst.ts:316:  ctx.fillText('Simulated Vm and Nernst Prediction', 12 * dpr, 14 * dpr);
codex
I found the likely target at `src/pages/inspect_resting_potential_nernst.ts`; next I’m checking how the wall is drawn and how equation text is rendered so we can remove the middle wall and add MathJax-style formatting.
exec
/bin/zsh -lc "sed -n '260,620p' src/pages/inspect_resting_potential_nernst.ts" in /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_webexec
/bin/zsh -lc "sed -n '1,260p' src/pages/inspect_resting_potential_nernst.ts" in /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_web succeeded in 51ms:
  const rect = canvas.getBoundingClientRect();
  const w = Math.max(1, Math.round(rect.width * dpr));
  const h = Math.max(1, Math.round(rect.height * dpr));
  if (canvas.width !== w || canvas.height !== h) { canvas.width = w; canvas.height = h; }
  const ctx = canvas.getContext('2d');
  if (!ctx) return;
  ctx.clearRect(0, 0, w, h);
  ctx.fillStyle = '#03060b';
  ctx.fillRect(0, 0, w, h);
  ctx.strokeStyle = 'rgba(120,170,255,0.10)';
  ctx.lineWidth = 1;
  for (let i = 1; i < 10; i += 1) {
    const gx = (w * i) / 10;
    const gy = (h * i) / 10;
    ctx.beginPath(); ctx.moveTo(gx, 0); ctx.lineTo(gx, h); ctx.stroke();
    ctx.beginPath(); ctx.moveTo(0, gy); ctx.lineTo(w, gy); ctx.stroke();
  }
  const padL = 36 * dpr;
  const padR = 10 * dpr;
  const padT = 18 * dpr;
 succeeded in 50ms:
import '../style.css';
import {
  DEFAULT_DIFFUSION_SD,
  DEFAULT_NUM_PARTICLES,
  DEFAULT_POTENTIAL_SCALE,
  drawMembraneWall,
  MAX_PARTICLES,
  SIM_COLORS,
  centeredChannelBounds,
  fieldColor
} from './sim_shared';

interface SimParams {
  T: number;
  dt: number;
  numParticles: number;
  boxWidth: number;
  boxHeight: number;
  wallThickness: number;
  diffusionSd: number;
exec
/bin/zsh -lc "sed -n '620,980p' src/pages/inspect_resting_potential_nernst.ts" in /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_web succeeded in 51ms:
}
function trimTrace(traceHistory: TraceHistory, currentTime: number, traceWindowMs: number): void {
  const minTime = Math.max(0, currentTime - traceWindowMs);
  while (traceHistory.times.length > 1 && traceHistory.times[0] < minTime) {
    traceHistory.times.shift();
    traceHistory.actual.shift();
    traceHistory.nernst.shift();
  }
}
function rebuildFromInputs(): void {
  simParams = readSimInputs();
  displayParams = readDisplayInputs();
  currentSeed = clamp(Math.floor(Number(inputs.seed.value) || currentSeed), 0, 0xffffffff) >>> 0;
  rng = new Rng(currentSeed);
  state = createState(simParams, currentSeed);
  trace = createTrace(state, simParams.T);
  stepAccumulator = 0;
  writeInputs();
  updateEquationText();
}
codex
I’ve confirmed this page currently draws a dashed “charge wall” at the membrane center and uses plain text equations. I’ll now align it to the requested layout by moving/removing that middle wall depiction, updating labels/text to inside-left/outside-right with a left far-wall charge concept, and replacing equation text with rendered math.
exec
/bin/zsh -lc "sed -n '1,260p' src/pages/sim_shared.ts" in /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_webexec
/bin/zsh -lc "sed -n '260,520p' src/pages/sim_shared.ts" in /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_webexec
/bin/zsh -lc 'rg -n "MathJax|katex|equation-card|equation" src/style.css src/pages/*.ts' in /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_web succeeded in 50ms:
export const MAX_PARTICLES = 5000;
export const DEFAULT_NUM_PARTICLES = 3000;
export const DEFAULT_DIFFUSION_SD = 0.25;
export const DEFAULT_POTENTIAL_SCALE = 26;
export const DEFAULT_CHARGE_WALL_INSET = 2;

export const SIM_COLORS = {
  particle: 'rgba(245, 178, 72, 0.92)',
  ionA: 'rgba(245, 178, 72, 0.92)',
  ionB: 'rgba(66, 200, 255, 0.92)',
  ionC: 'rgba(114, 255, 178, 0.92)',
  outsideTrace: '#42c8ff',
  insideTrace: '#72ffb2',
  ionATrace: '#f5b248',
  ionBTrace: '#42c8ff',
  ionCTrace: '#72ffb2',
  totalTrace: '#c68dff',
  predictionTrace: '#f2f5fb',
  channelFill: 'rgba(159, 255, 106, 0.12)',
  membraneFill: 'rgba(200, 220, 255, 0.08)',
 succeeded in 51ms:

 succeeded in 52ms:
src/pages/inspect_diffusion_1.ts:320:            <div class="equation-card">
src/pages/inspect_diffusion_1.ts:321:              <pre class="equation" id="equation-block"></pre>
src/pages/inspect_diffusion_1.ts:371:const equationBlock = getEl<HTMLElement>('#equation-block');
src/pages/inspect_diffusion_1.ts:436:  equationBlock.innerHTML = [
src/pages/inspect_electrochemical_4.ts:649:            <div class="equation-card">
src/pages/inspect_electrochemical_4.ts:650:              <pre class="equation" id="equation-block"></pre>
src/pages/inspect_electrochemical_4.ts:688:const equationBlock = getEl<HTMLElement>('#equation-block');
src/pages/inspect_electrochemical_4.ts:811:  equationBlock.innerHTML = [
src/pages/inspect_diffusion_2.ts:554:            <div class="equation-card">
src/pages/inspect_diffusion_2.ts:555:              <pre class="equation" id="equation-block"></pre>
src/pages/inspect_diffusion_2.ts:609:const equationBlock = getEl<HTMLElement>('#equation-block');
src/pages/inspect_diffusion_2.ts:670:  equationBlock.innerHTML = [
src/pages/inspect_electrochemical_3.ts:510:            <div class="equation-card">
src/pages/inspect_electrochemical_3.ts:511:              <pre class="equation" id="equation-block"></pre>
src/pages/inspect_electrochemical_3.ts:566:const equationBlock = getEl<HTMLElement>('#equation-block');
src/pages/inspect_electrochemical_3.ts:724:  equationBlock.innerHTML = [
src/pages/resting_two_ion_common.ts:37:  equationGroupLabel?: string;
src/pages/resting_two_ion_common.ts:38:  equationDescription?: string;
src/pages/resting_two_ion_common.ts:512:            <div class="group"><p class="group-label">${variant.equationGroupLabel ?? 'Equation + Trace Readout'}</p><div class="equation-card"><pre class="equation" id="equation-block"></pre><p>${variant.equationDescription ?? (variant.withGoldman ? 'Per-ion and total compartment imbalances are qualitative charge-separation proxies. The dashed white line is a permeability-weighted Goldman-style prediction scaled for comparison.' : 'Per-ion and total compartment imbalances are qualitative charge-separation proxies built from the charge-wall resting-potential model.')}</p></div></div>
src/pages/resting_two_ion_common.ts:539:  const equationBlock = getEl<HTMLElement>('#equation-block');
exec
/bin/zsh -lc 'cat package.json' in /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_web succeeded in 52ms:
{
  "name": "electrochemical-signals-progression-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^6.4.1"
  }
}
codex
I’m checking the earlier one-ion/one-pore simulation implementation so this page can match its geometry and “inside far-wall charge” behavior rather than inventing a new pattern.
exec
/bin/zsh -lc "sed -n '1,240p' src/pages/inspect_electrochemical_2.ts" in /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_webexec
/bin/zsh -lc "rg -n \"leftEdgeChargeWallX|charge wall|wallFieldDrift|drawParticles\\(|electricStrength\" src/pages/inspect_electrochemical_2.ts src/pages/inspect_electrochemical_1.ts" in /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_webexec
/bin/zsh -lc "sed -n '240,420p' src/pages/inspect_electrochemical_2.ts" in /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_web exited 1 in 52ms:
sed: src/pages/inspect_electrochemical_2.ts: No such file or directory
 exited 2 in 51ms:
rg: src/pages/inspect_electrochemical_2.ts: No such file or directory (os error 2)
src/pages/inspect_electrochemical_1.ts:17:  electricStrength: number;
src/pages/inspect_electrochemical_1.ts:81:  electricStrength: -0.15,
src/pages/inspect_electrochemical_1.ts:170:  const electricStrength = clamp(params.electricStrength, -10, 10);
src/pages/inspect_electrochemical_1.ts:175:    const [driftX, driftY] = pointFieldDrift(dx, dy, electricStrength, 1);
src/pages/inspect_electrochemical_1.ts:210:function drawFrame(canvas: HTMLCanvasElement, state: LiveState, display: DisplayParams, electricStrength: number): void {
src/pages/inspect_electrochemical_1.ts:268:  ctx.fillStyle = fieldColor(electricStrength);
src/pages/inspect_electrochemical_1.ts:378:  electricStrength: getEl<HTMLInputElement>('#electric-strength'),
src/pages/inspect_electrochemical_1.ts:422:  setNumberInput(inputs.electricStrength, simParams.electricStrength, 3);
src/pages/inspect_electrochemical_1.ts:442:    electricStrength: clamp(Number(inputs.electricStrength.value) || 0, -10, 10),
src/pages/inspect_electrochemical_1.ts:488:    `Current: diffusionSd = ${sigma.toFixed(3)}, dt = ${dt.toFixed(3)}, electric strength = ${simParams.electricStrength.toFixed(3)}`,
src/pages/inspect_electrochemical_1.ts:533:  drawFrame(canvas, state, displayParams, simParams.electricStrength);
src/pages/inspect_electrochemical_1.ts:567:for (const key of ['numParticles', 'diffusionSd', 'electricStrength', 'seed', 'totalTime', 'dt', 'initClusterSd', 'negX', 'negY'] as const) {
 exited 1 in 51ms:
sed: src/pages/inspect_electrochemical_2.ts: No such file or directory
exec
/bin/zsh -lc 'ls -1 src/pages | rg electrochemical' in /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_web succeeded in 52ms:
inspect_electrochemical_1.ts
inspect_electrochemical_3.ts
inspect_electrochemical_4.ts
codex
I hit a stale reference while searching, so I’m using existing electrochemical pages (`1/3/4`) to mirror the established one-ion wall behavior.
exec
/bin/zsh -lc "sed -n '220,360p' src/pages/inspect_electrochemical_3.ts" in /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_webexec
/bin/zsh -lc 'rg -n "leftEdgeChargeWallX|wallFieldDrift|chargeWallX|dashed charge wall|inside|outside" src/pages/inspect_electrochemical_3.ts src/pages/inspect_electrochemical_4.ts src/pages/inspect_electrochemical_1.ts' in /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_webexec
/bin/zsh -lc "sed -n '1,220p' src/pages/inspect_electrochemical_3.ts" in /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_web succeeded in 52ms:
  const halfH = state.boxHeight / 2;
  for (let i = 0; i < state.numParticles; i += 1) {
    state.x[i] = reflectIntoBounds(state.x[i], state.negX, halfW);
    state.y[i] = reflectIntoBounds(state.y[i], -halfH, halfH);
    const inWall = state.x[i] > state.leftWall && state.x[i] < state.rightWall;
    const inChannel = state.y[i] >= state.channelYMin && state.y[i] <= state.channelYMax;
    if (inWall && !inChannel) state.x[i] = state.x[i] < 0 ? state.leftWall : state.rightWall;
  }
  countSides(state);
}

function trimTrace(trace: TraceHistory, currentTime: number, traceWindowMs: number): void {
  const minTime = Math.max(0, currentTime - traceWindowMs);
  while (trace.times.length > 1 && trace.times[0] < minTime) {
    trace.times.shift();
    trace.leftFrac.shift();
    trace.rightFrac.shift();
  }
}

 succeeded in 51ms:
src/pages/inspect_electrochemical_3.ts:10:  leftEdgeChargeWallX,
src/pages/inspect_electrochemical_3.ts:11:  wallFieldDrift
src/pages/inspect_electrochemical_3.ts:108:  negX: leftEdgeChargeWallX(100)
src/pages/inspect_electrochemical_3.ts:156:    negX: leftEdgeChargeWallX(boxWidth)
src/pages/inspect_electrochemical_3.ts:247:    const driftX = wallFieldDrift(dx, electricStrength, 1);
src/pages/inspect_electrochemical_3.ts:410:    { data: trace.leftFrac, color: SIM_COLORS.outsideTrace },
src/pages/inspect_electrochemical_3.ts:411:    { data: trace.rightFrac, color: SIM_COLORS.insideTrace }
src/pages/inspect_electrochemical_3.ts:514:                positive electric strength reverses the drift without resetting the simulation. The dashed charge wall is also a reflecting boundary.
src/pages/inspect_electrochemical_3.ts:554:          <span><span class="swatch" style="background:${SIM_COLORS.outsideTrace}"></span>Left trace</span>
src/pages/inspect_electrochemical_3.ts:555:          <span><span class="swatch" style="background:${SIM_COLORS.insideTrace}"></span>Right trace</span>
src/pages/inspect_electrochemical_3.ts:654:    negX: leftEdgeChargeWallX(boxWidth)
src/pages/inspect_electrochemical_3.ts:736:    'The dashed charge wall sits just inside the left boundary and also reflects particles.',
src/pages/inspect_electrochemical_4.ts:10:  leftEdgeChargeWallX,
src/pages/inspect_electrochemical_4.ts:11:  wallFieldDrift
src/pages/inspect_electrochemical_4.ts:126:  negX: leftEdgeChargeWallX(100)
src/pages/inspect_electrochemical_4.ts:177:    negX: leftEdgeChargeWallX(boxWidth)
src/pages/inspect_electrochemical_4.ts:360:    const driftX = wallFieldDrift(dx, params.electricStrength, state.charges[i]);
src/pages/inspect_electrochemical_4.ts:781:    negX: leftEdgeChargeWallX(boxWidth)
src/pages/inspect_electrochemical_4.ts:824:    'The dashed charge wall sits just inside the left boundary and also reflects particles.',
 succeeded in 51ms:
import '../style.css';
import {
  DEFAULT_DIFFUSION_SD,
  DEFAULT_NUM_PARTICLES,
  MAX_PARTICLES,
  SIM_COLORS,
  centeredChannelBounds,
  drawMembraneWall,
  fieldColor,
  leftEdgeChargeWallX,
  wallFieldDrift
} from './sim_shared';

interface SimParams {
  T: number;
  dt: number;
  numParticles: number;
  boxWidth: number;
  boxHeight: number;
  wallThickness: number;
exec
/bin/zsh -lc "sed -n '360,500p' src/style.css" in /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_web succeeded in 52ms:
}

.trace-canvas {
  height: min(26vh, 208px);
}

.diffusion-pore-panel .pore-trace-canvas {
  height: min(54vh, 512px);
}

.square-canvas {
  aspect-ratio: 1 / 1;
  height: auto;
  max-height: min(50vh, 420px);
}

.legend {
  display: flex;
  flex-wrap: wrap;
  gap: 8px 12px;
codex
I’m now editing `inspect_resting_potential_nernst.ts` to do three concrete changes: render the Nernst equation in math markup, move the charge wall from the membrane center to the far-left inside boundary, and update wording/legend to match inside-left vs outside-right.
exec
/bin/zsh -lc "nl -ba src/pages/inspect_resting_potential_nernst.ts | sed -n '1,220p'" in /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_webexec
/bin/zsh -lc "nl -ba src/pages/inspect_resting_potential_nernst.ts | sed -n '520,860p'" in /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_webexec
/bin/zsh -lc "nl -ba src/pages/inspect_resting_potential_nernst.ts | sed -n '220,520p'" in /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_web succeeded in 52ms:
     1	import '../style.css';
     2	import {
     3	  DEFAULT_DIFFUSION_SD,
     4	  DEFAULT_NUM_PARTICLES,
     5	  DEFAULT_POTENTIAL_SCALE,
     6	  drawMembraneWall,
     7	  MAX_PARTICLES,
     8	  SIM_COLORS,
     9	  centeredChannelBounds,
    10	  fieldColor
    11	} from './sim_shared';
    12	
    13	interface SimParams {
    14	  T: number;
    15	  dt: number;
    16	  numParticles: number;
    17	  boxWidth: number;
    18	  boxHeight: number;
    19	  wallThickness: number;
    20	  diffusionSd: number;
 succeeded in 51ms:
   520	  setNumberInput(inputs.boxWidth, simParams.boxWidth, 1);
   521	  setNumberInput(inputs.boxHeight, simParams.boxHeight, 1);
   522	  setNumberInput(inputs.wallThickness, simParams.wallThickness, 2);
   523	  setNumberInput(inputs.voltageScale, simParams.voltageScale, 0);
   524	  setNumberInput(inputs.nernstScale, simParams.nernstScale, 0);
   525	  setNumberInput(inputs.traceYLim, displayParams.traceYLimit, 0);
   526	  setNumberInput(inputs.pointSize, displayParams.pointSize, 2);
   527	  setNumberInput(inputs.targetFps, displayParams.targetFps, 0);
   528	}
   529	function readSimInputs(): SimParams {
   530	  const boxHeight = clamp(Number(inputs.boxHeight.value) || defaultSim.boxHeight, 40, 500);
   531	  const boxWidth = clamp(Number(inputs.boxWidth.value) || defaultSim.boxWidth, 40, 500);
   532	  return normalizeSimParams({
   533	    T: clamp(Number(inputs.totalTime.value) || defaultSim.T, 100, 20000),
   534	    dt: clamp(Number(inputs.dt.value) || defaultSim.dt, 0.05, 20),
   535	    numParticles: clamp(Math.round(Number(inputs.numParticles.value) || defaultSim.numParticles), 10, MAX_PARTICLES),
   536	    boxWidth,
   537	    boxHeight,
   538	    wallThickness: clamp(Number(inputs.wallThickness.value) || defaultSim.wallThickness, 0.5, 50),
   539	    diffusionSd: clamp(Number(inputs.diffusionSd.value) || 0, 0, 20),
 succeeded in 51ms:
   220	    state.x[i] = xNew;
   221	    state.y[i] = yNew;
   222	  }
   223	  state.stepCount += 1;
   224	  state.simTime = state.stepCount * state.dt;
   225	  updateVoltages(state, params);
   226	}
   227	
   228	function createTrace(state: LiveState, traceWindowMs: number): TraceHistory {
   229	  const trace: TraceHistory = { times: [], actual: [], nernst: [] };
   230	  pushTrace(trace, state, traceWindowMs);
   231	  return trace;
   232	}
   233	function pushTrace(trace: TraceHistory, state: LiveState, traceWindowMs: number): void {
   234	  trace.times.push(state.simTime);
   235	  trace.actual.push(state.vProxy);
   236	  trace.nernst.push(state.vNernst);
   237	  const minTime = Math.max(0, state.simTime - traceWindowMs);
   238	  while (trace.times.length > 1 && trace.times[0] < minTime) {
   239	    trace.times.shift();
codex
I’ve mapped exact line locations; next I’ll apply a focused patch to this single page file and then run a build check.
file update
M /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
@@ -5,2 +5,3 @@
   DEFAULT_POTENTIAL_SCALE,
+  DEFAULT_CHARGE_WALL_INSET,
   drawMembraneWall,
@@ -9,3 +10,5 @@
   centeredChannelBounds,
-  fieldColor
+  fieldColor,
+  leftEdgeChargeWallX,
+  wallFieldDrift
 } from './sim_shared';
@@ -24,2 +27,3 @@
   nernstScale: number;
+  chargeWallInset: number;
 }
@@ -44,2 +48,3 @@
   rightWall: number;
+  negX: number;
   channelYMin: number;
@@ -102,3 +107,4 @@
   voltageScale: DEFAULT_POTENTIAL_SCALE,
-  nernstScale: DEFAULT_POTENTIAL_SCALE
+  nernstScale: DEFAULT_POTENTIAL_SCALE,
+  chargeWallInset: DEFAULT_CHARGE_WALL_INSET
 };
apply_patch(auto_approved=true) exited 0 in 93ms:
Success. Updated the following files:
M src/pages/inspect_resting_potential_nernst.ts
file update:
diff --git a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
index 7e72d55cd35ae3c2444359ae7ab9a07452c4bf12..62f85ed9c1b0df1283562867582ac10972fbb4c8
--- a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
+++ b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
@@ -3,11 +3,14 @@
   DEFAULT_DIFFUSION_SD,
   DEFAULT_NUM_PARTICLES,
   DEFAULT_POTENTIAL_SCALE,
+  DEFAULT_CHARGE_WALL_INSET,
   drawMembraneWall,
   MAX_PARTICLES,
   SIM_COLORS,
   centeredChannelBounds,
-  fieldColor
+  fieldColor,
+  leftEdgeChargeWallX,
+  wallFieldDrift
 } from './sim_shared';
 
 interface SimParams {
@@ -22,6 +25,7 @@
   channelWidth: number;
   voltageScale: number;
   nernstScale: number;
+  chargeWallInset: number;
 }
 
 interface DisplayParams {
@@ -42,6 +46,7 @@
   boxHeight: number;
   leftWall: number;
   rightWall: number;
+  negX: number;
   channelYMin: number;
   channelYMax: number;
   initialLeftCount: number;
@@ -100,7 +105,8 @@
   electricStrength: -0.35,
   channelWidth: 20,
   voltageScale: DEFAULT_POTENTIAL_SCALE,
-  nernstScale: DEFAULT_POTENTIAL_SCALE
+  nernstScale: DEFAULT_POTENTIAL_SCALE,
+  chargeWallInset: DEFAULT_CHARGE_WALL_INSET
 };
 
 const defaultDisplay: DisplayParams = { pointSize: 2.2, playbackSpeed: 1, targetFps: 30, traceYLimit: 20 };

file update:
diff --git a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
index 7e72d55cd35ae3c2444359ae7ab9a07452c4bf12..62f85ed9c1b0df1283562867582ac10972fbb4c8
--- a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
+++ b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
@@ -3,11 +3,14 @@
   DEFAULT_DIFFUSION_SD,
   DEFAULT_NUM_PARTICLES,
   DEFAULT_POTENTIAL_SCALE,
+  DEFAULT_CHARGE_WALL_INSET,
   drawMembraneWall,
   MAX_PARTICLES,
   SIM_COLORS,
   centeredChannelBounds,
-  fieldColor
+  fieldColor,
+  leftEdgeChargeWallX,
+  wallFieldDrift
 } from './sim_shared';
 
 interface SimParams {
@@ -22,6 +25,7 @@
   channelWidth: number;
   voltageScale: number;
   nernstScale: number;
+  chargeWallInset: number;
 }
 
 interface DisplayParams {
@@ -42,6 +46,7 @@
   boxHeight: number;
   leftWall: number;
   rightWall: number;
+  negX: number;
   channelYMin: number;
   channelYMax: number;
   initialLeftCount: number;
@@ -100,7 +105,8 @@
   electricStrength: -0.35,
   channelWidth: 20,
   voltageScale: DEFAULT_POTENTIAL_SCALE,
-  nernstScale: DEFAULT_POTENTIAL_SCALE
+  nernstScale: DEFAULT_POTENTIAL_SCALE,
+  chargeWallInset: DEFAULT_CHARGE_WALL_INSET
 };
 
 const defaultDisplay: DisplayParams = { pointSize: 2.2, playbackSpeed: 1, targetFps: 30, traceYLimit: 20 };

codex
I’ve started applying the page changes and will continue with the dedicated `apply_patch` tool for the remaining edits, then run the build.
file update
M /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
@@ -147,3 +147,4 @@
     voltageScale: clamp(params.voltageScale, 1, 500),
-    nernstScale: clamp(params.nernstScale, 1, 500)
+    nernstScale: clamp(params.nernstScale, 1, 500),
+    chargeWallInset: clamp(params.chargeWallInset, 0.5, Math.max(0.5, boxWidth - 1))
   };
apply_patch(auto_approved=true) exited 0 in 84ms:
Success. Updated the following files:
M src/pages/inspect_resting_potential_nernst.ts
file update:
diff --git a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
index 7e72d55cd35ae3c2444359ae7ab9a07452c4bf12..8437dd0ba25a5f20bc9006b8888f06989c1d0924
--- a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
+++ b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
@@ -3,11 +3,14 @@
   DEFAULT_DIFFUSION_SD,
   DEFAULT_NUM_PARTICLES,
   DEFAULT_POTENTIAL_SCALE,
+  DEFAULT_CHARGE_WALL_INSET,
   drawMembraneWall,
   MAX_PARTICLES,
   SIM_COLORS,
   centeredChannelBounds,
-  fieldColor
+  fieldColor,
+  leftEdgeChargeWallX,
+  wallFieldDrift
 } from './sim_shared';
 
 interface SimParams {
@@ -22,6 +25,7 @@
   channelWidth: number;
   voltageScale: number;
   nernstScale: number;
+  chargeWallInset: number;
 }
 
 interface DisplayParams {
@@ -42,6 +46,7 @@
   boxHeight: number;
   leftWall: number;
   rightWall: number;
+  negX: number;
   channelYMin: number;
   channelYMax: number;
   initialLeftCount: number;
@@ -100,7 +105,8 @@
   electricStrength: -0.35,
   channelWidth: 20,
   voltageScale: DEFAULT_POTENTIAL_SCALE,
-  nernstScale: DEFAULT_POTENTIAL_SCALE
+  nernstScale: DEFAULT_POTENTIAL_SCALE,
+  chargeWallInset: DEFAULT_CHARGE_WALL_INSET
 };
 
 const defaultDisplay: DisplayParams = { pointSize: 2.2, playbackSpeed: 1, targetFps: 30, traceYLimit: 20 };
@@ -139,7 +145,8 @@
     electricStrength: clamp(params.electricStrength, -10, 10),
     channelWidth: clamp(params.channelWidth, 0.5, boxHeight - 2),
     voltageScale: clamp(params.voltageScale, 1, 500),
-    nernstScale: clamp(params.nernstScale, 1, 500)
+    nernstScale: clamp(params.nernstScale, 1, 500),
+    chargeWallInset: clamp(params.chargeWallInset, 0.5, Math.max(0.5, boxWidth - 1))
   };
 }
 

file update:
diff --git a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
index 7e72d55cd35ae3c2444359ae7ab9a07452c4bf12..8437dd0ba25a5f20bc9006b8888f06989c1d0924
--- a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
+++ b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
@@ -3,11 +3,14 @@
   DEFAULT_DIFFUSION_SD,
   DEFAULT_NUM_PARTICLES,
   DEFAULT_POTENTIAL_SCALE,
+  DEFAULT_CHARGE_WALL_INSET,
   drawMembraneWall,
   MAX_PARTICLES,
   SIM_COLORS,
   centeredChannelBounds,
-  fieldColor
+  fieldColor,
+  leftEdgeChargeWallX,
+  wallFieldDrift
 } from './sim_shared';
 
 interface SimParams {
@@ -22,6 +25,7 @@
   channelWidth: number;
   voltageScale: number;
   nernstScale: number;
+  chargeWallInset: number;
 }
 
 interface DisplayParams {
@@ -42,6 +46,7 @@
   boxHeight: number;
   leftWall: number;
   rightWall: number;
+  negX: number;
   channelYMin: number;
   channelYMax: number;
   initialLeftCount: number;
@@ -100,7 +105,8 @@
   electricStrength: -0.35,
   channelWidth: 20,
   voltageScale: DEFAULT_POTENTIAL_SCALE,
-  nernstScale: DEFAULT_POTENTIAL_SCALE
+  nernstScale: DEFAULT_POTENTIAL_SCALE,
+  chargeWallInset: DEFAULT_CHARGE_WALL_INSET
 };
 
 const defaultDisplay: DisplayParams = { pointSize: 2.2, playbackSpeed: 1, targetFps: 30, traceYLimit: 20 };
@@ -139,7 +145,8 @@
     electricStrength: clamp(params.electricStrength, -10, 10),
     channelWidth: clamp(params.channelWidth, 0.5, boxHeight - 2),
     voltageScale: clamp(params.voltageScale, 1, 500),
-    nernstScale: clamp(params.nernstScale, 1, 500)
+    nernstScale: clamp(params.nernstScale, 1, 500),
+    chargeWallInset: clamp(params.chargeWallInset, 0.5, Math.max(0.5, boxWidth - 1))
   };
 }
 

file update
M /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
@@ -195,2 +195,3 @@
     rightWall,
+    negX: leftEdgeChargeWallX(p.boxWidth, p.chargeWallInset),
     channelYMin,
apply_patch(auto_approved=true) exited 0 in 88ms:
Success. Updated the following files:
M src/pages/inspect_resting_potential_nernst.ts
file update:
diff --git a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
index 7e72d55cd35ae3c2444359ae7ab9a07452c4bf12..2d645ae9ad33e2026baee6cdff54e09812528e25
--- a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
+++ b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
@@ -3,11 +3,14 @@
   DEFAULT_DIFFUSION_SD,
   DEFAULT_NUM_PARTICLES,
   DEFAULT_POTENTIAL_SCALE,
+  DEFAULT_CHARGE_WALL_INSET,
   drawMembraneWall,
   MAX_PARTICLES,
   SIM_COLORS,
   centeredChannelBounds,
-  fieldColor
+  fieldColor,
+  leftEdgeChargeWallX,
+  wallFieldDrift
 } from './sim_shared';
 
 interface SimParams {
@@ -22,6 +25,7 @@
   channelWidth: number;
   voltageScale: number;
   nernstScale: number;
+  chargeWallInset: number;
 }
 
 interface DisplayParams {
@@ -42,6 +46,7 @@
   boxHeight: number;
   leftWall: number;
   rightWall: number;
+  negX: number;
   channelYMin: number;
   channelYMax: number;
   initialLeftCount: number;
@@ -100,7 +105,8 @@
   electricStrength: -0.35,
   channelWidth: 20,
   voltageScale: DEFAULT_POTENTIAL_SCALE,
-  nernstScale: DEFAULT_POTENTIAL_SCALE
+  nernstScale: DEFAULT_POTENTIAL_SCALE,
+  chargeWallInset: DEFAULT_CHARGE_WALL_INSET
 };
 
 const defaultDisplay: DisplayParams = { pointSize: 2.2, playbackSpeed: 1, targetFps: 30, traceYLimit: 20 };
@@ -139,7 +145,8 @@
     electricStrength: clamp(params.electricStrength, -10, 10),
     channelWidth: clamp(params.channelWidth, 0.5, boxHeight - 2),
     voltageScale: clamp(params.voltageScale, 1, 500),
-    nernstScale: clamp(params.nernstScale, 1, 500)
+    nernstScale: clamp(params.nernstScale, 1, 500),
+    chargeWallInset: clamp(params.chargeWallInset, 0.5, Math.max(0.5, boxWidth - 1))
   };
 }
 
@@ -186,6 +193,7 @@
     boxHeight: p.boxHeight,
     leftWall,
     rightWall,
+    negX: leftEdgeChargeWallX(p.boxWidth, p.chargeWallInset),
     channelYMin,
     channelYMax,
     initialLeftCount: 0,

file update:
diff --git a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
index 7e72d55cd35ae3c2444359ae7ab9a07452c4bf12..2d645ae9ad33e2026baee6cdff54e09812528e25
--- a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
+++ b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
@@ -3,11 +3,14 @@
   DEFAULT_DIFFUSION_SD,
   DEFAULT_NUM_PARTICLES,
   DEFAULT_POTENTIAL_SCALE,
+  DEFAULT_CHARGE_WALL_INSET,
   drawMembraneWall,
   MAX_PARTICLES,
   SIM_COLORS,
   centeredChannelBounds,
-  fieldColor
+  fieldColor,
+  leftEdgeChargeWallX,
+  wallFieldDrift
 } from './sim_shared';
 
 interface SimParams {
@@ -22,6 +25,7 @@
   channelWidth: number;
   voltageScale: number;
   nernstScale: number;
+  chargeWallInset: number;
 }
 
 interface DisplayParams {
@@ -42,6 +46,7 @@
   boxHeight: number;
   leftWall: number;
   rightWall: number;
+  negX: number;
   channelYMin: number;
   channelYMax: number;
   initialLeftCount: number;
@@ -100,7 +105,8 @@
   electricStrength: -0.35,
   channelWidth: 20,
   voltageScale: DEFAULT_POTENTIAL_SCALE,
-  nernstScale: DEFAULT_POTENTIAL_SCALE
+  nernstScale: DEFAULT_POTENTIAL_SCALE,
+  chargeWallInset: DEFAULT_CHARGE_WALL_INSET
 };
 
 const defaultDisplay: DisplayParams = { pointSize: 2.2, playbackSpeed: 1, targetFps: 30, traceYLimit: 20 };
@@ -139,7 +145,8 @@
     electricStrength: clamp(params.electricStrength, -10, 10),
     channelWidth: clamp(params.channelWidth, 0.5, boxHeight - 2),
     voltageScale: clamp(params.voltageScale, 1, 500),
-    nernstScale: clamp(params.nernstScale, 1, 500)
+    nernstScale: clamp(params.nernstScale, 1, 500),
+    chargeWallInset: clamp(params.chargeWallInset, 0.5, Math.max(0.5, boxWidth - 1))
   };
 }
 
@@ -186,6 +193,7 @@
     boxHeight: p.boxHeight,
     leftWall,
     rightWall,
+    negX: leftEdgeChargeWallX(p.boxWidth, p.chargeWallInset),
     channelYMin,
     channelYMax,
     initialLeftCount: 0,

file update
M /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
@@ -212,3 +212,2 @@
   const halfH = state.boxHeight / 2;
-  const driftX = params.electricStrength * clamp(state.vProxy / Math.max(1, params.voltageScale), -4, 4);
   for (let i = 0; i < state.numParticles; i += 1) {
@@ -216,3 +215,5 @@
     const yPrev = state.y[i];
-    const xTrial = reflectIntoBounds(xPrev + (rng.normal(0, params.diffusionSd) + driftX) * state.dt, -halfW, halfW);
+    const dx = state.negX - xPrev;
+    const driftX = wallFieldDrift(dx, params.electricStrength, 1);
+    const xTrial = reflectIntoBounds(xPrev + (rng.normal(0, params.diffusionSd) + driftX) * state.dt, state.negX, halfW);
     const yNew = reflectIntoBounds(yPrev + rng.normal(0, params.diffusionSd) * state.dt, -halfH, halfH);
@@ -223,3 +224,5 @@
     if ((tryingToCrossLeft || tryingToCrossRight) && !inChannel) {
-      xNew = tryingToCrossLeft ? reflectIntoBounds(2 * state.rightWall - xTrial, -halfW, halfW) : reflectIntoBounds(2 * state.leftWall - xTrial, -halfW, halfW);
+      xNew = tryingToCrossLeft
+        ? reflectIntoBounds(2 * state.rightWall - xTrial, state.negX, halfW)
+        : reflectIntoBounds(2 * state.leftWall - xTrial, state.negX, halfW);
     } else if (!inChannel && xTrial > state.leftWall && xTrial < state.rightWall) {
apply_patch(auto_approved=true) exited 0 in 100ms:
Success. Updated the following files:
M src/pages/inspect_resting_potential_nernst.ts
file update:
diff --git a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
index 7e72d55cd35ae3c2444359ae7ab9a07452c4bf12..2069ca956b997a81305ffb7862ae0b9c422f732b
--- a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
+++ b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
@@ -3,11 +3,14 @@
   DEFAULT_DIFFUSION_SD,
   DEFAULT_NUM_PARTICLES,
   DEFAULT_POTENTIAL_SCALE,
+  DEFAULT_CHARGE_WALL_INSET,
   drawMembraneWall,
   MAX_PARTICLES,
   SIM_COLORS,
   centeredChannelBounds,
-  fieldColor
+  fieldColor,
+  leftEdgeChargeWallX,
+  wallFieldDrift
 } from './sim_shared';
 
 interface SimParams {
@@ -22,6 +25,7 @@
   channelWidth: number;
   voltageScale: number;
   nernstScale: number;
+  chargeWallInset: number;
 }
 
 interface DisplayParams {
@@ -42,6 +46,7 @@
   boxHeight: number;
   leftWall: number;
   rightWall: number;
+  negX: number;
   channelYMin: number;
   channelYMax: number;
   initialLeftCount: number;
@@ -100,7 +105,8 @@
   electricStrength: -0.35,
   channelWidth: 20,
   voltageScale: DEFAULT_POTENTIAL_SCALE,
-  nernstScale: DEFAULT_POTENTIAL_SCALE
+  nernstScale: DEFAULT_POTENTIAL_SCALE,
+  chargeWallInset: DEFAULT_CHARGE_WALL_INSET
 };
 
 const defaultDisplay: DisplayParams = { pointSize: 2.2, playbackSpeed: 1, targetFps: 30, traceYLimit: 20 };
@@ -139,7 +145,8 @@
     electricStrength: clamp(params.electricStrength, -10, 10),
     channelWidth: clamp(params.channelWidth, 0.5, boxHeight - 2),
     voltageScale: clamp(params.voltageScale, 1, 500),
-    nernstScale: clamp(params.nernstScale, 1, 500)
+    nernstScale: clamp(params.nernstScale, 1, 500),
+    chargeWallInset: clamp(params.chargeWallInset, 0.5, Math.max(0.5, boxWidth - 1))
   };
 }
 
@@ -186,6 +193,7 @@
     boxHeight: p.boxHeight,
     leftWall,
     rightWall,
+    negX: leftEdgeChargeWallX(p.boxWidth, p.chargeWallInset),
     channelYMin,
     channelYMax,
     initialLeftCount: 0,
@@ -202,18 +210,21 @@
 function stepState(state: LiveState, params: SimParams, rng: Rng): void {
   const halfW = state.boxWidth / 2;
   const halfH = state.boxHeight / 2;
-  const driftX = params.electricStrength * clamp(state.vProxy / Math.max(1, params.voltageScale), -4, 4);
   for (let i = 0; i < state.numParticles; i += 1) {
     const xPrev = state.x[i];
     const yPrev = state.y[i];
-    const xTrial = reflectIntoBounds(xPrev + (rng.normal(0, params.diffusionSd) + driftX) * state.dt, -halfW, halfW);
+    const dx = state.negX - xPrev;
+    const driftX = wallFieldDrift(dx, params.electricStrength, 1);
+    const xTrial = reflectIntoBounds(xPrev + (rng.normal(0, params.diffusionSd) + driftX) * state.dt, state.negX, halfW);
     const yNew = reflectIntoBounds(yPrev + rng.normal(0, params.diffusionSd) * state.dt, -halfH, halfH);
     let xNew = xTrial;
     const tryingToCrossLeft = xPrev > state.rightWall && xTrial <= state.rightWall;
     const tryingToCrossRight = xPrev < state.leftWall && xTrial >= state.leftWall;
     const inChannel = yNew >= state.channelYMin && yNew <= state.channelYMax;
     if ((tryingToCrossLeft || tryingToCrossRight) && !inChannel) {
-      xNew = tryingToCrossLeft ? reflectIntoBounds(2 * state.rightWall - xTrial, -halfW, halfW) : reflectIntoBounds(2 * state.leftWall - xTrial, -halfW, halfW);
+      xNew = tryingToCrossLeft
+        ? reflectIntoBounds(2 * state.rightWall - xTrial, state.negX, halfW)
+        : reflectIntoBounds(2 * state.leftWall - xTrial, state.negX, halfW);
     } else if (!inChannel && xTrial > state.leftWall && xTrial < state.rightWall) {
       xNew = xPrev < 0 ? state.leftWall : state.rightWall;
     }

file update:
diff --git a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
index 7e72d55cd35ae3c2444359ae7ab9a07452c4bf12..2069ca956b997a81305ffb7862ae0b9c422f732b
--- a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
+++ b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
@@ -3,11 +3,14 @@
   DEFAULT_DIFFUSION_SD,
   DEFAULT_NUM_PARTICLES,
   DEFAULT_POTENTIAL_SCALE,
+  DEFAULT_CHARGE_WALL_INSET,
   drawMembraneWall,
   MAX_PARTICLES,
   SIM_COLORS,
   centeredChannelBounds,
-  fieldColor
+  fieldColor,
+  leftEdgeChargeWallX,
+  wallFieldDrift
 } from './sim_shared';
 
 interface SimParams {
@@ -22,6 +25,7 @@
   channelWidth: number;
   voltageScale: number;
   nernstScale: number;
+  chargeWallInset: number;
 }
 
 interface DisplayParams {
@@ -42,6 +46,7 @@
   boxHeight: number;
   leftWall: number;
   rightWall: number;
+  negX: number;
   channelYMin: number;
   channelYMax: number;
   initialLeftCount: number;
@@ -100,7 +105,8 @@
   electricStrength: -0.35,
   channelWidth: 20,
   voltageScale: DEFAULT_POTENTIAL_SCALE,
-  nernstScale: DEFAULT_POTENTIAL_SCALE
+  nernstScale: DEFAULT_POTENTIAL_SCALE,
+  chargeWallInset: DEFAULT_CHARGE_WALL_INSET
 };
 
 const defaultDisplay: DisplayParams = { pointSize: 2.2, playbackSpeed: 1, targetFps: 30, traceYLimit: 20 };
@@ -139,7 +145,8 @@
     electricStrength: clamp(params.electricStrength, -10, 10),
     channelWidth: clamp(params.channelWidth, 0.5, boxHeight - 2),
     voltageScale: clamp(params.voltageScale, 1, 500),
-    nernstScale: clamp(params.nernstScale, 1, 500)
+    nernstScale: clamp(params.nernstScale, 1, 500),
+    chargeWallInset: clamp(params.chargeWallInset, 0.5, Math.max(0.5, boxWidth - 1))
   };
 }
 
@@ -186,6 +193,7 @@
     boxHeight: p.boxHeight,
     leftWall,
     rightWall,
+    negX: leftEdgeChargeWallX(p.boxWidth, p.chargeWallInset),
     channelYMin,
     channelYMax,
     initialLeftCount: 0,
@@ -202,18 +210,21 @@
 function stepState(state: LiveState, params: SimParams, rng: Rng): void {
   const halfW = state.boxWidth / 2;
   const halfH = state.boxHeight / 2;
-  const driftX = params.electricStrength * clamp(state.vProxy / Math.max(1, params.voltageScale), -4, 4);
   for (let i = 0; i < state.numParticles; i += 1) {
     const xPrev = state.x[i];
     const yPrev = state.y[i];
-    const xTrial = reflectIntoBounds(xPrev + (rng.normal(0, params.diffusionSd) + driftX) * state.dt, -halfW, halfW);
+    const dx = state.negX - xPrev;
+    const driftX = wallFieldDrift(dx, params.electricStrength, 1);
+    const xTrial = reflectIntoBounds(xPrev + (rng.normal(0, params.diffusionSd) + driftX) * state.dt, state.negX, halfW);
     const yNew = reflectIntoBounds(yPrev + rng.normal(0, params.diffusionSd) * state.dt, -halfH, halfH);
     let xNew = xTrial;
     const tryingToCrossLeft = xPrev > state.rightWall && xTrial <= state.rightWall;
     const tryingToCrossRight = xPrev < state.leftWall && xTrial >= state.leftWall;
     const inChannel = yNew >= state.channelYMin && yNew <= state.channelYMax;
     if ((tryingToCrossLeft || tryingToCrossRight) && !inChannel) {
-      xNew = tryingToCrossLeft ? reflectIntoBounds(2 * state.rightWall - xTrial, -halfW, halfW) : reflectIntoBounds(2 * state.leftWall - xTrial, -halfW, halfW);
+      xNew = tryingToCrossLeft
+        ? reflectIntoBounds(2 * state.rightWall - xTrial, state.negX, halfW)
+        : reflectIntoBounds(2 * state.leftWall - xTrial, state.negX, halfW);
     } else if (!inChannel && xTrial > state.leftWall && xTrial < state.rightWall) {
       xNew = xPrev < 0 ? state.leftWall : state.rightWall;
     }

file update
M /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
@@ -369,4 +369,4 @@
   });
-  const [chargeWallX] = toPx((state.leftWall + state.rightWall) / 2, 0);
-  ctx.strokeStyle = fieldColor(state.vProxy);
+  const [chargeWallX] = toPx(state.negX, 0);
+  ctx.strokeStyle = fieldColor(simParams.electricStrength);
   ctx.lineWidth = 1.5 * dpr;
apply_patch(auto_approved=true) exited 0 in 63ms:
Success. Updated the following files:
M src/pages/inspect_resting_potential_nernst.ts
file update:
diff --git a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
index 7e72d55cd35ae3c2444359ae7ab9a07452c4bf12..83e06906951293677a7a336036c482b0d8b4bc26
--- a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
+++ b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
@@ -3,11 +3,14 @@
   DEFAULT_DIFFUSION_SD,
   DEFAULT_NUM_PARTICLES,
   DEFAULT_POTENTIAL_SCALE,
+  DEFAULT_CHARGE_WALL_INSET,
   drawMembraneWall,
   MAX_PARTICLES,
   SIM_COLORS,
   centeredChannelBounds,
-  fieldColor
+  fieldColor,
+  leftEdgeChargeWallX,
+  wallFieldDrift
 } from './sim_shared';
 
 interface SimParams {
@@ -22,6 +25,7 @@
   channelWidth: number;
   voltageScale: number;
   nernstScale: number;
+  chargeWallInset: number;
 }
 
 interface DisplayParams {
@@ -42,6 +46,7 @@
   boxHeight: number;
   leftWall: number;
   rightWall: number;
+  negX: number;
   channelYMin: number;
   channelYMax: number;
   initialLeftCount: number;
@@ -100,7 +105,8 @@
   electricStrength: -0.35,
   channelWidth: 20,
   voltageScale: DEFAULT_POTENTIAL_SCALE,
-  nernstScale: DEFAULT_POTENTIAL_SCALE
+  nernstScale: DEFAULT_POTENTIAL_SCALE,
+  chargeWallInset: DEFAULT_CHARGE_WALL_INSET
 };
 
 const defaultDisplay: DisplayParams = { pointSize: 2.2, playbackSpeed: 1, targetFps: 30, traceYLimit: 20 };
@@ -139,7 +145,8 @@
     electricStrength: clamp(params.electricStrength, -10, 10),
     channelWidth: clamp(params.channelWidth, 0.5, boxHeight - 2),
     voltageScale: clamp(params.voltageScale, 1, 500),
-    nernstScale: clamp(params.nernstScale, 1, 500)
+    nernstScale: clamp(params.nernstScale, 1, 500),
+    chargeWallInset: clamp(params.chargeWallInset, 0.5, Math.max(0.5, boxWidth - 1))
   };
 }
 
@@ -186,6 +193,7 @@
     boxHeight: p.boxHeight,
     leftWall,
     rightWall,
+    negX: leftEdgeChargeWallX(p.boxWidth, p.chargeWallInset),
     channelYMin,
     channelYMax,
     initialLeftCount: 0,
@@ -202,18 +210,21 @@
 function stepState(state: LiveState, params: SimParams, rng: Rng): void {
   const halfW = state.boxWidth / 2;
   const halfH = state.boxHeight / 2;
-  const driftX = params.electricStrength * clamp(state.vProxy / Math.max(1, params.voltageScale), -4, 4);
   for (let i = 0; i < state.numParticles; i += 1) {
     const xPrev = state.x[i];
     const yPrev = state.y[i];
-    const xTrial = reflectIntoBounds(xPrev + (rng.normal(0, params.diffusionSd) + driftX) * state.dt, -halfW, halfW);
+    const dx = state.negX - xPrev;
+    const driftX = wallFieldDrift(dx, params.electricStrength, 1);
+    const xTrial = reflectIntoBounds(xPrev + (rng.normal(0, params.diffusionSd) + driftX) * state.dt, state.negX, halfW);
     const yNew = reflectIntoBounds(yPrev + rng.normal(0, params.diffusionSd) * state.dt, -halfH, halfH);
     let xNew = xTrial;
     const tryingToCrossLeft = xPrev > state.rightWall && xTrial <= state.rightWall;
     const tryingToCrossRight = xPrev < state.leftWall && xTrial >= state.leftWall;
     const inChannel = yNew >= state.channelYMin && yNew <= state.channelYMax;
     if ((tryingToCrossLeft || tryingToCrossRight) && !inChannel) {
-      xNew = tryingToCrossLeft ? reflectIntoBounds(2 * state.rightWall - xTrial, -halfW, halfW) : reflectIntoBounds(2 * state.leftWall - xTrial, -halfW, halfW);
+      xNew = tryingToCrossLeft
+        ? reflectIntoBounds(2 * state.rightWall - xTrial, state.negX, halfW)
+        : reflectIntoBounds(2 * state.leftWall - xTrial, state.negX, halfW);
     } else if (!inChannel && xTrial > state.leftWall && xTrial < state.rightWall) {
       xNew = xPrev < 0 ? state.leftWall : state.rightWall;
     }
@@ -356,8 +367,8 @@
     dpr,
     channels: [{ top: cTop, bottom: cBottom }]
   });
-  const [chargeWallX] = toPx((state.leftWall + state.rightWall) / 2, 0);
-  ctx.strokeStyle = fieldColor(state.vProxy);
+  const [chargeWallX] = toPx(state.negX, 0);
+  ctx.strokeStyle = fieldColor(simParams.electricStrength);
   ctx.lineWidth = 1.5 * dpr;
   ctx.setLineDash([5 * dpr, 4 * dpr]);
   ctx.beginPath();

file update:
diff --git a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
index 7e72d55cd35ae3c2444359ae7ab9a07452c4bf12..83e06906951293677a7a336036c482b0d8b4bc26
--- a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
+++ b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
@@ -3,11 +3,14 @@
   DEFAULT_DIFFUSION_SD,
   DEFAULT_NUM_PARTICLES,
   DEFAULT_POTENTIAL_SCALE,
+  DEFAULT_CHARGE_WALL_INSET,
   drawMembraneWall,
   MAX_PARTICLES,
   SIM_COLORS,
   centeredChannelBounds,
-  fieldColor
+  fieldColor,
+  leftEdgeChargeWallX,
+  wallFieldDrift
 } from './sim_shared';
 
 interface SimParams {
@@ -22,6 +25,7 @@
   channelWidth: number;
   voltageScale: number;
   nernstScale: number;
+  chargeWallInset: number;
 }
 
 interface DisplayParams {
@@ -42,6 +46,7 @@
   boxHeight: number;
   leftWall: number;
   rightWall: number;
+  negX: number;
   channelYMin: number;
   channelYMax: number;
   initialLeftCount: number;
@@ -100,7 +105,8 @@
   electricStrength: -0.35,
   channelWidth: 20,
   voltageScale: DEFAULT_POTENTIAL_SCALE,
-  nernstScale: DEFAULT_POTENTIAL_SCALE
+  nernstScale: DEFAULT_POTENTIAL_SCALE,
+  chargeWallInset: DEFAULT_CHARGE_WALL_INSET
 };
 
 const defaultDisplay: DisplayParams = { pointSize: 2.2, playbackSpeed: 1, targetFps: 30, traceYLimit: 20 };
@@ -139,7 +145,8 @@
     electricStrength: clamp(params.electricStrength, -10, 10),
     channelWidth: clamp(params.channelWidth, 0.5, boxHeight - 2),
     voltageScale: clamp(params.voltageScale, 1, 500),
-    nernstScale: clamp(params.nernstScale, 1, 500)
+    nernstScale: clamp(params.nernstScale, 1, 500),
+    chargeWallInset: clamp(params.chargeWallInset, 0.5, Math.max(0.5, boxWidth - 1))
   };
 }
 
@@ -186,6 +193,7 @@
     boxHeight: p.boxHeight,
     leftWall,
     rightWall,
+    negX: leftEdgeChargeWallX(p.boxWidth, p.chargeWallInset),
     channelYMin,
     channelYMax,
     initialLeftCount: 0,
@@ -202,18 +210,21 @@
 function stepState(state: LiveState, params: SimParams, rng: Rng): void {
   const halfW = state.boxWidth / 2;
   const halfH = state.boxHeight / 2;
-  const driftX = params.electricStrength * clamp(state.vProxy / Math.max(1, params.voltageScale), -4, 4);
   for (let i = 0; i < state.numParticles; i += 1) {
     const xPrev = state.x[i];
     const yPrev = state.y[i];
-    const xTrial = reflectIntoBounds(xPrev + (rng.normal(0, params.diffusionSd) + driftX) * state.dt, -halfW, halfW);
+    const dx = state.negX - xPrev;
+    const driftX = wallFieldDrift(dx, params.electricStrength, 1);
+    const xTrial = reflectIntoBounds(xPrev + (rng.normal(0, params.diffusionSd) + driftX) * state.dt, state.negX, halfW);
     const yNew = reflectIntoBounds(yPrev + rng.normal(0, params.diffusionSd) * state.dt, -halfH, halfH);
     let xNew = xTrial;
     const tryingToCrossLeft = xPrev > state.rightWall && xTrial <= state.rightWall;
     const tryingToCrossRight = xPrev < state.leftWall && xTrial >= state.leftWall;
     const inChannel = yNew >= state.channelYMin && yNew <= state.channelYMax;
     if ((tryingToCrossLeft || tryingToCrossRight) && !inChannel) {
-      xNew = tryingToCrossLeft ? reflectIntoBounds(2 * state.rightWall - xTrial, -halfW, halfW) : reflectIntoBounds(2 * state.leftWall - xTrial, -halfW, halfW);
+      xNew = tryingToCrossLeft
+        ? reflectIntoBounds(2 * state.rightWall - xTrial, state.negX, halfW)
+        : reflectIntoBounds(2 * state.leftWall - xTrial, state.negX, halfW);
     } else if (!inChannel && xTrial > state.leftWall && xTrial < state.rightWall) {
       xNew = xPrev < 0 ? state.leftWall : state.rightWall;
     }
@@ -356,8 +367,8 @@
     dpr,
     channels: [{ top: cTop, bottom: cBottom }]
   });
-  const [chargeWallX] = toPx((state.leftWall + state.rightWall) / 2, 0);
-  ctx.strokeStyle = fieldColor(state.vProxy);
+  const [chargeWallX] = toPx(state.negX, 0);
+  ctx.strokeStyle = fieldColor(simParams.electricStrength);
   ctx.lineWidth = 1.5 * dpr;
   ctx.setLineDash([5 * dpr, 4 * dpr]);
   ctx.beginPath();

codex
I’m now patching the UI text/legend/math markup block so the equation displays in proper math form and the labels reflect inside-left/outside-right semantics.
file update
M /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
@@ -397,3 +397,9 @@
         <li>Membrane potential is determined by relative ion concentration inside vs outside the cell.</li>
-        <li>At equilibrium (no net flux), this is given by the Nernst equation: V = S ln(C_out / C_in).</li>
+        <li>
+          At equilibrium (no net flux), this simulation uses
+          <math display="inline" aria-label="V equals S times natural log of C out over C in">
+            <mi>V</mi><mo>=</mo><mi>S</mi><mo>ln</mo><mo>(</mo><mfrac><msub><mi>C</mi><mtext>out</mtext></msub><msub><mi>C</mi><mtext>in</mtext></msub></mfrac><mo>)</mo>
+          </math>.
+        </li>
+        <li>Inside is the left compartment and outside is the right compartment; the charge wall is at the far-left inside boundary.</li>
       </ul>
@@ -410,3 +416,3 @@
               <div class="field"><label for="diffusion-sd">Diffusion SD</label><input id="diffusion-sd" type="number" min="0" max="20" step="0.01" /></div>
-              <div class="field"><label for="electric-strength">Electric strength (charge wall)</label><input id="electric-strength" type="number" min="-10" max="10" step="0.01" /></div>
+              <div class="field"><label for="electric-strength">Electric strength (inside charge wall)</label><input id="electric-strength" type="number" min="-10" max="10" step="0.01" /></div>
               <div class="field"><label for="channel-width">Channel width</label><input id="channel-width" type="number" min="0.5" step="0.5" /></div>
@@ -449,4 +455,9 @@
                 White is the Nernst prediction for a single permeant cation:
-                <code>V = S ln(C_out / C_in) = S ln(C_right / C_left)</code>.
               </p>
+              <math display="block" aria-label="V equals S times natural log of C out over C in equals S times natural log of C right over C left">
+                <mi>V</mi><mo>=</mo><mi>S</mi><mo>ln</mo><mo>(</mo><mfrac><msub><mi>C</mi><mtext>out</mtext></msub><msub><mi>C</mi><mtext>in</mtext></msub></mfrac><mo>)</mo>
+                <mo>=</mo>
+                <mi>S</mi><mo>ln</mo><mo>(</mo><mfrac><msub><mi>C</mi><mtext>right</mtext></msub><msub><mi>C</mi><mtext>left</mtext></msub></mfrac><mo>)</mo>
+              </math>
+              <p>In this layout, inside is left and outside is right.</p>
             </div>
@@ -462,3 +473,3 @@
           <span><span class="swatch" style="background:${SIM_COLORS.ionA}"></span>Positive ion</span>
-          <span><span class="swatch" style="background:${SIM_COLORS.fieldNegative}"></span>Charge wall</span>
+          <span><span class="swatch" style="background:${SIM_COLORS.fieldNegative}"></span>Inside charge wall (far left)</span>
           <span><span class="swatch" style="background:${SIM_COLORS.totalTrace}"></span>Simulated membrane potential trace</span>
apply_patch(auto_approved=true) exited 0 in 105ms:
Success. Updated the following files:
M src/pages/inspect_resting_potential_nernst.ts
file update:
diff --git a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
index 7e72d55cd35ae3c2444359ae7ab9a07452c4bf12..03d9d72077563e57d0e377b93c7a3af019925a29
--- a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
+++ b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
@@ -3,11 +3,14 @@
   DEFAULT_DIFFUSION_SD,
   DEFAULT_NUM_PARTICLES,
   DEFAULT_POTENTIAL_SCALE,
+  DEFAULT_CHARGE_WALL_INSET,
   drawMembraneWall,
   MAX_PARTICLES,
   SIM_COLORS,
   centeredChannelBounds,
-  fieldColor
+  fieldColor,
+  leftEdgeChargeWallX,
+  wallFieldDrift
 } from './sim_shared';
 
 interface SimParams {
@@ -22,6 +25,7 @@
   channelWidth: number;
   voltageScale: number;
   nernstScale: number;
+  chargeWallInset: number;
 }
 
 interface DisplayParams {
@@ -42,6 +46,7 @@
   boxHeight: number;
   leftWall: number;
   rightWall: number;
+  negX: number;
   channelYMin: number;
   channelYMax: number;
   initialLeftCount: number;
@@ -100,7 +105,8 @@
   electricStrength: -0.35,
   channelWidth: 20,
   voltageScale: DEFAULT_POTENTIAL_SCALE,
-  nernstScale: DEFAULT_POTENTIAL_SCALE
+  nernstScale: DEFAULT_POTENTIAL_SCALE,
+  chargeWallInset: DEFAULT_CHARGE_WALL_INSET
 };
 
 const defaultDisplay: DisplayParams = { pointSize: 2.2, playbackSpeed: 1, targetFps: 30, traceYLimit: 20 };
@@ -139,7 +145,8 @@
     electricStrength: clamp(params.electricStrength, -10, 10),
     channelWidth: clamp(params.channelWidth, 0.5, boxHeight - 2),
     voltageScale: clamp(params.voltageScale, 1, 500),
-    nernstScale: clamp(params.nernstScale, 1, 500)
+    nernstScale: clamp(params.nernstScale, 1, 500),
+    chargeWallInset: clamp(params.chargeWallInset, 0.5, Math.max(0.5, boxWidth - 1))
   };
 }
 
@@ -186,6 +193,7 @@
     boxHeight: p.boxHeight,
     leftWall,
     rightWall,
+    negX: leftEdgeChargeWallX(p.boxWidth, p.chargeWallInset),
     channelYMin,
     channelYMax,
     initialLeftCount: 0,
@@ -202,18 +210,21 @@
 function stepState(state: LiveState, params: SimParams, rng: Rng): void {
   const halfW = state.boxWidth / 2;
   const halfH = state.boxHeight / 2;
-  const driftX = params.electricStrength * clamp(state.vProxy / Math.max(1, params.voltageScale), -4, 4);
   for (let i = 0; i < state.numParticles; i += 1) {
     const xPrev = state.x[i];
     const yPrev = state.y[i];
-    const xTrial = reflectIntoBounds(xPrev + (rng.normal(0, params.diffusionSd) + driftX) * state.dt, -halfW, halfW);
+    const dx = state.negX - xPrev;
+    const driftX = wallFieldDrift(dx, params.electricStrength, 1);
+    const xTrial = reflectIntoBounds(xPrev + (rng.normal(0, params.diffusionSd) + driftX) * state.dt, state.negX, halfW);
     const yNew = reflectIntoBounds(yPrev + rng.normal(0, params.diffusionSd) * state.dt, -halfH, halfH);
     let xNew = xTrial;
     const tryingToCrossLeft = xPrev > state.rightWall && xTrial <= state.rightWall;
     const tryingToCrossRight = xPrev < state.leftWall && xTrial >= state.leftWall;
     const inChannel = yNew >= state.channelYMin && yNew <= state.channelYMax;
     if ((tryingToCrossLeft || tryingToCrossRight) && !inChannel) {
-      xNew = tryingToCrossLeft ? reflectIntoBounds(2 * state.rightWall - xTrial, -halfW, halfW) : reflectIntoBounds(2 * state.leftWall - xTrial, -halfW, halfW);
+      xNew = tryingToCrossLeft
+        ? reflectIntoBounds(2 * state.rightWall - xTrial, state.negX, halfW)
+        : reflectIntoBounds(2 * state.leftWall - xTrial, state.negX, halfW);
     } else if (!inChannel && xTrial > state.leftWall && xTrial < state.rightWall) {
       xNew = xPrev < 0 ? state.leftWall : state.rightWall;
     }
@@ -356,8 +367,8 @@
     dpr,
     channels: [{ top: cTop, bottom: cBottom }]
   });
-  const [chargeWallX] = toPx((state.leftWall + state.rightWall) / 2, 0);
-  ctx.strokeStyle = fieldColor(state.vProxy);
+  const [chargeWallX] = toPx(state.negX, 0);
+  ctx.strokeStyle = fieldColor(simParams.electricStrength);
   ctx.lineWidth = 1.5 * dpr;
   ctx.setLineDash([5 * dpr, 4 * dpr]);
   ctx.beginPath();
@@ -384,7 +395,13 @@
       <h1>Nernst Equilibrium for One Ion</h1>
       <ul class="key-points">
         <li>Membrane potential is determined by relative ion concentration inside vs outside the cell.</li>
-        <li>At equilibrium (no net flux), this is given by the Nernst equation: V = S ln(C_out / C_in).</li>
+        <li>
+          At equilibrium (no net flux), this simulation uses
+          <math display="inline" aria-label="V equals S times natural log of C out over C in">
+            <mi>V</mi><mo>=</mo><mi>S</mi><mo>ln</mo><mo>(</mo><mfrac><msub><mi>C</mi><mtext>out</mtext></msub><msub><mi>C</mi><mtext>in</mtext></msub></mfrac><mo>)</mo>
+          </math>.
+        </li>
+        <li>Inside is the left compartment and outside is the right compartment; the charge wall is at the far-left inside boundary.</li>
       </ul>
     </header>
     <div class="sim-layout">
@@ -397,7 +414,7 @@
             <div class="control-grid">
               <div class="field"><label for="num-particles">Particles</label><input id="num-particles" type="number" min="10" max="5000" step="1" /></div>
               <div class="field"><label for="diffusion-sd">Diffusion SD</label><input id="diffusion-sd" type="number" min="0" max="20" step="0.01" /></div>
-              <div class="field"><label for="electric-strength">Electric strength (charge wall)</label><input id="electric-strength" type="number" min="-10" max="10" step="0.01" /></div>
+              <div class="field"><label for="electric-strength">Electric strength (inside charge wall)</label><input id="electric-strength" type="number" min="-10" max="10" step="0.01" /></div>
               <div class="field"><label for="channel-width">Channel width</label><input id="channel-width" type="number" min="0.5" step="0.5" /></div>
               <div class="field"><label for="playback-speed">Playback speed</label><input id="playback-speed" type="number" min="0.1" max="8" step="0.1" /></div>
               <div class="field"><label for="seed">Seed</label><input id="seed" type="number" min="0" max="4294967295" step="1" /></div>
@@ -436,8 +453,13 @@
               <p>
                 Purple is the simulated membrane potential from compartment concentrations.
                 White is the Nernst prediction for a single permeant cation:
-                <code>V = S ln(C_out / C_in) = S ln(C_right / C_left)</code>.
               </p>
+              <math display="block" aria-label="V equals S times natural log of C out over C in equals S times natural log of C right over C left">
+                <mi>V</mi><mo>=</mo><mi>S</mi><mo>ln</mo><mo>(</mo><mfrac><msub><mi>C</mi><mtext>out</mtext></msub><msub><mi>C</mi><mtext>in</mtext></msub></mfrac><mo>)</mo>
+                <mo>=</mo>
+                <mi>S</mi><mo>ln</mo><mo>(</mo><mfrac><msub><mi>C</mi><mtext>right</mtext></msub><msub><mi>C</mi><mtext>left</mtext></msub></mfrac><mo>)</mo>
+              </math>
+              <p>In this layout, inside is left and outside is right.</p>
             </div>
           </div>
         </div>
@@ -449,7 +471,7 @@
         </div>
         <div class="legend">
           <span><span class="swatch" style="background:${SIM_COLORS.ionA}"></span>Positive ion</span>
-          <span><span class="swatch" style="background:${SIM_COLORS.fieldNegative}"></span>Charge wall</span>
+          <span><span class="swatch" style="background:${SIM_COLORS.fieldNegative}"></span>Inside charge wall (far left)</span>
           <span><span class="swatch" style="background:${SIM_COLORS.totalTrace}"></span>Simulated membrane potential trace</span>
           <span><span class="swatch" style="background:${SIM_COLORS.predictionTrace}"></span>Nernst prediction trace</span>
         </div>

file update:
diff --git a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
index 7e72d55cd35ae3c2444359ae7ab9a07452c4bf12..03d9d72077563e57d0e377b93c7a3af019925a29
--- a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
+++ b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
@@ -3,11 +3,14 @@
   DEFAULT_DIFFUSION_SD,
   DEFAULT_NUM_PARTICLES,
   DEFAULT_POTENTIAL_SCALE,
+  DEFAULT_CHARGE_WALL_INSET,
   drawMembraneWall,
   MAX_PARTICLES,
   SIM_COLORS,
   centeredChannelBounds,
-  fieldColor
+  fieldColor,
+  leftEdgeChargeWallX,
+  wallFieldDrift
 } from './sim_shared';
 
 interface SimParams {
@@ -22,6 +25,7 @@
   channelWidth: number;
   voltageScale: number;
   nernstScale: number;
+  chargeWallInset: number;
 }
 
 interface DisplayParams {
@@ -42,6 +46,7 @@
   boxHeight: number;
   leftWall: number;
   rightWall: number;
+  negX: number;
   channelYMin: number;
   channelYMax: number;
   initialLeftCount: number;
@@ -100,7 +105,8 @@
   electricStrength: -0.35,
   channelWidth: 20,
   voltageScale: DEFAULT_POTENTIAL_SCALE,
-  nernstScale: DEFAULT_POTENTIAL_SCALE
+  nernstScale: DEFAULT_POTENTIAL_SCALE,
+  chargeWallInset: DEFAULT_CHARGE_WALL_INSET
 };
 
 const defaultDisplay: DisplayParams = { pointSize: 2.2, playbackSpeed: 1, targetFps: 30, traceYLimit: 20 };
@@ -139,7 +145,8 @@
     electricStrength: clamp(params.electricStrength, -10, 10),
     channelWidth: clamp(params.channelWidth, 0.5, boxHeight - 2),
     voltageScale: clamp(params.voltageScale, 1, 500),
-    nernstScale: clamp(params.nernstScale, 1, 500)
+    nernstScale: clamp(params.nernstScale, 1, 500),
+    chargeWallInset: clamp(params.chargeWallInset, 0.5, Math.max(0.5, boxWidth - 1))
   };
 }
 
@@ -186,6 +193,7 @@
     boxHeight: p.boxHeight,
     leftWall,
     rightWall,
+    negX: leftEdgeChargeWallX(p.boxWidth, p.chargeWallInset),
     channelYMin,
     channelYMax,
     initialLeftCount: 0,
@@ -202,18 +210,21 @@
 function stepState(state: LiveState, params: SimParams, rng: Rng): void {
   const halfW = state.boxWidth / 2;
   const halfH = state.boxHeight / 2;
-  const driftX = params.electricStrength * clamp(state.vProxy / Math.max(1, params.voltageScale), -4, 4);
   for (let i = 0; i < state.numParticles; i += 1) {
     const xPrev = state.x[i];
     const yPrev = state.y[i];
-    const xTrial = reflectIntoBounds(xPrev + (rng.normal(0, params.diffusionSd) + driftX) * state.dt, -halfW, halfW);
+    const dx = state.negX - xPrev;
+    const driftX = wallFieldDrift(dx, params.electricStrength, 1);
+    const xTrial = reflectIntoBounds(xPrev + (rng.normal(0, params.diffusionSd) + driftX) * state.dt, state.negX, halfW);
     const yNew = reflectIntoBounds(yPrev + rng.normal(0, params.diffusionSd) * state.dt, -halfH, halfH);
     let xNew = xTrial;
     const tryingToCrossLeft = xPrev > state.rightWall && xTrial <= state.rightWall;
     const tryingToCrossRight = xPrev < state.leftWall && xTrial >= state.leftWall;
     const inChannel = yNew >= state.channelYMin && yNew <= state.channelYMax;
     if ((tryingToCrossLeft || tryingToCrossRight) && !inChannel) {
-      xNew = tryingToCrossLeft ? reflectIntoBounds(2 * state.rightWall - xTrial, -halfW, halfW) : reflectIntoBounds(2 * state.leftWall - xTrial, -halfW, halfW);
+      xNew = tryingToCrossLeft
+        ? reflectIntoBounds(2 * state.rightWall - xTrial, state.negX, halfW)
+        : reflectIntoBounds(2 * state.leftWall - xTrial, state.negX, halfW);
     } else if (!inChannel && xTrial > state.leftWall && xTrial < state.rightWall) {
       xNew = xPrev < 0 ? state.leftWall : state.rightWall;
     }
@@ -356,8 +367,8 @@
     dpr,
     channels: [{ top: cTop, bottom: cBottom }]
   });
-  const [chargeWallX] = toPx((state.leftWall + state.rightWall) / 2, 0);
-  ctx.strokeStyle = fieldColor(state.vProxy);
+  const [chargeWallX] = toPx(state.negX, 0);
+  ctx.strokeStyle = fieldColor(simParams.electricStrength);
   ctx.lineWidth = 1.5 * dpr;
   ctx.setLineDash([5 * dpr, 4 * dpr]);
   ctx.beginPath();
@@ -384,7 +395,13 @@
       <h1>Nernst Equilibrium for One Ion</h1>
       <ul class="key-points">
         <li>Membrane potential is determined by relative ion concentration inside vs outside the cell.</li>
-        <li>At equilibrium (no net flux), this is given by the Nernst equation: V = S ln(C_out / C_in).</li>
+        <li>
+          At equilibrium (no net flux), this simulation uses
+          <math display="inline" aria-label="V equals S times natural log of C out over C in">
+            <mi>V</mi><mo>=</mo><mi>S</mi><mo>ln</mo><mo>(</mo><mfrac><msub><mi>C</mi><mtext>out</mtext></msub><msub><mi>C</mi><mtext>in</mtext></msub></mfrac><mo>)</mo>
+          </math>.
+        </li>
+        <li>Inside is the left compartment and outside is the right compartment; the charge wall is at the far-left inside boundary.</li>
       </ul>
     </header>
     <div class="sim-layout">
@@ -397,7 +414,7 @@
             <div class="control-grid">
               <div class="field"><label for="num-particles">Particles</label><input id="num-particles" type="number" min="10" max="5000" step="1" /></div>
               <div class="field"><label for="diffusion-sd">Diffusion SD</label><input id="diffusion-sd" type="number" min="0" max="20" step="0.01" /></div>
-              <div class="field"><label for="electric-strength">Electric strength (charge wall)</label><input id="electric-strength" type="number" min="-10" max="10" step="0.01" /></div>
+              <div class="field"><label for="electric-strength">Electric strength (inside charge wall)</label><input id="electric-strength" type="number" min="-10" max="10" step="0.01" /></div>
               <div class="field"><label for="channel-width">Channel width</label><input id="channel-width" type="number" min="0.5" step="0.5" /></div>
               <div class="field"><label for="playback-speed">Playback speed</label><input id="playback-speed" type="number" min="0.1" max="8" step="0.1" /></div>
               <div class="field"><label for="seed">Seed</label><input id="seed" type="number" min="0" max="4294967295" step="1" /></div>
@@ -436,8 +453,13 @@
               <p>
                 Purple is the simulated membrane potential from compartment concentrations.
                 White is the Nernst prediction for a single permeant cation:
-                <code>V = S ln(C_out / C_in) = S ln(C_right / C_left)</code>.
               </p>
+              <math display="block" aria-label="V equals S times natural log of C out over C in equals S times natural log of C right over C left">
+                <mi>V</mi><mo>=</mo><mi>S</mi><mo>ln</mo><mo>(</mo><mfrac><msub><mi>C</mi><mtext>out</mtext></msub><msub><mi>C</mi><mtext>in</mtext></msub></mfrac><mo>)</mo>
+                <mo>=</mo>
+                <mi>S</mi><mo>ln</mo><mo>(</mo><mfrac><msub><mi>C</mi><mtext>right</mtext></msub><msub><mi>C</mi><mtext>left</mtext></msub></mfrac><mo>)</mo>
+              </math>
+              <p>In this layout, inside is left and outside is right.</p>
             </div>
           </div>
         </div>
@@ -449,7 +471,7 @@
         </div>
         <div class="legend">
           <span><span class="swatch" style="background:${SIM_COLORS.ionA}"></span>Positive ion</span>
-          <span><span class="swatch" style="background:${SIM_COLORS.fieldNegative}"></span>Charge wall</span>
+          <span><span class="swatch" style="background:${SIM_COLORS.fieldNegative}"></span>Inside charge wall (far left)</span>
           <span><span class="swatch" style="background:${SIM_COLORS.totalTrace}"></span>Simulated membrane potential trace</span>
           <span><span class="swatch" style="background:${SIM_COLORS.predictionTrace}"></span>Nernst prediction trace</span>
         </div>

file update:
diff --git a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
index 7e72d55cd35ae3c2444359ae7ab9a07452c4bf12..03d9d72077563e57d0e377b93c7a3af019925a29
--- a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
+++ b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
@@ -3,11 +3,14 @@
   DEFAULT_DIFFUSION_SD,
   DEFAULT_NUM_PARTICLES,
   DEFAULT_POTENTIAL_SCALE,
+  DEFAULT_CHARGE_WALL_INSET,
   drawMembraneWall,
   MAX_PARTICLES,
   SIM_COLORS,
   centeredChannelBounds,
-  fieldColor
+  fieldColor,
+  leftEdgeChargeWallX,
+  wallFieldDrift
 } from './sim_shared';
 
 interface SimParams {
@@ -22,6 +25,7 @@
   channelWidth: number;
   voltageScale: number;
   nernstScale: number;
+  chargeWallInset: number;
 }
 
 interface DisplayParams {
@@ -42,6 +46,7 @@
   boxHeight: number;
   leftWall: number;
   rightWall: number;
+  negX: number;
   channelYMin: number;
   channelYMax: number;
   initialLeftCount: number;
@@ -100,7 +105,8 @@
   electricStrength: -0.35,
   channelWidth: 20,
   voltageScale: DEFAULT_POTENTIAL_SCALE,
-  nernstScale: DEFAULT_POTENTIAL_SCALE
+  nernstScale: DEFAULT_POTENTIAL_SCALE,
+  chargeWallInset: DEFAULT_CHARGE_WALL_INSET
 };
 
 const defaultDisplay: DisplayParams = { pointSize: 2.2, playbackSpeed: 1, targetFps: 30, traceYLimit: 20 };
@@ -139,7 +145,8 @@
     electricStrength: clamp(params.electricStrength, -10, 10),
     channelWidth: clamp(params.channelWidth, 0.5, boxHeight - 2),
     voltageScale: clamp(params.voltageScale, 1, 500),
-    nernstScale: clamp(params.nernstScale, 1, 500)
+    nernstScale: clamp(params.nernstScale, 1, 500),
+    chargeWallInset: clamp(params.chargeWallInset, 0.5, Math.max(0.5, boxWidth - 1))
   };
 }
 
@@ -186,6 +193,7 @@
     boxHeight: p.boxHeight,
     leftWall,
     rightWall,
+    negX: leftEdgeChargeWallX(p.boxWidth, p.chargeWallInset),
     channelYMin,
     channelYMax,
     initialLeftCount: 0,
@@ -202,18 +210,21 @@
 function stepState(state: LiveState, params: SimParams, rng: Rng): void {
   const halfW = state.boxWidth / 2;
   const halfH = state.boxHeight / 2;
-  const driftX = params.electricStrength * clamp(state.vProxy / Math.max(1, params.voltageScale), -4, 4);
   for (let i = 0; i < state.numParticles; i += 1) {
     const xPrev = state.x[i];
     const yPrev = state.y[i];
-    const xTrial = reflectIntoBounds(xPrev + (rng.normal(0, params.diffusionSd) + driftX) * state.dt, -halfW, halfW);
+    const dx = state.negX - xPrev;
+    const driftX = wallFieldDrift(dx, params.electricStrength, 1);
+    const xTrial = reflectIntoBounds(xPrev + (rng.normal(0, params.diffusionSd) + driftX) * state.dt, state.negX, halfW);
     const yNew = reflectIntoBounds(yPrev + rng.normal(0, params.diffusionSd) * state.dt, -halfH, halfH);
     let xNew = xTrial;
     const tryingToCrossLeft = xPrev > state.rightWall && xTrial <= state.rightWall;
     const tryingToCrossRight = xPrev < state.leftWall && xTrial >= state.leftWall;
     const inChannel = yNew >= state.channelYMin && yNew <= state.channelYMax;
     if ((tryingToCrossLeft || tryingToCrossRight) && !inChannel) {
-      xNew = tryingToCrossLeft ? reflectIntoBounds(2 * state.rightWall - xTrial, -halfW, halfW) : reflectIntoBounds(2 * state.leftWall - xTrial, -halfW, halfW);
+      xNew = tryingToCrossLeft
+        ? reflectIntoBounds(2 * state.rightWall - xTrial, state.negX, halfW)
+        : reflectIntoBounds(2 * state.leftWall - xTrial, state.negX, halfW);
     } else if (!inChannel && xTrial > state.leftWall && xTrial < state.rightWall) {
       xNew = xPrev < 0 ? state.leftWall : state.rightWall;
     }
@@ -356,8 +367,8 @@
     dpr,
     channels: [{ top: cTop, bottom: cBottom }]
   });
-  const [chargeWallX] = toPx((state.leftWall + state.rightWall) / 2, 0);
-  ctx.strokeStyle = fieldColor(state.vProxy);
+  const [chargeWallX] = toPx(state.negX, 0);
+  ctx.strokeStyle = fieldColor(simParams.electricStrength);
   ctx.lineWidth = 1.5 * dpr;
   ctx.setLineDash([5 * dpr, 4 * dpr]);
   ctx.beginPath();
@@ -384,7 +395,13 @@
       <h1>Nernst Equilibrium for One Ion</h1>
       <ul class="key-points">
         <li>Membrane potential is determined by relative ion concentration inside vs outside the cell.</li>
-        <li>At equilibrium (no net flux), this is given by the Nernst equation: V = S ln(C_out / C_in).</li>
+        <li>
+          At equilibrium (no net flux), this simulation uses
+          <math display="inline" aria-label="V equals S times natural log of C out over C in">
+            <mi>V</mi><mo>=</mo><mi>S</mi><mo>ln</mo><mo>(</mo><mfrac><msub><mi>C</mi><mtext>out</mtext></msub><msub><mi>C</mi><mtext>in</mtext></msub></mfrac><mo>)</mo>
+          </math>.
+        </li>
+        <li>Inside is the left compartment and outside is the right compartment; the charge wall is at the far-left inside boundary.</li>
       </ul>
     </header>
     <div class="sim-layout">
@@ -397,7 +414,7 @@
             <div class="control-grid">
               <div class="field"><label for="num-particles">Particles</label><input id="num-particles" type="number" min="10" max="5000" step="1" /></div>
               <div class="field"><label for="diffusion-sd">Diffusion SD</label><input id="diffusion-sd" type="number" min="0" max="20" step="0.01" /></div>
-              <div class="field"><label for="electric-strength">Electric strength (charge wall)</label><input id="electric-strength" type="number" min="-10" max="10" step="0.01" /></div>
+              <div class="field"><label for="electric-strength">Electric strength (inside charge wall)</label><input id="electric-strength" type="number" min="-10" max="10" step="0.01" /></div>
               <div class="field"><label for="channel-width">Channel width</label><input id="channel-width" type="number" min="0.5" step="0.5" /></div>
               <div class="field"><label for="playback-speed">Playback speed</label><input id="playback-speed" type="number" min="0.1" max="8" step="0.1" /></div>
               <div class="field"><label for="seed">Seed</label><input id="seed" type="number" min="0" max="4294967295" step="1" /></div>
@@ -436,8 +453,13 @@
               <p>
                 Purple is the simulated membrane potential from compartment concentrations.
                 White is the Nernst prediction for a single permeant cation:
-                <code>V = S ln(C_out / C_in) = S ln(C_right / C_left)</code>.
               </p>
+              <math display="block" aria-label="V equals S times natural log of C out over C in equals S times natural log of C right over C left">
+                <mi>V</mi><mo>=</mo><mi>S</mi><mo>ln</mo><mo>(</mo><mfrac><msub><mi>C</mi><mtext>out</mtext></msub><msub><mi>C</mi><mtext>in</mtext></msub></mfrac><mo>)</mo>
+                <mo>=</mo>
+                <mi>S</mi><mo>ln</mo><mo>(</mo><mfrac><msub><mi>C</mi><mtext>right</mtext></msub><msub><mi>C</mi><mtext>left</mtext></msub></mfrac><mo>)</mo>
+              </math>
+              <p>In this layout, inside is left and outside is right.</p>
             </div>
           </div>
         </div>
@@ -449,7 +471,7 @@
         </div>
         <div class="legend">
           <span><span class="swatch" style="background:${SIM_COLORS.ionA}"></span>Positive ion</span>
-          <span><span class="swatch" style="background:${SIM_COLORS.fieldNegative}"></span>Charge wall</span>
+          <span><span class="swatch" style="background:${SIM_COLORS.fieldNegative}"></span>Inside charge wall (far left)</span>
           <span><span class="swatch" style="background:${SIM_COLORS.totalTrace}"></span>Simulated membrane potential trace</span>
           <span><span class="swatch" style="background:${SIM_COLORS.predictionTrace}"></span>Nernst prediction trace</span>
         </div>

exec
/bin/zsh -lc "nl -ba src/pages/inspect_resting_potential_nernst.ts | sed -n '520,700p'" in /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_web succeeded in 52ms:
   520	  randomSeed: getEl<HTMLButtonElement>('#random-seed')
   521	};
   522	
   523	let simParams: SimParams = { ...defaultSim };
   524	let displayParams: DisplayParams = { ...defaultDisplay };
   525	let currentSeed = randomSeed();
   526	let rng = new Rng(currentSeed);
   527	let state = createState(simParams, currentSeed);
   528	let trace = createTrace(state, simParams.T);
   529	let isPlaying = true;
   530	let lastTs = performance.now();
   531	let stepAccumulator = 0;
   532	
   533	function writeInputs(): void {
   534	  setNumberInput(inputs.numParticles, simParams.numParticles, 0);
   535	  setNumberInput(inputs.diffusionSd, simParams.diffusionSd, 3);
   536	  setNumberInput(inputs.electricStrength, simParams.electricStrength, 3);
   537	  setNumberInput(inputs.channelWidth, simParams.channelWidth, 2);
   538	  setNumberInput(inputs.playbackSpeed, displayParams.playbackSpeed, 2);
   539	  setNumberInput(inputs.seed, currentSeed, 0);
file update:
diff --git a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
index 7e72d55cd35ae3c2444359ae7ab9a07452c4bf12..03d9d72077563e57d0e377b93c7a3af019925a29
--- a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
+++ b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
@@ -3,11 +3,14 @@
   DEFAULT_DIFFUSION_SD,
   DEFAULT_NUM_PARTICLES,
   DEFAULT_POTENTIAL_SCALE,
+  DEFAULT_CHARGE_WALL_INSET,
   drawMembraneWall,
   MAX_PARTICLES,
   SIM_COLORS,
   centeredChannelBounds,
-  fieldColor
+  fieldColor,
+  leftEdgeChargeWallX,
+  wallFieldDrift
 } from './sim_shared';
 
 interface SimParams {
@@ -22,6 +25,7 @@
   channelWidth: number;
   voltageScale: number;
   nernstScale: number;
+  chargeWallInset: number;
 }
 
 interface DisplayParams {
@@ -42,6 +46,7 @@
   boxHeight: number;
   leftWall: number;
   rightWall: number;
+  negX: number;
   channelYMin: number;
   channelYMax: number;
   initialLeftCount: number;
@@ -100,7 +105,8 @@
   electricStrength: -0.35,
   channelWidth: 20,
   voltageScale: DEFAULT_POTENTIAL_SCALE,
-  nernstScale: DEFAULT_POTENTIAL_SCALE
+  nernstScale: DEFAULT_POTENTIAL_SCALE,
+  chargeWallInset: DEFAULT_CHARGE_WALL_INSET
 };
 
 const defaultDisplay: DisplayParams = { pointSize: 2.2, playbackSpeed: 1, targetFps: 30, traceYLimit: 20 };
@@ -139,7 +145,8 @@
     electricStrength: clamp(params.electricStrength, -10, 10),
     channelWidth: clamp(params.channelWidth, 0.5, boxHeight - 2),
     voltageScale: clamp(params.voltageScale, 1, 500),
-    nernstScale: clamp(params.nernstScale, 1, 500)
+    nernstScale: clamp(params.nernstScale, 1, 500),
+    chargeWallInset: clamp(params.chargeWallInset, 0.5, Math.max(0.5, boxWidth - 1))
   };
 }
 
@@ -186,6 +193,7 @@
     boxHeight: p.boxHeight,
     leftWall,
     rightWall,
+    negX: leftEdgeChargeWallX(p.boxWidth, p.chargeWallInset),
     channelYMin,
     channelYMax,
     initialLeftCount: 0,
@@ -202,18 +210,21 @@
 function stepState(state: LiveState, params: SimParams, rng: Rng): void {
   const halfW = state.boxWidth / 2;
   const halfH = state.boxHeight / 2;
-  const driftX = params.electricStrength * clamp(state.vProxy / Math.max(1, params.voltageScale), -4, 4);
   for (let i = 0; i < state.numParticles; i += 1) {
     const xPrev = state.x[i];
     const yPrev = state.y[i];
-    const xTrial = reflectIntoBounds(xPrev + (rng.normal(0, params.diffusionSd) + driftX) * state.dt, -halfW, halfW);
+    const dx = state.negX - xPrev;
+    const driftX = wallFieldDrift(dx, params.electricStrength, 1);
+    const xTrial = reflectIntoBounds(xPrev + (rng.normal(0, params.diffusionSd) + driftX) * state.dt, state.negX, halfW);
     const yNew = reflectIntoBounds(yPrev + rng.normal(0, params.diffusionSd) * state.dt, -halfH, halfH);
     let xNew = xTrial;
     const tryingToCrossLeft = xPrev > state.rightWall && xTrial <= state.rightWall;
     const tryingToCrossRight = xPrev < state.leftWall && xTrial >= state.leftWall;
     const inChannel = yNew >= state.channelYMin && yNew <= state.channelYMax;
     if ((tryingToCrossLeft || tryingToCrossRight) && !inChannel) {
-      xNew = tryingToCrossLeft ? reflectIntoBounds(2 * state.rightWall - xTrial, -halfW, halfW) : reflectIntoBounds(2 * state.leftWall - xTrial, -halfW, halfW);
+      xNew = tryingToCrossLeft
+        ? reflectIntoBounds(2 * state.rightWall - xTrial, state.negX, halfW)
+        : reflectIntoBounds(2 * state.leftWall - xTrial, state.negX, halfW);
     } else if (!inChannel && xTrial > state.leftWall && xTrial < state.rightWall) {
       xNew = xPrev < 0 ? state.leftWall : state.rightWall;
     }
@@ -356,8 +367,8 @@
     dpr,
     channels: [{ top: cTop, bottom: cBottom }]
   });
-  const [chargeWallX] = toPx((state.leftWall + state.rightWall) / 2, 0);
-  ctx.strokeStyle = fieldColor(state.vProxy);
+  const [chargeWallX] = toPx(state.negX, 0);
+  ctx.strokeStyle = fieldColor(simParams.electricStrength);
   ctx.lineWidth = 1.5 * dpr;
   ctx.setLineDash([5 * dpr, 4 * dpr]);
   ctx.beginPath();
@@ -384,7 +395,13 @@
       <h1>Nernst Equilibrium for One Ion</h1>
       <ul class="key-points">
         <li>Membrane potential is determined by relative ion concentration inside vs outside the cell.</li>
-        <li>At equilibrium (no net flux), this is given by the Nernst equation: V = S ln(C_out / C_in).</li>
+        <li>
+          At equilibrium (no net flux), this simulation uses
+          <math display="inline" aria-label="V equals S times natural log of C out over C in">
+            <mi>V</mi><mo>=</mo><mi>S</mi><mo>ln</mo><mo>(</mo><mfrac><msub><mi>C</mi><mtext>out</mtext></msub><msub><mi>C</mi><mtext>in</mtext></msub></mfrac><mo>)</mo>
+          </math>.
+        </li>
+        <li>Inside is the left compartment and outside is the right compartment; the charge wall is at the far-left inside boundary.</li>
       </ul>
     </header>
     <div class="sim-layout">
@@ -397,7 +414,7 @@
             <div class="control-grid">
               <div class="field"><label for="num-particles">Particles</label><input id="num-particles" type="number" min="10" max="5000" step="1" /></div>
               <div class="field"><label for="diffusion-sd">Diffusion SD</label><input id="diffusion-sd" type="number" min="0" max="20" step="0.01" /></div>
-              <div class="field"><label for="electric-strength">Electric strength (charge wall)</label><input id="electric-strength" type="number" min="-10" max="10" step="0.01" /></div>
+              <div class="field"><label for="electric-strength">Electric strength (inside charge wall)</label><input id="electric-strength" type="number" min="-10" max="10" step="0.01" /></div>
               <div class="field"><label for="channel-width">Channel width</label><input id="channel-width" type="number" min="0.5" step="0.5" /></div>
               <div class="field"><label for="playback-speed">Playback speed</label><input id="playback-speed" type="number" min="0.1" max="8" step="0.1" /></div>
               <div class="field"><label for="seed">Seed</label><input id="seed" type="number" min="0" max="4294967295" step="1" /></div>
@@ -436,8 +453,13 @@
               <p>
                 Purple is the simulated membrane potential from compartment concentrations.
                 White is the Nernst prediction for a single permeant cation:
-                <code>V = S ln(C_out / C_in) = S ln(C_right / C_left)</code>.
               </p>
+              <math display="block" aria-label="V equals S times natural log of C out over C in equals S times natural log of C right over C left">
+                <mi>V</mi><mo>=</mo><mi>S</mi><mo>ln</mo><mo>(</mo><mfrac><msub><mi>C</mi><mtext>out</mtext></msub><msub><mi>C</mi><mtext>in</mtext></msub></mfrac><mo>)</mo>
+                <mo>=</mo>
+                <mi>S</mi><mo>ln</mo><mo>(</mo><mfrac><msub><mi>C</mi><mtext>right</mtext></msub><msub><mi>C</mi><mtext>left</mtext></msub></mfrac><mo>)</mo>
+              </math>
+              <p>In this layout, inside is left and outside is right.</p>
             </div>
           </div>
         </div>
@@ -449,7 +471,7 @@
         </div>
         <div class="legend">
           <span><span class="swatch" style="background:${SIM_COLORS.ionA}"></span>Positive ion</span>
-          <span><span class="swatch" style="background:${SIM_COLORS.fieldNegative}"></span>Charge wall</span>
+          <span><span class="swatch" style="background:${SIM_COLORS.fieldNegative}"></span>Inside charge wall (far left)</span>
           <span><span class="swatch" style="background:${SIM_COLORS.totalTrace}"></span>Simulated membrane potential trace</span>
           <span><span class="swatch" style="background:${SIM_COLORS.predictionTrace}"></span>Nernst prediction trace</span>
         </div>

file update
M /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
@@ -564,3 +564,4 @@
     voltageScale: clamp(Number(inputs.voltageScale.value) || defaultSim.voltageScale, 1, 500),
-    nernstScale: clamp(Number(inputs.nernstScale.value) || defaultSim.nernstScale, 1, 500)
+    nernstScale: clamp(Number(inputs.nernstScale.value) || defaultSim.nernstScale, 1, 500),
+    chargeWallInset: simParams.chargeWallInset
   });
@@ -578,4 +579,4 @@
   equationBlock.innerHTML = [
-    '<span class="accent">Live update with a membrane charge-wall field term</span>',
-    'drift_x = electric_strength · (V_sim / voltage_scale)',
+    '<span class="accent">Live update with a left inside charge-wall field term</span>',
+    'drift_x = wallFieldDrift(charge_wall_x - x, electric_strength)',
     'x_new = x_old + (dxdt + drift_x) · dt',
@@ -587,6 +588,6 @@
     'V_Nernst = nernst_scale · ln((C_out + 0.5) / (C_in + 0.5))',
-    'For a single positive ion, Nernst predicts the charge-wall voltage that balances the concentration gradient',
+    'For a single positive ion, Nernst predicts the balance point for diffusion vs electrical drift',
     '',
     `Per-step Brownian displacement SD = diffusionSd × dt = ${stepSd.toFixed(3)}`,
-    `Electric strength (charge wall) = ${simParams.electricStrength.toFixed(3)}`,
+    `Electric strength (inside charge wall) = ${simParams.electricStrength.toFixed(3)}`,
     `Trace window T = ${simParams.T.toFixed(0)} ms`
@@ -675,2 +676,3 @@
   state.rightWall = simParams.wallThickness / 2;
+  state.negX = leftEdgeChargeWallX(simParams.boxWidth, simParams.chargeWallInset);
   [state.channelYMin, state.channelYMax] = centeredChannelBounds(simParams.channelWidth, simParams.boxHeight);
apply_patch(auto_approved=true) exited 0 in 105ms:
Success. Updated the following files:
M src/pages/inspect_resting_potential_nernst.ts
file update:
diff --git a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
index 7e72d55cd35ae3c2444359ae7ab9a07452c4bf12..17b44f0174bbb5955900ff87d01947e0399d7631
--- a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
+++ b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
@@ -3,11 +3,14 @@
   DEFAULT_DIFFUSION_SD,
   DEFAULT_NUM_PARTICLES,
   DEFAULT_POTENTIAL_SCALE,
+  DEFAULT_CHARGE_WALL_INSET,
   drawMembraneWall,
   MAX_PARTICLES,
   SIM_COLORS,
   centeredChannelBounds,
-  fieldColor
+  fieldColor,
+  leftEdgeChargeWallX,
+  wallFieldDrift
 } from './sim_shared';
 
 interface SimParams {
@@ -22,6 +25,7 @@
   channelWidth: number;
   voltageScale: number;
   nernstScale: number;
+  chargeWallInset: number;
 }
 
 interface DisplayParams {
@@ -42,6 +46,7 @@
   boxHeight: number;
   leftWall: number;
   rightWall: number;
+  negX: number;
   channelYMin: number;
   channelYMax: number;
   initialLeftCount: number;
@@ -100,7 +105,8 @@
   electricStrength: -0.35,
   channelWidth: 20,
   voltageScale: DEFAULT_POTENTIAL_SCALE,
-  nernstScale: DEFAULT_POTENTIAL_SCALE
+  nernstScale: DEFAULT_POTENTIAL_SCALE,
+  chargeWallInset: DEFAULT_CHARGE_WALL_INSET
 };
 
 const defaultDisplay: DisplayParams = { pointSize: 2.2, playbackSpeed: 1, targetFps: 30, traceYLimit: 20 };
@@ -139,7 +145,8 @@
     electricStrength: clamp(params.electricStrength, -10, 10),
     channelWidth: clamp(params.channelWidth, 0.5, boxHeight - 2),
     voltageScale: clamp(params.voltageScale, 1, 500),
-    nernstScale: clamp(params.nernstScale, 1, 500)
+    nernstScale: clamp(params.nernstScale, 1, 500),
+    chargeWallInset: clamp(params.chargeWallInset, 0.5, Math.max(0.5, boxWidth - 1))
   };
 }
 
@@ -186,6 +193,7 @@
     boxHeight: p.boxHeight,
     leftWall,
     rightWall,
+    negX: leftEdgeChargeWallX(p.boxWidth, p.chargeWallInset),
     channelYMin,
     channelYMax,
     initialLeftCount: 0,
@@ -202,18 +210,21 @@
 function stepState(state: LiveState, params: SimParams, rng: Rng): void {
   const halfW = state.boxWidth / 2;
   const halfH = state.boxHeight / 2;
-  const driftX = params.electricStrength * clamp(state.vProxy / Math.max(1, params.voltageScale), -4, 4);
   for (let i = 0; i < state.numParticles; i += 1) {
     const xPrev = state.x[i];
     const yPrev = state.y[i];
-    const xTrial = reflectIntoBounds(xPrev + (rng.normal(0, params.diffusionSd) + driftX) * state.dt, -halfW, halfW);
+    const dx = state.negX - xPrev;
+    const driftX = wallFieldDrift(dx, params.electricStrength, 1);
+    const xTrial = reflectIntoBounds(xPrev + (rng.normal(0, params.diffusionSd) + driftX) * state.dt, state.negX, halfW);
     const yNew = reflectIntoBounds(yPrev + rng.normal(0, params.diffusionSd) * state.dt, -halfH, halfH);
     let xNew = xTrial;
     const tryingToCrossLeft = xPrev > state.rightWall && xTrial <= state.rightWall;
     const tryingToCrossRight = xPrev < state.leftWall && xTrial >= state.leftWall;
     const inChannel = yNew >= state.channelYMin && yNew <= state.channelYMax;
     if ((tryingToCrossLeft || tryingToCrossRight) && !inChannel) {
-      xNew = tryingToCrossLeft ? reflectIntoBounds(2 * state.rightWall - xTrial, -halfW, halfW) : reflectIntoBounds(2 * state.leftWall - xTrial, -halfW, halfW);
+      xNew = tryingToCrossLeft
+        ? reflectIntoBounds(2 * state.rightWall - xTrial, state.negX, halfW)
+        : reflectIntoBounds(2 * state.leftWall - xTrial, state.negX, halfW);
     } else if (!inChannel && xTrial > state.leftWall && xTrial < state.rightWall) {
       xNew = xPrev < 0 ? state.leftWall : state.rightWall;
     }
@@ -356,8 +367,8 @@
     dpr,
     channels: [{ top: cTop, bottom: cBottom }]
   });
-  const [chargeWallX] = toPx((state.leftWall + state.rightWall) / 2, 0);
-  ctx.strokeStyle = fieldColor(state.vProxy);
+  const [chargeWallX] = toPx(state.negX, 0);
+  ctx.strokeStyle = fieldColor(simParams.electricStrength);
   ctx.lineWidth = 1.5 * dpr;
   ctx.setLineDash([5 * dpr, 4 * dpr]);
   ctx.beginPath();
@@ -384,7 +395,13 @@
       <h1>Nernst Equilibrium for One Ion</h1>
       <ul class="key-points">
         <li>Membrane potential is determined by relative ion concentration inside vs outside the cell.</li>
-        <li>At equilibrium (no net flux), this is given by the Nernst equation: V = S ln(C_out / C_in).</li>
+        <li>
+          At equilibrium (no net flux), this simulation uses
+          <math display="inline" aria-label="V equals S times natural log of C out over C in">
+            <mi>V</mi><mo>=</mo><mi>S</mi><mo>ln</mo><mo>(</mo><mfrac><msub><mi>C</mi><mtext>out</mtext></msub><msub><mi>C</mi><mtext>in</mtext></msub></mfrac><mo>)</mo>
+          </math>.
+        </li>
+        <li>Inside is the left compartment and outside is the right compartment; the charge wall is at the far-left inside boundary.</li>
       </ul>
     </header>
     <div class="sim-layout">
@@ -397,7 +414,7 @@
             <div class="control-grid">
               <div class="field"><label for="num-particles">Particles</label><input id="num-particles" type="number" min="10" max="5000" step="1" /></div>
               <div class="field"><label for="diffusion-sd">Diffusion SD</label><input id="diffusion-sd" type="number" min="0" max="20" step="0.01" /></div>
-              <div class="field"><label for="electric-strength">Electric strength (charge wall)</label><input id="electric-strength" type="number" min="-10" max="10" step="0.01" /></div>
+              <div class="field"><label for="electric-strength">Electric strength (inside charge wall)</label><input id="electric-strength" type="number" min="-10" max="10" step="0.01" /></div>
               <div class="field"><label for="channel-width">Channel width</label><input id="channel-width" type="number" min="0.5" step="0.5" /></div>
               <div class="field"><label for="playback-speed">Playback speed</label><input id="playback-speed" type="number" min="0.1" max="8" step="0.1" /></div>
               <div class="field"><label for="seed">Seed</label><input id="seed" type="number" min="0" max="4294967295" step="1" /></div>
@@ -436,8 +453,13 @@
               <p>
                 Purple is the simulated membrane potential from compartment concentrations.
                 White is the Nernst prediction for a single permeant cation:
-                <code>V = S ln(C_out / C_in) = S ln(C_right / C_left)</code>.
               </p>
+              <math display="block" aria-label="V equals S times natural log of C out over C in equals S times natural log of C right over C left">
+                <mi>V</mi><mo>=</mo><mi>S</mi><mo>ln</mo><mo>(</mo><mfrac><msub><mi>C</mi><mtext>out</mtext></msub><msub><mi>C</mi><mtext>in</mtext></msub></mfrac><mo>)</mo>
+                <mo>=</mo>
+                <mi>S</mi><mo>ln</mo><mo>(</mo><mfrac><msub><mi>C</mi><mtext>right</mtext></msub><msub><mi>C</mi><mtext>left</mtext></msub></mfrac><mo>)</mo>
+              </math>
+              <p>In this layout, inside is left and outside is right.</p>
             </div>
           </div>
         </div>
@@ -449,7 +471,7 @@
         </div>
         <div class="legend">
           <span><span class="swatch" style="background:${SIM_COLORS.ionA}"></span>Positive ion</span>
-          <span><span class="swatch" style="background:${SIM_COLORS.fieldNegative}"></span>Charge wall</span>
+          <span><span class="swatch" style="background:${SIM_COLORS.fieldNegative}"></span>Inside charge wall (far left)</span>
           <span><span class="swatch" style="background:${SIM_COLORS.totalTrace}"></span>Simulated membrane potential trace</span>
           <span><span class="swatch" style="background:${SIM_COLORS.predictionTrace}"></span>Nernst prediction trace</span>
         </div>
@@ -540,7 +562,8 @@
     electricStrength: clamp(Number(inputs.electricStrength.value) || 0, -10, 10),
     channelWidth: clamp(Number(inputs.channelWidth.value) || defaultSim.channelWidth, 0.5, boxHeight - 2),
     voltageScale: clamp(Number(inputs.voltageScale.value) || defaultSim.voltageScale, 1, 500),
-    nernstScale: clamp(Number(inputs.nernstScale.value) || defaultSim.nernstScale, 1, 500)
+    nernstScale: clamp(Number(inputs.nernstScale.value) || defaultSim.nernstScale, 1, 500),
+    chargeWallInset: simParams.chargeWallInset
   });
 }
 function readDisplayInputs(): DisplayParams {
@@ -554,8 +577,8 @@
 function updateEquationText(): void {
   const stepSd = simParams.diffusionSd * simParams.dt;
   equationBlock.innerHTML = [
-    '<span class="accent">Live update with a membrane charge-wall field term</span>',
-    'drift_x = electric_strength · (V_sim / voltage_scale)',
+    '<span class="accent">Live update with a left inside charge-wall field term</span>',
+    'drift_x = wallFieldDrift(charge_wall_x - x, electric_strength)',
     'x_new = x_old + (dxdt + drift_x) · dt',
     'y_new = reflect(y_old + dydt · dt, -H/2, H/2)',
     'dxdt, dydt ~ Normal(0, diffusionSd²)',
@@ -563,10 +586,10 @@
     '<span class="accent-2">Simulated Vm and Nernst comparison</span>',
     'V_sim = nernst_scale · ln((C_out + 0.5) / (C_in + 0.5))',
     'V_Nernst = nernst_scale · ln((C_out + 0.5) / (C_in + 0.5))',
-    'For a single positive ion, Nernst predicts the charge-wall voltage that balances the concentration gradient',
+    'For a single positive ion, Nernst predicts the balance point for diffusion vs electrical drift',
     '',
     `Per-step Brownian displacement SD = diffusionSd × dt = ${stepSd.toFixed(3)}`,
-    `Electric strength (charge wall) = ${simParams.electricStrength.toFixed(3)}`,
+    `Electric strength (inside charge wall) = ${simParams.electricStrength.toFixed(3)}`,
     `Trace window T = ${simParams.T.toFixed(0)} ms`
   ].join('\n');
 }
@@ -651,6 +674,7 @@
   state.boxHeight = simParams.boxHeight;
   state.leftWall = -simParams.wallThickness / 2;
   state.rightWall = simParams.wallThickness / 2;
+  state.negX = leftEdgeChargeWallX(simParams.boxWidth, simParams.chargeWallInset);
   [state.channelYMin, state.channelYMax] = centeredChannelBounds(simParams.channelWidth, simParams.boxHeight);
   enforceGeometry(state);
   trimTrace(trace, state.simTime, simParams.T);

file update:
diff --git a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
index 7e72d55cd35ae3c2444359ae7ab9a07452c4bf12..17b44f0174bbb5955900ff87d01947e0399d7631
--- a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
+++ b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
@@ -3,11 +3,14 @@
   DEFAULT_DIFFUSION_SD,
   DEFAULT_NUM_PARTICLES,
   DEFAULT_POTENTIAL_SCALE,
+  DEFAULT_CHARGE_WALL_INSET,
   drawMembraneWall,
   MAX_PARTICLES,
   SIM_COLORS,
   centeredChannelBounds,
-  fieldColor
+  fieldColor,
+  leftEdgeChargeWallX,
+  wallFieldDrift
 } from './sim_shared';
 
 interface SimParams {
@@ -22,6 +25,7 @@
   channelWidth: number;
   voltageScale: number;
   nernstScale: number;
+  chargeWallInset: number;
 }
 
 interface DisplayParams {
@@ -42,6 +46,7 @@
   boxHeight: number;
   leftWall: number;
   rightWall: number;
+  negX: number;
   channelYMin: number;
   channelYMax: number;
   initialLeftCount: number;
@@ -100,7 +105,8 @@
   electricStrength: -0.35,
   channelWidth: 20,
   voltageScale: DEFAULT_POTENTIAL_SCALE,
-  nernstScale: DEFAULT_POTENTIAL_SCALE
+  nernstScale: DEFAULT_POTENTIAL_SCALE,
+  chargeWallInset: DEFAULT_CHARGE_WALL_INSET
 };
 
 const defaultDisplay: DisplayParams = { pointSize: 2.2, playbackSpeed: 1, targetFps: 30, traceYLimit: 20 };
@@ -139,7 +145,8 @@
     electricStrength: clamp(params.electricStrength, -10, 10),
     channelWidth: clamp(params.channelWidth, 0.5, boxHeight - 2),
     voltageScale: clamp(params.voltageScale, 1, 500),
-    nernstScale: clamp(params.nernstScale, 1, 500)
+    nernstScale: clamp(params.nernstScale, 1, 500),
+    chargeWallInset: clamp(params.chargeWallInset, 0.5, Math.max(0.5, boxWidth - 1))
   };
 }
 
@@ -186,6 +193,7 @@
     boxHeight: p.boxHeight,
     leftWall,
     rightWall,
+    negX: leftEdgeChargeWallX(p.boxWidth, p.chargeWallInset),
     channelYMin,
     channelYMax,
     initialLeftCount: 0,
@@ -202,18 +210,21 @@
 function stepState(state: LiveState, params: SimParams, rng: Rng): void {
   const halfW = state.boxWidth / 2;
   const halfH = state.boxHeight / 2;
-  const driftX = params.electricStrength * clamp(state.vProxy / Math.max(1, params.voltageScale), -4, 4);
   for (let i = 0; i < state.numParticles; i += 1) {
     const xPrev = state.x[i];
     const yPrev = state.y[i];
-    const xTrial = reflectIntoBounds(xPrev + (rng.normal(0, params.diffusionSd) + driftX) * state.dt, -halfW, halfW);
+    const dx = state.negX - xPrev;
+    const driftX = wallFieldDrift(dx, params.electricStrength, 1);
+    const xTrial = reflectIntoBounds(xPrev + (rng.normal(0, params.diffusionSd) + driftX) * state.dt, state.negX, halfW);
     const yNew = reflectIntoBounds(yPrev + rng.normal(0, params.diffusionSd) * state.dt, -halfH, halfH);
     let xNew = xTrial;
     const tryingToCrossLeft = xPrev > state.rightWall && xTrial <= state.rightWall;
     const tryingToCrossRight = xPrev < state.leftWall && xTrial >= state.leftWall;
     const inChannel = yNew >= state.channelYMin && yNew <= state.channelYMax;
     if ((tryingToCrossLeft || tryingToCrossRight) && !inChannel) {
-      xNew = tryingToCrossLeft ? reflectIntoBounds(2 * state.rightWall - xTrial, -halfW, halfW) : reflectIntoBounds(2 * state.leftWall - xTrial, -halfW, halfW);
+      xNew = tryingToCrossLeft
+        ? reflectIntoBounds(2 * state.rightWall - xTrial, state.negX, halfW)
+        : reflectIntoBounds(2 * state.leftWall - xTrial, state.negX, halfW);
     } else if (!inChannel && xTrial > state.leftWall && xTrial < state.rightWall) {
       xNew = xPrev < 0 ? state.leftWall : state.rightWall;
     }
@@ -356,8 +367,8 @@
     dpr,
     channels: [{ top: cTop, bottom: cBottom }]
   });
-  const [chargeWallX] = toPx((state.leftWall + state.rightWall) / 2, 0);
-  ctx.strokeStyle = fieldColor(state.vProxy);
+  const [chargeWallX] = toPx(state.negX, 0);
+  ctx.strokeStyle = fieldColor(simParams.electricStrength);
   ctx.lineWidth = 1.5 * dpr;
   ctx.setLineDash([5 * dpr, 4 * dpr]);
   ctx.beginPath();
@@ -384,7 +395,13 @@
       <h1>Nernst Equilibrium for One Ion</h1>
       <ul class="key-points">
         <li>Membrane potential is determined by relative ion concentration inside vs outside the cell.</li>
-        <li>At equilibrium (no net flux), this is given by the Nernst equation: V = S ln(C_out / C_in).</li>
+        <li>
+          At equilibrium (no net flux), this simulation uses
+          <math display="inline" aria-label="V equals S times natural log of C out over C in">
+            <mi>V</mi><mo>=</mo><mi>S</mi><mo>ln</mo><mo>(</mo><mfrac><msub><mi>C</mi><mtext>out</mtext></msub><msub><mi>C</mi><mtext>in</mtext></msub></mfrac><mo>)</mo>
+          </math>.
+        </li>
+        <li>Inside is the left compartment and outside is the right compartment; the charge wall is at the far-left inside boundary.</li>
       </ul>
     </header>
     <div class="sim-layout">
@@ -397,7 +414,7 @@
             <div class="control-grid">
               <div class="field"><label for="num-particles">Particles</label><input id="num-particles" type="number" min="10" max="5000" step="1" /></div>
               <div class="field"><label for="diffusion-sd">Diffusion SD</label><input id="diffusion-sd" type="number" min="0" max="20" step="0.01" /></div>
-              <div class="field"><label for="electric-strength">Electric strength (charge wall)</label><input id="electric-strength" type="number" min="-10" max="10" step="0.01" /></div>
+              <div class="field"><label for="electric-strength">Electric strength (inside charge wall)</label><input id="electric-strength" type="number" min="-10" max="10" step="0.01" /></div>
               <div class="field"><label for="channel-width">Channel width</label><input id="channel-width" type="number" min="0.5" step="0.5" /></div>
               <div class="field"><label for="playback-speed">Playback speed</label><input id="playback-speed" type="number" min="0.1" max="8" step="0.1" /></div>
               <div class="field"><label for="seed">Seed</label><input id="seed" type="number" min="0" max="4294967295" step="1" /></div>
@@ -436,8 +453,13 @@
               <p>
                 Purple is the simulated membrane potential from compartment concentrations.
                 White is the Nernst prediction for a single permeant cation:
-                <code>V = S ln(C_out / C_in) = S ln(C_right / C_left)</code>.
               </p>
+              <math display="block" aria-label="V equals S times natural log of C out over C in equals S times natural log of C right over C left">
+                <mi>V</mi><mo>=</mo><mi>S</mi><mo>ln</mo><mo>(</mo><mfrac><msub><mi>C</mi><mtext>out</mtext></msub><msub><mi>C</mi><mtext>in</mtext></msub></mfrac><mo>)</mo>
+                <mo>=</mo>
+                <mi>S</mi><mo>ln</mo><mo>(</mo><mfrac><msub><mi>C</mi><mtext>right</mtext></msub><msub><mi>C</mi><mtext>left</mtext></msub></mfrac><mo>)</mo>
+              </math>
+              <p>In this layout, inside is left and outside is right.</p>
             </div>
           </div>
         </div>
@@ -449,7 +471,7 @@
         </div>
         <div class="legend">
           <span><span class="swatch" style="background:${SIM_COLORS.ionA}"></span>Positive ion</span>
-          <span><span class="swatch" style="background:${SIM_COLORS.fieldNegative}"></span>Charge wall</span>
+          <span><span class="swatch" style="background:${SIM_COLORS.fieldNegative}"></span>Inside charge wall (far left)</span>
           <span><span class="swatch" style="background:${SIM_COLORS.totalTrace}"></span>Simulated membrane potential trace</span>
           <span><span class="swatch" style="background:${SIM_COLORS.predictionTrace}"></span>Nernst prediction trace</span>
         </div>
@@ -540,7 +562,8 @@
     electricStrength: clamp(Number(inputs.electricStrength.value) || 0, -10, 10),
     channelWidth: clamp(Number(inputs.channelWidth.value) || defaultSim.channelWidth, 0.5, boxHeight - 2),
     voltageScale: clamp(Number(inputs.voltageScale.value) || defaultSim.voltageScale, 1, 500),
-    nernstScale: clamp(Number(inputs.nernstScale.value) || defaultSim.nernstScale, 1, 500)
+    nernstScale: clamp(Number(inputs.nernstScale.value) || defaultSim.nernstScale, 1, 500),
+    chargeWallInset: simParams.chargeWallInset
   });
 }
 function readDisplayInputs(): DisplayParams {
@@ -554,8 +577,8 @@
 function updateEquationText(): void {
   const stepSd = simParams.diffusionSd * simParams.dt;
   equationBlock.innerHTML = [
-    '<span class="accent">Live update with a membrane charge-wall field term</span>',
-    'drift_x = electric_strength · (V_sim / voltage_scale)',
+    '<span class="accent">Live update with a left inside charge-wall field term</span>',
+    'drift_x = wallFieldDrift(charge_wall_x - x, electric_strength)',
     'x_new = x_old + (dxdt + drift_x) · dt',
     'y_new = reflect(y_old + dydt · dt, -H/2, H/2)',
     'dxdt, dydt ~ Normal(0, diffusionSd²)',
@@ -563,10 +586,10 @@
     '<span class="accent-2">Simulated Vm and Nernst comparison</span>',
     'V_sim = nernst_scale · ln((C_out + 0.5) / (C_in + 0.5))',
     'V_Nernst = nernst_scale · ln((C_out + 0.5) / (C_in + 0.5))',
-    'For a single positive ion, Nernst predicts the charge-wall voltage that balances the concentration gradient',
+    'For a single positive ion, Nernst predicts the balance point for diffusion vs electrical drift',
     '',
     `Per-step Brownian displacement SD = diffusionSd × dt = ${stepSd.toFixed(3)}`,
-    `Electric strength (charge wall) = ${simParams.electricStrength.toFixed(3)}`,
+    `Electric strength (inside charge wall) = ${simParams.electricStrength.toFixed(3)}`,
     `Trace window T = ${simParams.T.toFixed(0)} ms`
   ].join('\n');
 }
@@ -651,6 +674,7 @@
   state.boxHeight = simParams.boxHeight;
   state.leftWall = -simParams.wallThickness / 2;
   state.rightWall = simParams.wallThickness / 2;
+  state.negX = leftEdgeChargeWallX(simParams.boxWidth, simParams.chargeWallInset);
   [state.channelYMin, state.channelYMax] = centeredChannelBounds(simParams.channelWidth, simParams.boxHeight);
   enforceGeometry(state);
   trimTrace(trace, state.simTime, simParams.T);

file update
M /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
@@ -635,3 +635,3 @@
   for (let i = 0; i < stateRef.numParticles; i += 1) {
-    stateRef.x[i] = reflectIntoBounds(stateRef.x[i], -halfW, halfW);
+    stateRef.x[i] = reflectIntoBounds(stateRef.x[i], stateRef.negX, halfW);
     stateRef.y[i] = reflectIntoBounds(stateRef.y[i], -halfH, halfH);
apply_patch(auto_approved=true) exited 0 in 73ms:
Success. Updated the following files:
M src/pages/inspect_resting_potential_nernst.ts
file update:
diff --git a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
index 7e72d55cd35ae3c2444359ae7ab9a07452c4bf12..2c37f0c606feabdb0d1adec7ab62e661d59f6f35
--- a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
+++ b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
@@ -3,11 +3,14 @@
   DEFAULT_DIFFUSION_SD,
   DEFAULT_NUM_PARTICLES,
   DEFAULT_POTENTIAL_SCALE,
+  DEFAULT_CHARGE_WALL_INSET,
   drawMembraneWall,
   MAX_PARTICLES,
   SIM_COLORS,
   centeredChannelBounds,
-  fieldColor
+  fieldColor,
+  leftEdgeChargeWallX,
+  wallFieldDrift
 } from './sim_shared';
 
 interface SimParams {
@@ -22,6 +25,7 @@
   channelWidth: number;
   voltageScale: number;
   nernstScale: number;
+  chargeWallInset: number;
 }
 
 interface DisplayParams {
@@ -42,6 +46,7 @@
   boxHeight: number;
   leftWall: number;
   rightWall: number;
+  negX: number;
   channelYMin: number;
   channelYMax: number;
   initialLeftCount: number;
@@ -100,7 +105,8 @@
   electricStrength: -0.35,
   channelWidth: 20,
   voltageScale: DEFAULT_POTENTIAL_SCALE,
-  nernstScale: DEFAULT_POTENTIAL_SCALE
+  nernstScale: DEFAULT_POTENTIAL_SCALE,
+  chargeWallInset: DEFAULT_CHARGE_WALL_INSET
 };
 
 const defaultDisplay: DisplayParams = { pointSize: 2.2, playbackSpeed: 1, targetFps: 30, traceYLimit: 20 };
@@ -139,7 +145,8 @@
     electricStrength: clamp(params.electricStrength, -10, 10),
     channelWidth: clamp(params.channelWidth, 0.5, boxHeight - 2),
     voltageScale: clamp(params.voltageScale, 1, 500),
-    nernstScale: clamp(params.nernstScale, 1, 500)
+    nernstScale: clamp(params.nernstScale, 1, 500),
+    chargeWallInset: clamp(params.chargeWallInset, 0.5, Math.max(0.5, boxWidth - 1))
   };
 }
 
@@ -186,6 +193,7 @@
     boxHeight: p.boxHeight,
     leftWall,
     rightWall,
+    negX: leftEdgeChargeWallX(p.boxWidth, p.chargeWallInset),
     channelYMin,
     channelYMax,
     initialLeftCount: 0,
@@ -202,18 +210,21 @@
 function stepState(state: LiveState, params: SimParams, rng: Rng): void {
   const halfW = state.boxWidth / 2;
   const halfH = state.boxHeight / 2;
-  const driftX = params.electricStrength * clamp(state.vProxy / Math.max(1, params.voltageScale), -4, 4);
   for (let i = 0; i < state.numParticles; i += 1) {
     const xPrev = state.x[i];
     const yPrev = state.y[i];
-    const xTrial = reflectIntoBounds(xPrev + (rng.normal(0, params.diffusionSd) + driftX) * state.dt, -halfW, halfW);
+    const dx = state.negX - xPrev;
+    const driftX = wallFieldDrift(dx, params.electricStrength, 1);
+    const xTrial = reflectIntoBounds(xPrev + (rng.normal(0, params.diffusionSd) + driftX) * state.dt, state.negX, halfW);
     const yNew = reflectIntoBounds(yPrev + rng.normal(0, params.diffusionSd) * state.dt, -halfH, halfH);
     let xNew = xTrial;
     const tryingToCrossLeft = xPrev > state.rightWall && xTrial <= state.rightWall;
     const tryingToCrossRight = xPrev < state.leftWall && xTrial >= state.leftWall;
     const inChannel = yNew >= state.channelYMin && yNew <= state.channelYMax;
     if ((tryingToCrossLeft || tryingToCrossRight) && !inChannel) {
-      xNew = tryingToCrossLeft ? reflectIntoBounds(2 * state.rightWall - xTrial, -halfW, halfW) : reflectIntoBounds(2 * state.leftWall - xTrial, -halfW, halfW);
+      xNew = tryingToCrossLeft
+        ? reflectIntoBounds(2 * state.rightWall - xTrial, state.negX, halfW)
+        : reflectIntoBounds(2 * state.leftWall - xTrial, state.negX, halfW);
     } else if (!inChannel && xTrial > state.leftWall && xTrial < state.rightWall) {
       xNew = xPrev < 0 ? state.leftWall : state.rightWall;
     }
@@ -356,8 +367,8 @@
     dpr,
     channels: [{ top: cTop, bottom: cBottom }]
   });
-  const [chargeWallX] = toPx((state.leftWall + state.rightWall) / 2, 0);
-  ctx.strokeStyle = fieldColor(state.vProxy);
+  const [chargeWallX] = toPx(state.negX, 0);
+  ctx.strokeStyle = fieldColor(simParams.electricStrength);
   ctx.lineWidth = 1.5 * dpr;
   ctx.setLineDash([5 * dpr, 4 * dpr]);
   ctx.beginPath();
@@ -384,7 +395,13 @@
       <h1>Nernst Equilibrium for One Ion</h1>
       <ul class="key-points">
         <li>Membrane potential is determined by relative ion concentration inside vs outside the cell.</li>
-        <li>At equilibrium (no net flux), this is given by the Nernst equation: V = S ln(C_out / C_in).</li>
+        <li>
+          At equilibrium (no net flux), this simulation uses
+          <math display="inline" aria-label="V equals S times natural log of C out over C in">
+            <mi>V</mi><mo>=</mo><mi>S</mi><mo>ln</mo><mo>(</mo><mfrac><msub><mi>C</mi><mtext>out</mtext></msub><msub><mi>C</mi><mtext>in</mtext></msub></mfrac><mo>)</mo>
+          </math>.
+        </li>
+        <li>Inside is the left compartment and outside is the right compartment; the charge wall is at the far-left inside boundary.</li>
       </ul>
     </header>
     <div class="sim-layout">
@@ -397,7 +414,7 @@
             <div class="control-grid">
               <div class="field"><label for="num-particles">Particles</label><input id="num-particles" type="number" min="10" max="5000" step="1" /></div>
               <div class="field"><label for="diffusion-sd">Diffusion SD</label><input id="diffusion-sd" type="number" min="0" max="20" step="0.01" /></div>
-              <div class="field"><label for="electric-strength">Electric strength (charge wall)</label><input id="electric-strength" type="number" min="-10" max="10" step="0.01" /></div>
+              <div class="field"><label for="electric-strength">Electric strength (inside charge wall)</label><input id="electric-strength" type="number" min="-10" max="10" step="0.01" /></div>
               <div class="field"><label for="channel-width">Channel width</label><input id="channel-width" type="number" min="0.5" step="0.5" /></div>
               <div class="field"><label for="playback-speed">Playback speed</label><input id="playback-speed" type="number" min="0.1" max="8" step="0.1" /></div>
               <div class="field"><label for="seed">Seed</label><input id="seed" type="number" min="0" max="4294967295" step="1" /></div>
@@ -436,8 +453,13 @@
               <p>
                 Purple is the simulated membrane potential from compartment concentrations.
                 White is the Nernst prediction for a single permeant cation:
-                <code>V = S ln(C_out / C_in) = S ln(C_right / C_left)</code>.
               </p>
+              <math display="block" aria-label="V equals S times natural log of C out over C in equals S times natural log of C right over C left">
+                <mi>V</mi><mo>=</mo><mi>S</mi><mo>ln</mo><mo>(</mo><mfrac><msub><mi>C</mi><mtext>out</mtext></msub><msub><mi>C</mi><mtext>in</mtext></msub></mfrac><mo>)</mo>
+                <mo>=</mo>
+                <mi>S</mi><mo>ln</mo><mo>(</mo><mfrac><msub><mi>C</mi><mtext>right</mtext></msub><msub><mi>C</mi><mtext>left</mtext></msub></mfrac><mo>)</mo>
+              </math>
+              <p>In this layout, inside is left and outside is right.</p>
             </div>
           </div>
         </div>
@@ -449,7 +471,7 @@
         </div>
         <div class="legend">
           <span><span class="swatch" style="background:${SIM_COLORS.ionA}"></span>Positive ion</span>
-          <span><span class="swatch" style="background:${SIM_COLORS.fieldNegative}"></span>Charge wall</span>
+          <span><span class="swatch" style="background:${SIM_COLORS.fieldNegative}"></span>Inside charge wall (far left)</span>
           <span><span class="swatch" style="background:${SIM_COLORS.totalTrace}"></span>Simulated membrane potential trace</span>
           <span><span class="swatch" style="background:${SIM_COLORS.predictionTrace}"></span>Nernst prediction trace</span>
         </div>
@@ -540,7 +562,8 @@
     electricStrength: clamp(Number(inputs.electricStrength.value) || 0, -10, 10),
     channelWidth: clamp(Number(inputs.channelWidth.value) || defaultSim.channelWidth, 0.5, boxHeight - 2),
     voltageScale: clamp(Number(inputs.voltageScale.value) || defaultSim.voltageScale, 1, 500),
-    nernstScale: clamp(Number(inputs.nernstScale.value) || defaultSim.nernstScale, 1, 500)
+    nernstScale: clamp(Number(inputs.nernstScale.value) || defaultSim.nernstScale, 1, 500),
+    chargeWallInset: simParams.chargeWallInset
   });
 }
 function readDisplayInputs(): DisplayParams {
@@ -554,8 +577,8 @@
 function updateEquationText(): void {
   const stepSd = simParams.diffusionSd * simParams.dt;
   equationBlock.innerHTML = [
-    '<span class="accent">Live update with a membrane charge-wall field term</span>',
-    'drift_x = electric_strength · (V_sim / voltage_scale)',
+    '<span class="accent">Live update with a left inside charge-wall field term</span>',
+    'drift_x = wallFieldDrift(charge_wall_x - x, electric_strength)',
     'x_new = x_old + (dxdt + drift_x) · dt',
     'y_new = reflect(y_old + dydt · dt, -H/2, H/2)',
     'dxdt, dydt ~ Normal(0, diffusionSd²)',
@@ -563,10 +586,10 @@
     '<span class="accent-2">Simulated Vm and Nernst comparison</span>',
     'V_sim = nernst_scale · ln((C_out + 0.5) / (C_in + 0.5))',
     'V_Nernst = nernst_scale · ln((C_out + 0.5) / (C_in + 0.5))',
-    'For a single positive ion, Nernst predicts the charge-wall voltage that balances the concentration gradient',
+    'For a single positive ion, Nernst predicts the balance point for diffusion vs electrical drift',
     '',
     `Per-step Brownian displacement SD = diffusionSd × dt = ${stepSd.toFixed(3)}`,
-    `Electric strength (charge wall) = ${simParams.electricStrength.toFixed(3)}`,
+    `Electric strength (inside charge wall) = ${simParams.electricStrength.toFixed(3)}`,
     `Trace window T = ${simParams.T.toFixed(0)} ms`
   ].join('\n');
 }
@@ -610,7 +633,7 @@
   const halfW = stateRef.boxWidth / 2;
   const halfH = stateRef.boxHeight / 2;
   for (let i = 0; i < stateRef.numParticles; i += 1) {
-    stateRef.x[i] = reflectIntoBounds(stateRef.x[i], -halfW, halfW);
+    stateRef.x[i] = reflectIntoBounds(stateRef.x[i], stateRef.negX, halfW);
     stateRef.y[i] = reflectIntoBounds(stateRef.y[i], -halfH, halfH);
     if (stateRef.x[i] > stateRef.leftWall && stateRef.x[i] < stateRef.rightWall && !(stateRef.y[i] >= stateRef.channelYMin && stateRef.y[i] <= stateRef.channelYMax)) {
       stateRef.x[i] = stateRef.x[i] < 0 ? stateRef.leftWall : stateRef.rightWall;
@@ -651,6 +674,7 @@
   state.boxHeight = simParams.boxHeight;
   state.leftWall = -simParams.wallThickness / 2;
   state.rightWall = simParams.wallThickness / 2;
+  state.negX = leftEdgeChargeWallX(simParams.boxWidth, simParams.chargeWallInset);
   [state.channelYMin, state.channelYMax] = centeredChannelBounds(simParams.channelWidth, simParams.boxHeight);
   enforceGeometry(state);
   trimTrace(trace, state.simTime, simParams.T);

file update:
diff --git a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
index 7e72d55cd35ae3c2444359ae7ab9a07452c4bf12..2c37f0c606feabdb0d1adec7ab62e661d59f6f35
--- a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
+++ b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
@@ -3,11 +3,14 @@
   DEFAULT_DIFFUSION_SD,
   DEFAULT_NUM_PARTICLES,
   DEFAULT_POTENTIAL_SCALE,
+  DEFAULT_CHARGE_WALL_INSET,
   drawMembraneWall,
   MAX_PARTICLES,
   SIM_COLORS,
   centeredChannelBounds,
-  fieldColor
+  fieldColor,
+  leftEdgeChargeWallX,
+  wallFieldDrift
 } from './sim_shared';
 
 interface SimParams {
@@ -22,6 +25,7 @@
   channelWidth: number;
   voltageScale: number;
   nernstScale: number;
+  chargeWallInset: number;
 }
 
 interface DisplayParams {
@@ -42,6 +46,7 @@
   boxHeight: number;
   leftWall: number;
   rightWall: number;
+  negX: number;
   channelYMin: number;
   channelYMax: number;
   initialLeftCount: number;
@@ -100,7 +105,8 @@
   electricStrength: -0.35,
   channelWidth: 20,
   voltageScale: DEFAULT_POTENTIAL_SCALE,
-  nernstScale: DEFAULT_POTENTIAL_SCALE
+  nernstScale: DEFAULT_POTENTIAL_SCALE,
+  chargeWallInset: DEFAULT_CHARGE_WALL_INSET
 };
 
 const defaultDisplay: DisplayParams = { pointSize: 2.2, playbackSpeed: 1, targetFps: 30, traceYLimit: 20 };
@@ -139,7 +145,8 @@
     electricStrength: clamp(params.electricStrength, -10, 10),
     channelWidth: clamp(params.channelWidth, 0.5, boxHeight - 2),
     voltageScale: clamp(params.voltageScale, 1, 500),
-    nernstScale: clamp(params.nernstScale, 1, 500)
+    nernstScale: clamp(params.nernstScale, 1, 500),
+    chargeWallInset: clamp(params.chargeWallInset, 0.5, Math.max(0.5, boxWidth - 1))
   };
 }
 
@@ -186,6 +193,7 @@
     boxHeight: p.boxHeight,
     leftWall,
     rightWall,
+    negX: leftEdgeChargeWallX(p.boxWidth, p.chargeWallInset),
     channelYMin,
     channelYMax,
     initialLeftCount: 0,
@@ -202,18 +210,21 @@
 function stepState(state: LiveState, params: SimParams, rng: Rng): void {
   const halfW = state.boxWidth / 2;
   const halfH = state.boxHeight / 2;
-  const driftX = params.electricStrength * clamp(state.vProxy / Math.max(1, params.voltageScale), -4, 4);
   for (let i = 0; i < state.numParticles; i += 1) {
     const xPrev = state.x[i];
     const yPrev = state.y[i];
-    const xTrial = reflectIntoBounds(xPrev + (rng.normal(0, params.diffusionSd) + driftX) * state.dt, -halfW, halfW);
+    const dx = state.negX - xPrev;
+    const driftX = wallFieldDrift(dx, params.electricStrength, 1);
+    const xTrial = reflectIntoBounds(xPrev + (rng.normal(0, params.diffusionSd) + driftX) * state.dt, state.negX, halfW);
     const yNew = reflectIntoBounds(yPrev + rng.normal(0, params.diffusionSd) * state.dt, -halfH, halfH);
     let xNew = xTrial;
     const tryingToCrossLeft = xPrev > state.rightWall && xTrial <= state.rightWall;
     const tryingToCrossRight = xPrev < state.leftWall && xTrial >= state.leftWall;
     const inChannel = yNew >= state.channelYMin && yNew <= state.channelYMax;
     if ((tryingToCrossLeft || tryingToCrossRight) && !inChannel) {
-      xNew = tryingToCrossLeft ? reflectIntoBounds(2 * state.rightWall - xTrial, -halfW, halfW) : reflectIntoBounds(2 * state.leftWall - xTrial, -halfW, halfW);
+      xNew = tryingToCrossLeft
+        ? reflectIntoBounds(2 * state.rightWall - xTrial, state.negX, halfW)
+        : reflectIntoBounds(2 * state.leftWall - xTrial, state.negX, halfW);
     } else if (!inChannel && xTrial > state.leftWall && xTrial < state.rightWall) {
       xNew = xPrev < 0 ? state.leftWall : state.rightWall;
     }
@@ -356,8 +367,8 @@
     dpr,
     channels: [{ top: cTop, bottom: cBottom }]
   });
-  const [chargeWallX] = toPx((state.leftWall + state.rightWall) / 2, 0);
-  ctx.strokeStyle = fieldColor(state.vProxy);
+  const [chargeWallX] = toPx(state.negX, 0);
+  ctx.strokeStyle = fieldColor(simParams.electricStrength);
   ctx.lineWidth = 1.5 * dpr;
   ctx.setLineDash([5 * dpr, 4 * dpr]);
   ctx.beginPath();
@@ -384,7 +395,13 @@
       <h1>Nernst Equilibrium for One Ion</h1>
       <ul class="key-points">
         <li>Membrane potential is determined by relative ion concentration inside vs outside the cell.</li>
-        <li>At equilibrium (no net flux), this is given by the Nernst equation: V = S ln(C_out / C_in).</li>
+        <li>
+          At equilibrium (no net flux), this simulation uses
+          <math display="inline" aria-label="V equals S times natural log of C out over C in">
+            <mi>V</mi><mo>=</mo><mi>S</mi><mo>ln</mo><mo>(</mo><mfrac><msub><mi>C</mi><mtext>out</mtext></msub><msub><mi>C</mi><mtext>in</mtext></msub></mfrac><mo>)</mo>
+          </math>.
+        </li>
+        <li>Inside is the left compartment and outside is the right compartment; the charge wall is at the far-left inside boundary.</li>
       </ul>
     </header>
     <div class="sim-layout">
@@ -397,7 +414,7 @@
             <div class="control-grid">
               <div class="field"><label for="num-particles">Particles</label><input id="num-particles" type="number" min="10" max="5000" step="1" /></div>
               <div class="field"><label for="diffusion-sd">Diffusion SD</label><input id="diffusion-sd" type="number" min="0" max="20" step="0.01" /></div>
-              <div class="field"><label for="electric-strength">Electric strength (charge wall)</label><input id="electric-strength" type="number" min="-10" max="10" step="0.01" /></div>
+              <div class="field"><label for="electric-strength">Electric strength (inside charge wall)</label><input id="electric-strength" type="number" min="-10" max="10" step="0.01" /></div>
               <div class="field"><label for="channel-width">Channel width</label><input id="channel-width" type="number" min="0.5" step="0.5" /></div>
               <div class="field"><label for="playback-speed">Playback speed</label><input id="playback-speed" type="number" min="0.1" max="8" step="0.1" /></div>
               <div class="field"><label for="seed">Seed</label><input id="seed" type="number" min="0" max="4294967295" step="1" /></div>
@@ -436,8 +453,13 @@
               <p>
                 Purple is the simulated membrane potential from compartment concentrations.
                 White is the Nernst prediction for a single permeant cation:
-                <code>V = S ln(C_out / C_in) = S ln(C_right / C_left)</code>.
               </p>
+              <math display="block" aria-label="V equals S times natural log of C out over C in equals S times natural log of C right over C left">
+                <mi>V</mi><mo>=</mo><mi>S</mi><mo>ln</mo><mo>(</mo><mfrac><msub><mi>C</mi><mtext>out</mtext></msub><msub><mi>C</mi><mtext>in</mtext></msub></mfrac><mo>)</mo>
+                <mo>=</mo>
+                <mi>S</mi><mo>ln</mo><mo>(</mo><mfrac><msub><mi>C</mi><mtext>right</mtext></msub><msub><mi>C</mi><mtext>left</mtext></msub></mfrac><mo>)</mo>
+              </math>
+              <p>In this layout, inside is left and outside is right.</p>
             </div>
           </div>
         </div>
@@ -449,7 +471,7 @@
         </div>
         <div class="legend">
           <span><span class="swatch" style="background:${SIM_COLORS.ionA}"></span>Positive ion</span>
-          <span><span class="swatch" style="background:${SIM_COLORS.fieldNegative}"></span>Charge wall</span>
+          <span><span class="swatch" style="background:${SIM_COLORS.fieldNegative}"></span>Inside charge wall (far left)</span>
           <span><span class="swatch" style="background:${SIM_COLORS.totalTrace}"></span>Simulated membrane potential trace</span>
           <span><span class="swatch" style="background:${SIM_COLORS.predictionTrace}"></span>Nernst prediction trace</span>
         </div>
@@ -540,7 +562,8 @@
     electricStrength: clamp(Number(inputs.electricStrength.value) || 0, -10, 10),
     channelWidth: clamp(Number(inputs.channelWidth.value) || defaultSim.channelWidth, 0.5, boxHeight - 2),
     voltageScale: clamp(Number(inputs.voltageScale.value) || defaultSim.voltageScale, 1, 500),
-    nernstScale: clamp(Number(inputs.nernstScale.value) || defaultSim.nernstScale, 1, 500)
+    nernstScale: clamp(Number(inputs.nernstScale.value) || defaultSim.nernstScale, 1, 500),
+    chargeWallInset: simParams.chargeWallInset
   });
 }
 function readDisplayInputs(): DisplayParams {
@@ -554,8 +577,8 @@
 function updateEquationText(): void {
   const stepSd = simParams.diffusionSd * simParams.dt;
   equationBlock.innerHTML = [
-    '<span class="accent">Live update with a membrane charge-wall field term</span>',
-    'drift_x = electric_strength · (V_sim / voltage_scale)',
+    '<span class="accent">Live update with a left inside charge-wall field term</span>',
+    'drift_x = wallFieldDrift(charge_wall_x - x, electric_strength)',
     'x_new = x_old + (dxdt + drift_x) · dt',
     'y_new = reflect(y_old + dydt · dt, -H/2, H/2)',
     'dxdt, dydt ~ Normal(0, diffusionSd²)',
@@ -563,10 +586,10 @@
     '<span class="accent-2">Simulated Vm and Nernst comparison</span>',
     'V_sim = nernst_scale · ln((C_out + 0.5) / (C_in + 0.5))',
     'V_Nernst = nernst_scale · ln((C_out + 0.5) / (C_in + 0.5))',
-    'For a single positive ion, Nernst predicts the charge-wall voltage that balances the concentration gradient',
+    'For a single positive ion, Nernst predicts the balance point for diffusion vs electrical drift',
     '',
     `Per-step Brownian displacement SD = diffusionSd × dt = ${stepSd.toFixed(3)}`,
-    `Electric strength (charge wall) = ${simParams.electricStrength.toFixed(3)}`,
+    `Electric strength (inside charge wall) = ${simParams.electricStrength.toFixed(3)}`,
     `Trace window T = ${simParams.T.toFixed(0)} ms`
   ].join('\n');
 }
@@ -610,7 +633,7 @@
   const halfW = stateRef.boxWidth / 2;
   const halfH = stateRef.boxHeight / 2;
   for (let i = 0; i < stateRef.numParticles; i += 1) {
-    stateRef.x[i] = reflectIntoBounds(stateRef.x[i], -halfW, halfW);
+    stateRef.x[i] = reflectIntoBounds(stateRef.x[i], stateRef.negX, halfW);
     stateRef.y[i] = reflectIntoBounds(stateRef.y[i], -halfH, halfH);
     if (stateRef.x[i] > stateRef.leftWall && stateRef.x[i] < stateRef.rightWall && !(stateRef.y[i] >= stateRef.channelYMin && stateRef.y[i] <= stateRef.channelYMax)) {
       stateRef.x[i] = stateRef.x[i] < 0 ? stateRef.leftWall : stateRef.rightWall;
@@ -651,6 +674,7 @@
   state.boxHeight = simParams.boxHeight;
   state.leftWall = -simParams.wallThickness / 2;
   state.rightWall = simParams.wallThickness / 2;
+  state.negX = leftEdgeChargeWallX(simParams.boxWidth, simParams.chargeWallInset);
   [state.channelYMin, state.channelYMax] = centeredChannelBounds(simParams.channelWidth, simParams.boxHeight);
   enforceGeometry(state);
   trimTrace(trace, state.simTime, simParams.T);

codex
Core edits are in place; I’m running a full TypeScript/Vite build now to verify the changes compile cleanly.
exec
/bin/zsh -lc 'npm run build' in /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_web succeeded in 1.11s:

> electrochemical-signals-progression-web@0.1.0 build
> tsc && vite build

vite v6.4.1 building for production...
transforming...
✓ 33 modules transformed.
rendering chunks...
computing gzip size...
dist/index.html                                            0.50 kB │ gzip: 0.31 kB
dist/inspect_diffusion_1.html                              0.58 kB │ gzip: 0.32 kB
dist/inspect_diffusion_4.html                              0.58 kB │ gzip: 0.33 kB
dist/inspect_nak_pump.html                                 0.59 kB │ gzip: 0.34 kB
dist/inspect_diffusion_3.html                              0.59 kB │ gzip: 0.33 kB
dist/inspect_diffusion_2.html                              0.59 kB │ gzip: 0.34 kB
dist/inspect_resting_potential_2.html                      0.59 kB │ gzip: 0.33 kB
dist/inspect_resting_potential_1.html                      0.60 kB │ gzip: 0.34 kB
dist/inspect_resting_potential_nernst.html                 0.60 kB │ gzip: 0.34 kB
dist/inspect_electrochemical_1.html                        0.61 kB │ gzip: 0.34 kB
dist/inspect_electrochemical_3.html                        0.62 kB │ gzip: 0.35 kB
file update:
diff --git a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
index 7e72d55cd35ae3c2444359ae7ab9a07452c4bf12..2c37f0c606feabdb0d1adec7ab62e661d59f6f35
--- a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
+++ b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
@@ -3,11 +3,14 @@
   DEFAULT_DIFFUSION_SD,
   DEFAULT_NUM_PARTICLES,
   DEFAULT_POTENTIAL_SCALE,
+  DEFAULT_CHARGE_WALL_INSET,
   drawMembraneWall,
   MAX_PARTICLES,
   SIM_COLORS,
   centeredChannelBounds,
-  fieldColor
+  fieldColor,
+  leftEdgeChargeWallX,
+  wallFieldDrift
 } from './sim_shared';
 
 interface SimParams {
@@ -22,6 +25,7 @@
   channelWidth: number;
   voltageScale: number;
   nernstScale: number;
+  chargeWallInset: number;
 }
 
 interface DisplayParams {
@@ -42,6 +46,7 @@
   boxHeight: number;
   leftWall: number;
   rightWall: number;
+  negX: number;
   channelYMin: number;
   channelYMax: number;
   initialLeftCount: number;
@@ -100,7 +105,8 @@
   electricStrength: -0.35,
   channelWidth: 20,
   voltageScale: DEFAULT_POTENTIAL_SCALE,
-  nernstScale: DEFAULT_POTENTIAL_SCALE
+  nernstScale: DEFAULT_POTENTIAL_SCALE,
+  chargeWallInset: DEFAULT_CHARGE_WALL_INSET
 };
 
 const defaultDisplay: DisplayParams = { pointSize: 2.2, playbackSpeed: 1, targetFps: 30, traceYLimit: 20 };
@@ -139,7 +145,8 @@
     electricStrength: clamp(params.electricStrength, -10, 10),
     channelWidth: clamp(params.channelWidth, 0.5, boxHeight - 2),
     voltageScale: clamp(params.voltageScale, 1, 500),
-    nernstScale: clamp(params.nernstScale, 1, 500)
+    nernstScale: clamp(params.nernstScale, 1, 500),
+    chargeWallInset: clamp(params.chargeWallInset, 0.5, Math.max(0.5, boxWidth - 1))
   };
 }
 
@@ -186,6 +193,7 @@
     boxHeight: p.boxHeight,
     leftWall,
     rightWall,
+    negX: leftEdgeChargeWallX(p.boxWidth, p.chargeWallInset),
     channelYMin,
     channelYMax,
     initialLeftCount: 0,
@@ -202,18 +210,21 @@
 function stepState(state: LiveState, params: SimParams, rng: Rng): void {
   const halfW = state.boxWidth / 2;
   const halfH = state.boxHeight / 2;
-  const driftX = params.electricStrength * clamp(state.vProxy / Math.max(1, params.voltageScale), -4, 4);
   for (let i = 0; i < state.numParticles; i += 1) {
     const xPrev = state.x[i];
     const yPrev = state.y[i];
-    const xTrial = reflectIntoBounds(xPrev + (rng.normal(0, params.diffusionSd) + driftX) * state.dt, -halfW, halfW);
+    const dx = state.negX - xPrev;
+    const driftX = wallFieldDrift(dx, params.electricStrength, 1);
+    const xTrial = reflectIntoBounds(xPrev + (rng.normal(0, params.diffusionSd) + driftX) * state.dt, state.negX, halfW);
     const yNew = reflectIntoBounds(yPrev + rng.normal(0, params.diffusionSd) * state.dt, -halfH, halfH);
     let xNew = xTrial;
     const tryingToCrossLeft = xPrev > state.rightWall && xTrial <= state.rightWall;
     const tryingToCrossRight = xPrev < state.leftWall && xTrial >= state.leftWall;
     const inChannel = yNew >= state.channelYMin && yNew <= state.channelYMax;
     if ((tryingToCrossLeft || tryingToCrossRight) && !inChannel) {
-      xNew = tryingToCrossLeft ? reflectIntoBounds(2 * state.rightWall - xTrial, -halfW, halfW) : reflectIntoBounds(2 * state.leftWall - xTrial, -halfW, halfW);
+      xNew = tryingToCrossLeft
+        ? reflectIntoBounds(2 * state.rightWall - xTrial, state.negX, halfW)
+        : reflectIntoBounds(2 * state.leftWall - xTrial, state.negX, halfW);
     } else if (!inChannel && xTrial > state.leftWall && xTrial < state.rightWall) {
       xNew = xPrev < 0 ? state.leftWall : state.rightWall;
     }
@@ -356,8 +367,8 @@
     dpr,
     channels: [{ top: cTop, bottom: cBottom }]
   });
-  const [chargeWallX] = toPx((state.leftWall + state.rightWall) / 2, 0);
-  ctx.strokeStyle = fieldColor(state.vProxy);
+  const [chargeWallX] = toPx(state.negX, 0);
+  ctx.strokeStyle = fieldColor(simParams.electricStrength);
   ctx.lineWidth = 1.5 * dpr;
   ctx.setLineDash([5 * dpr, 4 * dpr]);
   ctx.beginPath();
@@ -384,7 +395,13 @@
       <h1>Nernst Equilibrium for One Ion</h1>
       <ul class="key-points">
         <li>Membrane potential is determined by relative ion concentration inside vs outside the cell.</li>
-        <li>At equilibrium (no net flux), this is given by the Nernst equation: V = S ln(C_out / C_in).</li>
+        <li>
+          At equilibrium (no net flux), this simulation uses
+          <math display="inline" aria-label="V equals S times natural log of C out over C in">
+            <mi>V</mi><mo>=</mo><mi>S</mi><mo>ln</mo><mo>(</mo><mfrac><msub><mi>C</mi><mtext>out</mtext></msub><msub><mi>C</mi><mtext>in</mtext></msub></mfrac><mo>)</mo>
+          </math>.
+        </li>
+        <li>Inside is the left compartment and outside is the right compartment; the charge wall is at the far-left inside boundary.</li>
       </ul>
     </header>
     <div class="sim-layout">
@@ -397,7 +414,7 @@
             <div class="control-grid">
               <div class="field"><label for="num-particles">Particles</label><input id="num-particles" type="number" min="10" max="5000" step="1" /></div>
               <div class="field"><label for="diffusion-sd">Diffusion SD</label><input id="diffusion-sd" type="number" min="0" max="20" step="0.01" /></div>
-              <div class="field"><label for="electric-strength">Electric strength (charge wall)</label><input id="electric-strength" type="number" min="-10" max="10" step="0.01" /></div>
+              <div class="field"><label for="electric-strength">Electric strength (inside charge wall)</label><input id="electric-strength" type="number" min="-10" max="10" step="0.01" /></div>
               <div class="field"><label for="channel-width">Channel width</label><input id="channel-width" type="number" min="0.5" step="0.5" /></div>
               <div class="field"><label for="playback-speed">Playback speed</label><input id="playback-speed" type="number" min="0.1" max="8" step="0.1" /></div>
               <div class="field"><label for="seed">Seed</label><input id="seed" type="number" min="0" max="4294967295" step="1" /></div>
@@ -436,8 +453,13 @@
               <p>
                 Purple is the simulated membrane potential from compartment concentrations.
                 White is the Nernst prediction for a single permeant cation:
-                <code>V = S ln(C_out / C_in) = S ln(C_right / C_left)</code>.
               </p>
+              <math display="block" aria-label="V equals S times natural log of C out over C in equals S times natural log of C right over C left">
+                <mi>V</mi><mo>=</mo><mi>S</mi><mo>ln</mo><mo>(</mo><mfrac><msub><mi>C</mi><mtext>out</mtext></msub><msub><mi>C</mi><mtext>in</mtext></msub></mfrac><mo>)</mo>
+                <mo>=</mo>
+                <mi>S</mi><mo>ln</mo><mo>(</mo><mfrac><msub><mi>C</mi><mtext>right</mtext></msub><msub><mi>C</mi><mtext>left</mtext></msub></mfrac><mo>)</mo>
+              </math>
+              <p>In this layout, inside is left and outside is right.</p>
             </div>
           </div>
         </div>
@@ -449,7 +471,7 @@
         </div>
         <div class="legend">
           <span><span class="swatch" style="background:${SIM_COLORS.ionA}"></span>Positive ion</span>
-          <span><span class="swatch" style="background:${SIM_COLORS.fieldNegative}"></span>Charge wall</span>
+          <span><span class="swatch" style="background:${SIM_COLORS.fieldNegative}"></span>Inside charge wall (far left)</span>
           <span><span class="swatch" style="background:${SIM_COLORS.totalTrace}"></span>Simulated membrane potential trace</span>
           <span><span class="swatch" style="background:${SIM_COLORS.predictionTrace}"></span>Nernst prediction trace</span>
         </div>
@@ -540,7 +562,8 @@
     electricStrength: clamp(Number(inputs.electricStrength.value) || 0, -10, 10),
     channelWidth: clamp(Number(inputs.channelWidth.value) || defaultSim.channelWidth, 0.5, boxHeight - 2),
     voltageScale: clamp(Number(inputs.voltageScale.value) || defaultSim.voltageScale, 1, 500),
-    nernstScale: clamp(Number(inputs.nernstScale.value) || defaultSim.nernstScale, 1, 500)
+    nernstScale: clamp(Number(inputs.nernstScale.value) || defaultSim.nernstScale, 1, 500),
+    chargeWallInset: simParams.chargeWallInset
   });
 }
 function readDisplayInputs(): DisplayParams {
@@ -554,8 +577,8 @@
 function updateEquationText(): void {
   const stepSd = simParams.diffusionSd * simParams.dt;
   equationBlock.innerHTML = [
-    '<span class="accent">Live update with a membrane charge-wall field term</span>',
-    'drift_x = electric_strength · (V_sim / voltage_scale)',
+    '<span class="accent">Live update with a left inside charge-wall field term</span>',
+    'drift_x = wallFieldDrift(charge_wall_x - x, electric_strength)',
     'x_new = x_old + (dxdt + drift_x) · dt',
     'y_new = reflect(y_old + dydt · dt, -H/2, H/2)',
     'dxdt, dydt ~ Normal(0, diffusionSd²)',
@@ -563,10 +586,10 @@
     '<span class="accent-2">Simulated Vm and Nernst comparison</span>',
     'V_sim = nernst_scale · ln((C_out + 0.5) / (C_in + 0.5))',
     'V_Nernst = nernst_scale · ln((C_out + 0.5) / (C_in + 0.5))',
-    'For a single positive ion, Nernst predicts the charge-wall voltage that balances the concentration gradient',
+    'For a single positive ion, Nernst predicts the balance point for diffusion vs electrical drift',
     '',
     `Per-step Brownian displacement SD = diffusionSd × dt = ${stepSd.toFixed(3)}`,
-    `Electric strength (charge wall) = ${simParams.electricStrength.toFixed(3)}`,
+    `Electric strength (inside charge wall) = ${simParams.electricStrength.toFixed(3)}`,
     `Trace window T = ${simParams.T.toFixed(0)} ms`
   ].join('\n');
 }
@@ -610,7 +633,7 @@
   const halfW = stateRef.boxWidth / 2;
   const halfH = stateRef.boxHeight / 2;
   for (let i = 0; i < stateRef.numParticles; i += 1) {
-    stateRef.x[i] = reflectIntoBounds(stateRef.x[i], -halfW, halfW);
+    stateRef.x[i] = reflectIntoBounds(stateRef.x[i], stateRef.negX, halfW);
     stateRef.y[i] = reflectIntoBounds(stateRef.y[i], -halfH, halfH);
     if (stateRef.x[i] > stateRef.leftWall && stateRef.x[i] < stateRef.rightWall && !(stateRef.y[i] >= stateRef.channelYMin && stateRef.y[i] <= stateRef.channelYMax)) {
       stateRef.x[i] = stateRef.x[i] < 0 ? stateRef.leftWall : stateRef.rightWall;
@@ -651,6 +674,7 @@
   state.boxHeight = simParams.boxHeight;
   state.leftWall = -simParams.wallThickness / 2;
   state.rightWall = simParams.wallThickness / 2;
+  state.negX = leftEdgeChargeWallX(simParams.boxWidth, simParams.chargeWallInset);
   [state.channelYMin, state.channelYMax] = centeredChannelBounds(simParams.channelWidth, simParams.boxHeight);
   enforceGeometry(state);
   trimTrace(trace, state.simTime, simParams.T);

exec
/bin/zsh -lc "git diff -- src/pages/inspect_resting_potential_nernst.ts | sed -n '1,260p'" in /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_web succeeded in 52ms:

file update:
diff --git a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
index 7e72d55cd35ae3c2444359ae7ab9a07452c4bf12..2c37f0c606feabdb0d1adec7ab62e661d59f6f35
--- a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
+++ b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
@@ -3,11 +3,14 @@
   DEFAULT_DIFFUSION_SD,
   DEFAULT_NUM_PARTICLES,
   DEFAULT_POTENTIAL_SCALE,
+  DEFAULT_CHARGE_WALL_INSET,
   drawMembraneWall,
   MAX_PARTICLES,
   SIM_COLORS,
   centeredChannelBounds,
-  fieldColor
+  fieldColor,
+  leftEdgeChargeWallX,
+  wallFieldDrift
 } from './sim_shared';
 
 interface SimParams {
@@ -22,6 +25,7 @@
   channelWidth: number;
   voltageScale: number;
   nernstScale: number;
+  chargeWallInset: number;
 }
 
 interface DisplayParams {
@@ -42,6 +46,7 @@
   boxHeight: number;
   leftWall: number;
   rightWall: number;
+  negX: number;
   channelYMin: number;
   channelYMax: number;
   initialLeftCount: number;
@@ -100,7 +105,8 @@
   electricStrength: -0.35,
   channelWidth: 20,
   voltageScale: DEFAULT_POTENTIAL_SCALE,
-  nernstScale: DEFAULT_POTENTIAL_SCALE
+  nernstScale: DEFAULT_POTENTIAL_SCALE,
+  chargeWallInset: DEFAULT_CHARGE_WALL_INSET
 };
 
 const defaultDisplay: DisplayParams = { pointSize: 2.2, playbackSpeed: 1, targetFps: 30, traceYLimit: 20 };
@@ -139,7 +145,8 @@
     electricStrength: clamp(params.electricStrength, -10, 10),
     channelWidth: clamp(params.channelWidth, 0.5, boxHeight - 2),
     voltageScale: clamp(params.voltageScale, 1, 500),
-    nernstScale: clamp(params.nernstScale, 1, 500)
+    nernstScale: clamp(params.nernstScale, 1, 500),
+    chargeWallInset: clamp(params.chargeWallInset, 0.5, Math.max(0.5, boxWidth - 1))
   };
 }
 
@@ -186,6 +193,7 @@
     boxHeight: p.boxHeight,
     leftWall,
     rightWall,
+    negX: leftEdgeChargeWallX(p.boxWidth, p.chargeWallInset),
     channelYMin,
     channelYMax,
     initialLeftCount: 0,
@@ -202,18 +210,21 @@
 function stepState(state: LiveState, params: SimParams, rng: Rng): void {
   const halfW = state.boxWidth / 2;
   const halfH = state.boxHeight / 2;
-  const driftX = params.electricStrength * clamp(state.vProxy / Math.max(1, params.voltageScale), -4, 4);
   for (let i = 0; i < state.numParticles; i += 1) {
     const xPrev = state.x[i];
     const yPrev = state.y[i];
-    const xTrial = reflectIntoBounds(xPrev + (rng.normal(0, params.diffusionSd) + driftX) * state.dt, -halfW, halfW);
+    const dx = state.negX - xPrev;
+    const driftX = wallFieldDrift(dx, params.electricStrength, 1);
+    const xTrial = reflectIntoBounds(xPrev + (rng.normal(0, params.diffusionSd) + driftX) * state.dt, state.negX, halfW);
     const yNew = reflectIntoBounds(yPrev + rng.normal(0, params.diffusionSd) * state.dt, -halfH, halfH);
     let xNew = xTrial;
     const tryingToCrossLeft = xPrev > state.rightWall && xTrial <= state.rightWall;
     const tryingToCrossRight = xPrev < state.leftWall && xTrial >= state.leftWall;
     const inChannel = yNew >= state.channelYMin && yNew <= state.channelYMax;
     if ((tryingToCrossLeft || tryingToCrossRight) && !inChannel) {
-      xNew = tryingToCrossLeft ? reflectIntoBounds(2 * state.rightWall - xTrial, -halfW, halfW) : reflectIntoBounds(2 * state.leftWall - xTrial, -halfW, halfW);
+      xNew = tryingToCrossLeft
+        ? reflectIntoBounds(2 * state.rightWall - xTrial, state.negX, halfW)
+        : reflectIntoBounds(2 * state.leftWall - xTrial, state.negX, halfW);
     } else if (!inChannel && xTrial > state.leftWall && xTrial < state.rightWall) {
       xNew = xPrev < 0 ? state.leftWall : state.rightWall;
     }
@@ -356,8 +367,8 @@
     dpr,
     channels: [{ top: cTop, bottom: cBottom }]
   });
-  const [chargeWallX] = toPx((state.leftWall + state.rightWall) / 2, 0);
-  ctx.strokeStyle = fieldColor(state.vProxy);
+  const [chargeWallX] = toPx(state.negX, 0);
+  ctx.strokeStyle = fieldColor(simParams.electricStrength);
   ctx.lineWidth = 1.5 * dpr;
   ctx.setLineDash([5 * dpr, 4 * dpr]);
   ctx.beginPath();
@@ -384,7 +395,13 @@
       <h1>Nernst Equilibrium for One Ion</h1>
       <ul class="key-points">
         <li>Membrane potential is determined by relative ion concentration inside vs outside the cell.</li>
-        <li>At equilibrium (no net flux), this is given by the Nernst equation: V = S ln(C_out / C_in).</li>
+        <li>
+          At equilibrium (no net flux), this simulation uses
+          <math display="inline" aria-label="V equals S times natural log of C out over C in">
+            <mi>V</mi><mo>=</mo><mi>S</mi><mo>ln</mo><mo>(</mo><mfrac><msub><mi>C</mi><mtext>out</mtext></msub><msub><mi>C</mi><mtext>in</mtext></msub></mfrac><mo>)</mo>
+          </math>.
+        </li>
+        <li>Inside is the left compartment and outside is the right compartment; the charge wall is at the far-left inside boundary.</li>
       </ul>
     </header>
     <div class="sim-layout">
@@ -397,7 +414,7 @@
             <div class="control-grid">
               <div class="field"><label for="num-particles">Particles</label><input id="num-particles" type="number" min="10" max="5000" step="1" /></div>
               <div class="field"><label for="diffusion-sd">Diffusion SD</label><input id="diffusion-sd" type="number" min="0" max="20" step="0.01" /></div>
-              <div class="field"><label for="electric-strength">Electric strength (charge wall)</label><input id="electric-strength" type="number" min="-10" max="10" step="0.01" /></div>
+              <div class="field"><label for="electric-strength">Electric strength (inside charge wall)</label><input id="electric-strength" type="number" min="-10" max="10" step="0.01" /></div>
               <div class="field"><label for="channel-width">Channel width</label><input id="channel-width" type="number" min="0.5" step="0.5" /></div>
               <div class="field"><label for="playback-speed">Playback speed</label><input id="playback-speed" type="number" min="0.1" max="8" step="0.1" /></div>
               <div class="field"><label for="seed">Seed</label><input id="seed" type="number" min="0" max="4294967295" step="1" /></div>
@@ -436,8 +453,13 @@
               <p>
                 Purple is the simulated membrane potential from compartment concentrations.
                 White is the Nernst prediction for a single permeant cation:
-                <code>V = S ln(C_out / C_in) = S ln(C_right / C_left)</code>.
               </p>
+              <math display="block" aria-label="V equals S times natural log of C out over C in equals S times natural log of C right over C left">
+                <mi>V</mi><mo>=</mo><mi>S</mi><mo>ln</mo><mo>(</mo><mfrac><msub><mi>C</mi><mtext>out</mtext></msub><msub><mi>C</mi><mtext>in</mtext></msub></mfrac><mo>)</mo>
+                <mo>=</mo>
+                <mi>S</mi><mo>ln</mo><mo>(</mo><mfrac><msub><mi>C</mi><mtext>right</mtext></msub><msub><mi>C</mi><mtext>left</mtext></msub></mfrac><mo>)</mo>
+              </math>
+              <p>In this layout, inside is left and outside is right.</p>
             </div>
           </div>
         </div>
@@ -449,7 +471,7 @@
         </div>
         <div class="legend">
           <span><span class="swatch" style="background:${SIM_COLORS.ionA}"></span>Positive ion</span>
-          <span><span class="swatch" style="background:${SIM_COLORS.fieldNegative}"></span>Charge wall</span>
+          <span><span class="swatch" style="background:${SIM_COLORS.fieldNegative}"></span>Inside charge wall (far left)</span>
           <span><span class="swatch" style="background:${SIM_COLORS.totalTrace}"></span>Simulated membrane potential trace</span>
           <span><span class="swatch" style="background:${SIM_COLORS.predictionTrace}"></span>Nernst prediction trace</span>
         </div>
@@ -540,7 +562,8 @@
     electricStrength: clamp(Number(inputs.electricStrength.value) || 0, -10, 10),
     channelWidth: clamp(Number(inputs.channelWidth.value) || defaultSim.channelWidth, 0.5, boxHeight - 2),
     voltageScale: clamp(Number(inputs.voltageScale.value) || defaultSim.voltageScale, 1, 500),
-    nernstScale: clamp(Number(inputs.nernstScale.value) || defaultSim.nernstScale, 1, 500)
+    nernstScale: clamp(Number(inputs.nernstScale.value) || defaultSim.nernstScale, 1, 500),
+    chargeWallInset: simParams.chargeWallInset
   });
 }
 function readDisplayInputs(): DisplayParams {
@@ -554,8 +577,8 @@
 function updateEquationText(): void {
   const stepSd = simParams.diffusionSd * simParams.dt;
   equationBlock.innerHTML = [
-    '<span class="accent">Live update with a membrane charge-wall field term</span>',
-    'drift_x = electric_strength · (V_sim / voltage_scale)',
+    '<span class="accent">Live update with a left inside charge-wall field term</span>',
+    'drift_x = wallFieldDrift(charge_wall_x - x, electric_strength)',
     'x_new = x_old + (dxdt + drift_x) · dt',
     'y_new = reflect(y_old + dydt · dt, -H/2, H/2)',
     'dxdt, dydt ~ Normal(0, diffusionSd²)',
@@ -563,10 +586,10 @@
     '<span class="accent-2">Simulated Vm and Nernst comparison</span>',
     'V_sim = nernst_scale · ln((C_out + 0.5) / (C_in + 0.5))',
     'V_Nernst = nernst_scale · ln((C_out + 0.5) / (C_in + 0.5))',
-    'For a single positive ion, Nernst predicts the charge-wall voltage that balances the concentration gradient',
+    'For a single positive ion, Nernst predicts the balance point for diffusion vs electrical drift',
     '',
     `Per-step Brownian displacement SD = diffusionSd × dt = ${stepSd.toFixed(3)}`,
-    `Electric strength (charge wall) = ${simParams.electricStrength.toFixed(3)}`,
+    `Electric strength (inside charge wall) = ${simParams.electricStrength.toFixed(3)}`,
     `Trace window T = ${simParams.T.toFixed(0)} ms`
   ].join('\n');
 }
@@ -610,7 +633,7 @@
   const halfW = stateRef.boxWidth / 2;
   const halfH = stateRef.boxHeight / 2;
   for (let i = 0; i < stateRef.numParticles; i += 1) {
-    stateRef.x[i] = reflectIntoBounds(stateRef.x[i], -halfW, halfW);
+    stateRef.x[i] = reflectIntoBounds(stateRef.x[i], stateRef.negX, halfW);
     stateRef.y[i] = reflectIntoBounds(stateRef.y[i], -halfH, halfH);
     if (stateRef.x[i] > stateRef.leftWall && stateRef.x[i] < stateRef.rightWall && !(stateRef.y[i] >= stateRef.channelYMin && stateRef.y[i] <= stateRef.channelYMax)) {
       stateRef.x[i] = stateRef.x[i] < 0 ? stateRef.leftWall : stateRef.rightWall;
@@ -651,6 +674,7 @@
   state.boxHeight = simParams.boxHeight;
   state.leftWall = -simParams.wallThickness / 2;
   state.rightWall = simParams.wallThickness / 2;
+  state.negX = leftEdgeChargeWallX(simParams.boxWidth, simParams.chargeWallInset);
   [state.channelYMin, state.channelYMax] = centeredChannelBounds(simParams.channelWidth, simParams.boxHeight);
   enforceGeometry(state);
   trimTrace(trace, state.simTime, simParams.T);

exec
/bin/zsh -lc 'git status --short' in /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_web succeeded in 52ms:
 M README.md
 M inspect_diffusion_1.html
 M inspect_diffusion_2.html
 M inspect_diffusion_3.html
 M inspect_diffusion_4.html
 M inspect_electrochemical_1.html
 D inspect_electrochemical_2.html
 M inspect_electrochemical_3.html
 M inspect_resting_potential_1.html
 M inspect_resting_potential_2.html
 M inspect_resting_potential_3.html
 M inspect_resting_potential_4.html
 M src/index.ts
 M src/pages/inspect_diffusion_1.ts
 M src/pages/inspect_diffusion_2.ts
 M src/pages/inspect_diffusion_3.ts
 M src/pages/inspect_diffusion_4.ts
 M src/pages/inspect_electrochemical_1.ts
 D src/pages/inspect_electrochemical_2.ts
 M src/pages/inspect_electrochemical_3.ts
file update:
diff --git a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
index 7e72d55cd35ae3c2444359ae7ab9a07452c4bf12..2c37f0c606feabdb0d1adec7ab62e661d59f6f35
--- a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
+++ b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
@@ -3,11 +3,14 @@
   DEFAULT_DIFFUSION_SD,
   DEFAULT_NUM_PARTICLES,
   DEFAULT_POTENTIAL_SCALE,
+  DEFAULT_CHARGE_WALL_INSET,
   drawMembraneWall,
   MAX_PARTICLES,
   SIM_COLORS,
   centeredChannelBounds,
-  fieldColor
+  fieldColor,
+  leftEdgeChargeWallX,
+  wallFieldDrift
 } from './sim_shared';
 
 interface SimParams {
@@ -22,6 +25,7 @@
   channelWidth: number;
   voltageScale: number;
   nernstScale: number;
+  chargeWallInset: number;
 }
 
 interface DisplayParams {
@@ -42,6 +46,7 @@
   boxHeight: number;
   leftWall: number;
   rightWall: number;
+  negX: number;
   channelYMin: number;
   channelYMax: number;
   initialLeftCount: number;
@@ -100,7 +105,8 @@
   electricStrength: -0.35,
   channelWidth: 20,
   voltageScale: DEFAULT_POTENTIAL_SCALE,
-  nernstScale: DEFAULT_POTENTIAL_SCALE
+  nernstScale: DEFAULT_POTENTIAL_SCALE,
+  chargeWallInset: DEFAULT_CHARGE_WALL_INSET
 };
 
 const defaultDisplay: DisplayParams = { pointSize: 2.2, playbackSpeed: 1, targetFps: 30, traceYLimit: 20 };
@@ -139,7 +145,8 @@
     electricStrength: clamp(params.electricStrength, -10, 10),
     channelWidth: clamp(params.channelWidth, 0.5, boxHeight - 2),
     voltageScale: clamp(params.voltageScale, 1, 500),
-    nernstScale: clamp(params.nernstScale, 1, 500)
+    nernstScale: clamp(params.nernstScale, 1, 500),
+    chargeWallInset: clamp(params.chargeWallInset, 0.5, Math.max(0.5, boxWidth - 1))
   };
 }
 
@@ -186,6 +193,7 @@
     boxHeight: p.boxHeight,
     leftWall,
     rightWall,
+    negX: leftEdgeChargeWallX(p.boxWidth, p.chargeWallInset),
     channelYMin,
     channelYMax,
     initialLeftCount: 0,
@@ -202,18 +210,21 @@
 function stepState(state: LiveState, params: SimParams, rng: Rng): void {
   const halfW = state.boxWidth / 2;
   const halfH = state.boxHeight / 2;
-  const driftX = params.electricStrength * clamp(state.vProxy / Math.max(1, params.voltageScale), -4, 4);
   for (let i = 0; i < state.numParticles; i += 1) {
     const xPrev = state.x[i];
     const yPrev = state.y[i];
-    const xTrial = reflectIntoBounds(xPrev + (rng.normal(0, params.diffusionSd) + driftX) * state.dt, -halfW, halfW);
+    const dx = state.negX - xPrev;
+    const driftX = wallFieldDrift(dx, params.electricStrength, 1);
+    const xTrial = reflectIntoBounds(xPrev + (rng.normal(0, params.diffusionSd) + driftX) * state.dt, state.negX, halfW);
     const yNew = reflectIntoBounds(yPrev + rng.normal(0, params.diffusionSd) * state.dt, -halfH, halfH);
     let xNew = xTrial;
     const tryingToCrossLeft = xPrev > state.rightWall && xTrial <= state.rightWall;
     const tryingToCrossRight = xPrev < state.leftWall && xTrial >= state.leftWall;
     const inChannel = yNew >= state.channelYMin && yNew <= state.channelYMax;
     if ((tryingToCrossLeft || tryingToCrossRight) && !inChannel) {
-      xNew = tryingToCrossLeft ? reflectIntoBounds(2 * state.rightWall - xTrial, -halfW, halfW) : reflectIntoBounds(2 * state.leftWall - xTrial, -halfW, halfW);
+      xNew = tryingToCrossLeft
+        ? reflectIntoBounds(2 * state.rightWall - xTrial, state.negX, halfW)
+        : reflectIntoBounds(2 * state.leftWall - xTrial, state.negX, halfW);
     } else if (!inChannel && xTrial > state.leftWall && xTrial < state.rightWall) {
       xNew = xPrev < 0 ? state.leftWall : state.rightWall;
     }
@@ -356,8 +367,8 @@
     dpr,
     channels: [{ top: cTop, bottom: cBottom }]
   });
-  const [chargeWallX] = toPx((state.leftWall + state.rightWall) / 2, 0);
-  ctx.strokeStyle = fieldColor(state.vProxy);
+  const [chargeWallX] = toPx(state.negX, 0);
+  ctx.strokeStyle = fieldColor(simParams.electricStrength);
   ctx.lineWidth = 1.5 * dpr;
   ctx.setLineDash([5 * dpr, 4 * dpr]);
   ctx.beginPath();
@@ -384,7 +395,13 @@
       <h1>Nernst Equilibrium for One Ion</h1>
       <ul class="key-points">
         <li>Membrane potential is determined by relative ion concentration inside vs outside the cell.</li>
-        <li>At equilibrium (no net flux), this is given by the Nernst equation: V = S ln(C_out / C_in).</li>
+        <li>
+          At equilibrium (no net flux), this simulation uses
+          <math display="inline" aria-label="V equals S times natural log of C out over C in">
+            <mi>V</mi><mo>=</mo><mi>S</mi><mo>ln</mo><mo>(</mo><mfrac><msub><mi>C</mi><mtext>out</mtext></msub><msub><mi>C</mi><mtext>in</mtext></msub></mfrac><mo>)</mo>
+          </math>.
+        </li>
+        <li>Inside is the left compartment and outside is the right compartment; the charge wall is at the far-left inside boundary.</li>
       </ul>
     </header>
     <div class="sim-layout">
@@ -397,7 +414,7 @@
             <div class="control-grid">
               <div class="field"><label for="num-particles">Particles</label><input id="num-particles" type="number" min="10" max="5000" step="1" /></div>
               <div class="field"><label for="diffusion-sd">Diffusion SD</label><input id="diffusion-sd" type="number" min="0" max="20" step="0.01" /></div>
-              <div class="field"><label for="electric-strength">Electric strength (charge wall)</label><input id="electric-strength" type="number" min="-10" max="10" step="0.01" /></div>
+              <div class="field"><label for="electric-strength">Electric strength (inside charge wall)</label><input id="electric-strength" type="number" min="-10" max="10" step="0.01" /></div>
               <div class="field"><label for="channel-width">Channel width</label><input id="channel-width" type="number" min="0.5" step="0.5" /></div>
               <div class="field"><label for="playback-speed">Playback speed</label><input id="playback-speed" type="number" min="0.1" max="8" step="0.1" /></div>
               <div class="field"><label for="seed">Seed</label><input id="seed" type="number" min="0" max="4294967295" step="1" /></div>
@@ -436,8 +453,13 @@
               <p>
                 Purple is the simulated membrane potential from compartment concentrations.
                 White is the Nernst prediction for a single permeant cation:
-                <code>V = S ln(C_out / C_in) = S ln(C_right / C_left)</code>.
               </p>
+              <math display="block" aria-label="V equals S times natural log of C out over C in equals S times natural log of C right over C left">
+                <mi>V</mi><mo>=</mo><mi>S</mi><mo>ln</mo><mo>(</mo><mfrac><msub><mi>C</mi><mtext>out</mtext></msub><msub><mi>C</mi><mtext>in</mtext></msub></mfrac><mo>)</mo>
+                <mo>=</mo>
+                <mi>S</mi><mo>ln</mo><mo>(</mo><mfrac><msub><mi>C</mi><mtext>right</mtext></msub><msub><mi>C</mi><mtext>left</mtext></msub></mfrac><mo>)</mo>
+              </math>
+              <p>In this layout, inside is left and outside is right.</p>
             </div>
           </div>
         </div>
@@ -449,7 +471,7 @@
         </div>
         <div class="legend">
           <span><span class="swatch" style="background:${SIM_COLORS.ionA}"></span>Positive ion</span>
-          <span><span class="swatch" style="background:${SIM_COLORS.fieldNegative}"></span>Charge wall</span>
+          <span><span class="swatch" style="background:${SIM_COLORS.fieldNegative}"></span>Inside charge wall (far left)</span>
           <span><span class="swatch" style="background:${SIM_COLORS.totalTrace}"></span>Simulated membrane potential trace</span>
           <span><span class="swatch" style="background:${SIM_COLORS.predictionTrace}"></span>Nernst prediction trace</span>
         </div>
@@ -540,7 +562,8 @@
     electricStrength: clamp(Number(inputs.electricStrength.value) || 0, -10, 10),
     channelWidth: clamp(Number(inputs.channelWidth.value) || defaultSim.channelWidth, 0.5, boxHeight - 2),
     voltageScale: clamp(Number(inputs.voltageScale.value) || defaultSim.voltageScale, 1, 500),
-    nernstScale: clamp(Number(inputs.nernstScale.value) || defaultSim.nernstScale, 1, 500)
+    nernstScale: clamp(Number(inputs.nernstScale.value) || defaultSim.nernstScale, 1, 500),
+    chargeWallInset: simParams.chargeWallInset
   });
 }
 function readDisplayInputs(): DisplayParams {
@@ -554,8 +577,8 @@
 function updateEquationText(): void {
   const stepSd = simParams.diffusionSd * simParams.dt;
   equationBlock.innerHTML = [
-    '<span class="accent">Live update with a membrane charge-wall field term</span>',
-    'drift_x = electric_strength · (V_sim / voltage_scale)',
+    '<span class="accent">Live update with a left inside charge-wall field term</span>',
+    'drift_x = wallFieldDrift(charge_wall_x - x, electric_strength)',
     'x_new = x_old + (dxdt + drift_x) · dt',
     'y_new = reflect(y_old + dydt · dt, -H/2, H/2)',
     'dxdt, dydt ~ Normal(0, diffusionSd²)',
@@ -563,10 +586,10 @@
     '<span class="accent-2">Simulated Vm and Nernst comparison</span>',
     'V_sim = nernst_scale · ln((C_out + 0.5) / (C_in + 0.5))',
     'V_Nernst = nernst_scale · ln((C_out + 0.5) / (C_in + 0.5))',
-    'For a single positive ion, Nernst predicts the charge-wall voltage that balances the concentration gradient',
+    'For a single positive ion, Nernst predicts the balance point for diffusion vs electrical drift',
     '',
     `Per-step Brownian displacement SD = diffusionSd × dt = ${stepSd.toFixed(3)}`,
-    `Electric strength (charge wall) = ${simParams.electricStrength.toFixed(3)}`,
+    `Electric strength (inside charge wall) = ${simParams.electricStrength.toFixed(3)}`,
     `Trace window T = ${simParams.T.toFixed(0)} ms`
   ].join('\n');
 }
@@ -610,7 +633,7 @@
   const halfW = stateRef.boxWidth / 2;
   const halfH = stateRef.boxHeight / 2;
   for (let i = 0; i < stateRef.numParticles; i += 1) {
-    stateRef.x[i] = reflectIntoBounds(stateRef.x[i], -halfW, halfW);
+    stateRef.x[i] = reflectIntoBounds(stateRef.x[i], stateRef.negX, halfW);
     stateRef.y[i] = reflectIntoBounds(stateRef.y[i], -halfH, halfH);
     if (stateRef.x[i] > stateRef.leftWall && stateRef.x[i] < stateRef.rightWall && !(stateRef.y[i] >= stateRef.channelYMin && stateRef.y[i] <= stateRef.channelYMax)) {
       stateRef.x[i] = stateRef.x[i] < 0 ? stateRef.leftWall : stateRef.rightWall;
@@ -651,6 +674,7 @@
   state.boxHeight = simParams.boxHeight;
   state.leftWall = -simParams.wallThickness / 2;
   state.rightWall = simParams.wallThickness / 2;
+  state.negX = leftEdgeChargeWallX(simParams.boxWidth, simParams.chargeWallInset);
   [state.channelYMin, state.channelYMax] = centeredChannelBounds(simParams.channelWidth, simParams.boxHeight);
   enforceGeometry(state);
   trimTrace(trace, state.simTime, simParams.T);

codex
Build passes. I’m doing one quick content check on the target file to ensure both requested bullets are fully reflected before I summarize.
exec
/bin/zsh -lc "rg -n \"charge wall|far-left|math|V = S|wallFieldDrift|state\\.negX|Inside is|outside is\" src/pages/inspect_resting_potential_nernst.ts && sed -n '1,120p' src/pages/inspect_resting_potential_nernst.ts && sed -n '180,260p' src/pages/inspect_resting_potential_nernst.ts && sed -n '340,470p' src/pages/inspect_resting_potential_nernst.ts && sed -n '548,690p' src/pages/inspect_resting_potential_nernst.ts" in /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_web succeeded in 52ms:
13:  wallFieldDrift
216:    const dx = state.negX - xPrev;
217:    const driftX = wallFieldDrift(dx, params.electricStrength, 1);
218:    const xTrial = reflectIntoBounds(xPrev + (rng.normal(0, params.diffusionSd) + driftX) * state.dt, state.negX, halfW);
226:        ? reflectIntoBounds(2 * state.rightWall - xTrial, state.negX, halfW)
227:        : reflectIntoBounds(2 * state.leftWall - xTrial, state.negX, halfW);
370:  const [chargeWallX] = toPx(state.negX, 0);
400:          <math display="inline" aria-label="V equals S times natural log of C out over C in">
402:          </math>.
404:        <li>Inside is the left compartment and outside is the right compartment; the charge wall is at the far-left inside boundary.</li>
417:              <div class="field"><label for="electric-strength">Electric strength (inside charge wall)</label><input id="electric-strength" type="number" min="-10" max="10" step="0.01" /></div>
457:              <math display="block" aria-label="V equals S times natural log of C out over C in equals S times natural log of C right over C left">
461:              </math>
462:              <p>In this layout, inside is left and outside is right.</p>
474:          <span><span class="swatch" style="background:${SIM_COLORS.fieldNegative}"></span>Inside charge wall (far left)</span>
581:    'drift_x = wallFieldDrift(charge_wall_x - x, electric_strength)',
592:    `Electric strength (inside charge wall) = ${simParams.electricStrength.toFixed(3)}`,
677:  state.negX = leftEdgeChargeWallX(simParams.boxWidth, simParams.chargeWallInset);
import '../style.css';
import {
file update:
diff --git a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
index 7e72d55cd35ae3c2444359ae7ab9a07452c4bf12..2c37f0c606feabdb0d1adec7ab62e661d59f6f35
--- a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
+++ b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
@@ -3,11 +3,14 @@
   DEFAULT_DIFFUSION_SD,
   DEFAULT_NUM_PARTICLES,
   DEFAULT_POTENTIAL_SCALE,
+  DEFAULT_CHARGE_WALL_INSET,
   drawMembraneWall,
   MAX_PARTICLES,
   SIM_COLORS,
   centeredChannelBounds,
-  fieldColor
+  fieldColor,
+  leftEdgeChargeWallX,
+  wallFieldDrift
 } from './sim_shared';
 
 interface SimParams {
@@ -22,6 +25,7 @@
   channelWidth: number;
   voltageScale: number;
   nernstScale: number;
+  chargeWallInset: number;
 }
 
 interface DisplayParams {
@@ -42,6 +46,7 @@
   boxHeight: number;
   leftWall: number;
   rightWall: number;
+  negX: number;
   channelYMin: number;
   channelYMax: number;
   initialLeftCount: number;
@@ -100,7 +105,8 @@
   electricStrength: -0.35,
   channelWidth: 20,
   voltageScale: DEFAULT_POTENTIAL_SCALE,
-  nernstScale: DEFAULT_POTENTIAL_SCALE
+  nernstScale: DEFAULT_POTENTIAL_SCALE,
+  chargeWallInset: DEFAULT_CHARGE_WALL_INSET
 };
 
 const defaultDisplay: DisplayParams = { pointSize: 2.2, playbackSpeed: 1, targetFps: 30, traceYLimit: 20 };
@@ -139,7 +145,8 @@
     electricStrength: clamp(params.electricStrength, -10, 10),
     channelWidth: clamp(params.channelWidth, 0.5, boxHeight - 2),
     voltageScale: clamp(params.voltageScale, 1, 500),
-    nernstScale: clamp(params.nernstScale, 1, 500)
+    nernstScale: clamp(params.nernstScale, 1, 500),
+    chargeWallInset: clamp(params.chargeWallInset, 0.5, Math.max(0.5, boxWidth - 1))
   };
 }
 
@@ -186,6 +193,7 @@
     boxHeight: p.boxHeight,
     leftWall,
     rightWall,
+    negX: leftEdgeChargeWallX(p.boxWidth, p.chargeWallInset),
     channelYMin,
     channelYMax,
     initialLeftCount: 0,
@@ -202,18 +210,21 @@
 function stepState(state: LiveState, params: SimParams, rng: Rng): void {
   const halfW = state.boxWidth / 2;
   const halfH = state.boxHeight / 2;
-  const driftX = params.electricStrength * clamp(state.vProxy / Math.max(1, params.voltageScale), -4, 4);
   for (let i = 0; i < state.numParticles; i += 1) {
     const xPrev = state.x[i];
     const yPrev = state.y[i];
-    const xTrial = reflectIntoBounds(xPrev + (rng.normal(0, params.diffusionSd) + driftX) * state.dt, -halfW, halfW);
+    const dx = state.negX - xPrev;
+    const driftX = wallFieldDrift(dx, params.electricStrength, 1);
+    const xTrial = reflectIntoBounds(xPrev + (rng.normal(0, params.diffusionSd) + driftX) * state.dt, state.negX, halfW);
     const yNew = reflectIntoBounds(yPrev + rng.normal(0, params.diffusionSd) * state.dt, -halfH, halfH);
     let xNew = xTrial;
     const tryingToCrossLeft = xPrev > state.rightWall && xTrial <= state.rightWall;
     const tryingToCrossRight = xPrev < state.leftWall && xTrial >= state.leftWall;
     const inChannel = yNew >= state.channelYMin && yNew <= state.channelYMax;
     if ((tryingToCrossLeft || tryingToCrossRight) && !inChannel) {
-      xNew = tryingToCrossLeft ? reflectIntoBounds(2 * state.rightWall - xTrial, -halfW, halfW) : reflectIntoBounds(2 * state.leftWall - xTrial, -halfW, halfW);
+      xNew = tryingToCrossLeft
+        ? reflectIntoBounds(2 * state.rightWall - xTrial, state.negX, halfW)
+        : reflectIntoBounds(2 * state.leftWall - xTrial, state.negX, halfW);
     } else if (!inChannel && xTrial > state.leftWall && xTrial < state.rightWall) {
       xNew = xPrev < 0 ? state.leftWall : state.rightWall;
     }
@@ -356,8 +367,8 @@
     dpr,
     channels: [{ top: cTop, bottom: cBottom }]
   });
-  const [chargeWallX] = toPx((state.leftWall + state.rightWall) / 2, 0);
-  ctx.strokeStyle = fieldColor(state.vProxy);
+  const [chargeWallX] = toPx(state.negX, 0);
+  ctx.strokeStyle = fieldColor(simParams.electricStrength);
   ctx.lineWidth = 1.5 * dpr;
   ctx.setLineDash([5 * dpr, 4 * dpr]);
   ctx.beginPath();
@@ -384,7 +395,13 @@
       <h1>Nernst Equilibrium for One Ion</h1>
       <ul class="key-points">
         <li>Membrane potential is determined by relative ion concentration inside vs outside the cell.</li>
-        <li>At equilibrium (no net flux), this is given by the Nernst equation: V = S ln(C_out / C_in).</li>
+        <li>
+          At equilibrium (no net flux), this simulation uses
+          <math display="inline" aria-label="V equals S times natural log of C out over C in">
+            <mi>V</mi><mo>=</mo><mi>S</mi><mo>ln</mo><mo>(</mo><mfrac><msub><mi>C</mi><mtext>out</mtext></msub><msub><mi>C</mi><mtext>in</mtext></msub></mfrac><mo>)</mo>
+          </math>.
+        </li>
+        <li>Inside is the left compartment and outside is the right compartment; the charge wall is at the far-left inside boundary.</li>
       </ul>
     </header>
     <div class="sim-layout">
@@ -397,7 +414,7 @@
             <div class="control-grid">
               <div class="field"><label for="num-particles">Particles</label><input id="num-particles" type="number" min="10" max="5000" step="1" /></div>
               <div class="field"><label for="diffusion-sd">Diffusion SD</label><input id="diffusion-sd" type="number" min="0" max="20" step="0.01" /></div>
-              <div class="field"><label for="electric-strength">Electric strength (charge wall)</label><input id="electric-strength" type="number" min="-10" max="10" step="0.01" /></div>
+              <div class="field"><label for="electric-strength">Electric strength (inside charge wall)</label><input id="electric-strength" type="number" min="-10" max="10" step="0.01" /></div>
               <div class="field"><label for="channel-width">Channel width</label><input id="channel-width" type="number" min="0.5" step="0.5" /></div>
               <div class="field"><label for="playback-speed">Playback speed</label><input id="playback-speed" type="number" min="0.1" max="8" step="0.1" /></div>
               <div class="field"><label for="seed">Seed</label><input id="seed" type="number" min="0" max="4294967295" step="1" /></div>
@@ -436,8 +453,13 @@
               <p>
                 Purple is the simulated membrane potential from compartment concentrations.
                 White is the Nernst prediction for a single permeant cation:
-                <code>V = S ln(C_out / C_in) = S ln(C_right / C_left)</code>.
               </p>
+              <math display="block" aria-label="V equals S times natural log of C out over C in equals S times natural log of C right over C left">
+                <mi>V</mi><mo>=</mo><mi>S</mi><mo>ln</mo><mo>(</mo><mfrac><msub><mi>C</mi><mtext>out</mtext></msub><msub><mi>C</mi><mtext>in</mtext></msub></mfrac><mo>)</mo>
+                <mo>=</mo>
+                <mi>S</mi><mo>ln</mo><mo>(</mo><mfrac><msub><mi>C</mi><mtext>right</mtext></msub><msub><mi>C</mi><mtext>left</mtext></msub></mfrac><mo>)</mo>
+              </math>
+              <p>In this layout, inside is left and outside is right.</p>
             </div>
           </div>
         </div>
@@ -449,7 +471,7 @@
         </div>
         <div class="legend">
           <span><span class="swatch" style="background:${SIM_COLORS.ionA}"></span>Positive ion</span>
-          <span><span class="swatch" style="background:${SIM_COLORS.fieldNegative}"></span>Charge wall</span>
+          <span><span class="swatch" style="background:${SIM_COLORS.fieldNegative}"></span>Inside charge wall (far left)</span>
           <span><span class="swatch" style="background:${SIM_COLORS.totalTrace}"></span>Simulated membrane potential trace</span>
           <span><span class="swatch" style="background:${SIM_COLORS.predictionTrace}"></span>Nernst prediction trace</span>
         </div>
@@ -540,7 +562,8 @@
     electricStrength: clamp(Number(inputs.electricStrength.value) || 0, -10, 10),
     channelWidth: clamp(Number(inputs.channelWidth.value) || defaultSim.channelWidth, 0.5, boxHeight - 2),
     voltageScale: clamp(Number(inputs.voltageScale.value) || defaultSim.voltageScale, 1, 500),
-    nernstScale: clamp(Number(inputs.nernstScale.value) || defaultSim.nernstScale, 1, 500)
+    nernstScale: clamp(Number(inputs.nernstScale.value) || defaultSim.nernstScale, 1, 500),
+    chargeWallInset: simParams.chargeWallInset
   });
 }
 function readDisplayInputs(): DisplayParams {
@@ -554,8 +577,8 @@
 function updateEquationText(): void {
   const stepSd = simParams.diffusionSd * simParams.dt;
   equationBlock.innerHTML = [
-    '<span class="accent">Live update with a membrane charge-wall field term</span>',
-    'drift_x = electric_strength · (V_sim / voltage_scale)',
+    '<span class="accent">Live update with a left inside charge-wall field term</span>',
+    'drift_x = wallFieldDrift(charge_wall_x - x, electric_strength)',
     'x_new = x_old + (dxdt + drift_x) · dt',
     'y_new = reflect(y_old + dydt · dt, -H/2, H/2)',
     'dxdt, dydt ~ Normal(0, diffusionSd²)',
@@ -563,10 +586,10 @@
     '<span class="accent-2">Simulated Vm and Nernst comparison</span>',
     'V_sim = nernst_scale · ln((C_out + 0.5) / (C_in + 0.5))',
     'V_Nernst = nernst_scale · ln((C_out + 0.5) / (C_in + 0.5))',
-    'For a single positive ion, Nernst predicts the charge-wall voltage that balances the concentration gradient',
+    'For a single positive ion, Nernst predicts the balance point for diffusion vs electrical drift',
     '',
     `Per-step Brownian displacement SD = diffusionSd × dt = ${stepSd.toFixed(3)}`,
-    `Electric strength (charge wall) = ${simParams.electricStrength.toFixed(3)}`,
+    `Electric strength (inside charge wall) = ${simParams.electricStrength.toFixed(3)}`,
     `Trace window T = ${simParams.T.toFixed(0)} ms`
   ].join('\n');
 }
@@ -610,7 +633,7 @@
   const halfW = stateRef.boxWidth / 2;
   const halfH = stateRef.boxHeight / 2;
   for (let i = 0; i < stateRef.numParticles; i += 1) {
-    stateRef.x[i] = reflectIntoBounds(stateRef.x[i], -halfW, halfW);
+    stateRef.x[i] = reflectIntoBounds(stateRef.x[i], stateRef.negX, halfW);
     stateRef.y[i] = reflectIntoBounds(stateRef.y[i], -halfH, halfH);
     if (stateRef.x[i] > stateRef.leftWall && stateRef.x[i] < stateRef.rightWall && !(stateRef.y[i] >= stateRef.channelYMin && stateRef.y[i] <= stateRef.channelYMax)) {
       stateRef.x[i] = stateRef.x[i] < 0 ? stateRef.leftWall : stateRef.rightWall;
@@ -651,6 +674,7 @@
   state.boxHeight = simParams.boxHeight;
   state.leftWall = -simParams.wallThickness / 2;
   state.rightWall = simParams.wallThickness / 2;
+  state.negX = leftEdgeChargeWallX(simParams.boxWidth, simParams.chargeWallInset);
   [state.channelYMin, state.channelYMax] = centeredChannelBounds(simParams.channelWidth, simParams.boxHeight);
   enforceGeometry(state);
   trimTrace(trace, state.simTime, simParams.T);

codex
Implemented only the **“Nernst Equilibrium for One Ion”** requests in [src/pages/inspect_resting_potential_nernst.ts](/Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts).

Changes made:
1. Replaced plain-text Nernst equation display with rendered math markup (`<math>…</math>`) in the key points and equation card.
2. Removed the middle charge-wall behavior/visual and switched to a far-left inside charge wall:
   - Added `negX` (left-edge charge wall position) to state.
   - Updated drift to use `wallFieldDrift(charge_wall_x - x, electric_strength)`.
   - Constrained x-motion to reflect from that left inside wall.
   - Drew the dashed charge wall at the far-left inside boundary instead of membrane center.
3. Updated copy/labels/legend to match the requested framing:
   - Inside = left, outside = right.
   - Electric control label and legend now refer to the inside far-left charge wall.

Validation run:
1. `npm run build` completed successfully (`tsc && vite build`).
file update:
diff --git a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
index 7e72d55cd35ae3c2444359ae7ab9a07452c4bf12..2c37f0c606feabdb0d1adec7ab62e661d59f6f35
--- a/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
+++ b/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts
@@ -3,11 +3,14 @@
   DEFAULT_DIFFUSION_SD,
   DEFAULT_NUM_PARTICLES,
   DEFAULT_POTENTIAL_SCALE,
+  DEFAULT_CHARGE_WALL_INSET,
   drawMembraneWall,
   MAX_PARTICLES,
   SIM_COLORS,
   centeredChannelBounds,
-  fieldColor
+  fieldColor,
+  leftEdgeChargeWallX,
+  wallFieldDrift
 } from './sim_shared';
 
 interface SimParams {
@@ -22,6 +25,7 @@
   channelWidth: number;
   voltageScale: number;
   nernstScale: number;
+  chargeWallInset: number;
 }
 
 interface DisplayParams {
@@ -42,6 +46,7 @@
   boxHeight: number;
   leftWall: number;
   rightWall: number;
+  negX: number;
   channelYMin: number;
   channelYMax: number;
   initialLeftCount: number;
@@ -100,7 +105,8 @@
   electricStrength: -0.35,
   channelWidth: 20,
   voltageScale: DEFAULT_POTENTIAL_SCALE,
-  nernstScale: DEFAULT_POTENTIAL_SCALE
+  nernstScale: DEFAULT_POTENTIAL_SCALE,
+  chargeWallInset: DEFAULT_CHARGE_WALL_INSET
 };
 
 const defaultDisplay: DisplayParams = { pointSize: 2.2, playbackSpeed: 1, targetFps: 30, traceYLimit: 20 };
@@ -139,7 +145,8 @@
     electricStrength: clamp(params.electricStrength, -10, 10),
     channelWidth: clamp(params.channelWidth, 0.5, boxHeight - 2),
     voltageScale: clamp(params.voltageScale, 1, 500),
-    nernstScale: clamp(params.nernstScale, 1, 500)
+    nernstScale: clamp(params.nernstScale, 1, 500),
+    chargeWallInset: clamp(params.chargeWallInset, 0.5, Math.max(0.5, boxWidth - 1))
   };
 }
 
@@ -186,6 +193,7 @@
     boxHeight: p.boxHeight,
     leftWall,
     rightWall,
+    negX: leftEdgeChargeWallX(p.boxWidth, p.chargeWallInset),
     channelYMin,
     channelYMax,
     initialLeftCount: 0,
@@ -202,18 +210,21 @@
 function stepState(state: LiveState, params: SimParams, rng: Rng): void {
   const halfW = state.boxWidth / 2;
   const halfH = state.boxHeight / 2;
-  const driftX = params.electricStrength * clamp(state.vProxy / Math.max(1, params.voltageScale), -4, 4);
   for (let i = 0; i < state.numParticles; i += 1) {
     const xPrev = state.x[i];
     const yPrev = state.y[i];
-    const xTrial = reflectIntoBounds(xPrev + (rng.normal(0, params.diffusionSd) + driftX) * state.dt, -halfW, halfW);
+    const dx = state.negX - xPrev;
+    const driftX = wallFieldDrift(dx, params.electricStrength, 1);
+    const xTrial = reflectIntoBounds(xPrev + (rng.normal(0, params.diffusionSd) + driftX) * state.dt, state.negX, halfW);
     const yNew = reflectIntoBounds(yPrev + rng.normal(0, params.diffusionSd) * state.dt, -halfH, halfH);
     let xNew = xTrial;
     const tryingToCrossLeft = xPrev > state.rightWall && xTrial <= state.rightWall;
     const tryingToCrossRight = xPrev < state.leftWall && xTrial >= state.leftWall;
     const inChannel = yNew >= state.channelYMin && yNew <= state.channelYMax;
     if ((tryingToCrossLeft || tryingToCrossRight) && !inChannel) {
-      xNew = tryingToCrossLeft ? reflectIntoBounds(2 * state.rightWall - xTrial, -halfW, halfW) : reflectIntoBounds(2 * state.leftWall - xTrial, -halfW, halfW);
+      xNew = tryingToCrossLeft
+        ? reflectIntoBounds(2 * state.rightWall - xTrial, state.negX, halfW)
+        : reflectIntoBounds(2 * state.leftWall - xTrial, state.negX, halfW);
     } else if (!inChannel && xTrial > state.leftWall && xTrial < state.rightWall) {
       xNew = xPrev < 0 ? state.leftWall : state.rightWall;
     }
@@ -356,8 +367,8 @@
     dpr,
     channels: [{ top: cTop, bottom: cBottom }]
   });
-  const [chargeWallX] = toPx((state.leftWall + state.rightWall) / 2, 0);
-  ctx.strokeStyle = fieldColor(state.vProxy);
+  const [chargeWallX] = toPx(state.negX, 0);
+  ctx.strokeStyle = fieldColor(simParams.electricStrength);
   ctx.lineWidth = 1.5 * dpr;
   ctx.setLineDash([5 * dpr, 4 * dpr]);
   ctx.beginPath();
@@ -384,7 +395,13 @@
       <h1>Nernst Equilibrium for One Ion</h1>
       <ul class="key-points">
         <li>Membrane potential is determined by relative ion concentration inside vs outside the cell.</li>
-        <li>At equilibrium (no net flux), this is given by the Nernst equation: V = S ln(C_out / C_in).</li>
+        <li>
+          At equilibrium (no net flux), this simulation uses
+          <math display="inline" aria-label="V equals S times natural log of C out over C in">
+            <mi>V</mi><mo>=</mo><mi>S</mi><mo>ln</mo><mo>(</mo><mfrac><msub><mi>C</mi><mtext>out</mtext></msub><msub><mi>C</mi><mtext>in</mtext></msub></mfrac><mo>)</mo>
+          </math>.
+        </li>
+        <li>Inside is the left compartment and outside is the right compartment; the charge wall is at the far-left inside boundary.</li>
       </ul>
     </header>
     <div class="sim-layout">
@@ -397,7 +414,7 @@
             <div class="control-grid">
               <div class="field"><label for="num-particles">Particles</label><input id="num-particles" type="number" min="10" max="5000" step="1" /></div>
               <div class="field"><label for="diffusion-sd">Diffusion SD</label><input id="diffusion-sd" type="number" min="0" max="20" step="0.01" /></div>
-              <div class="field"><label for="electric-strength">Electric strength (charge wall)</label><input id="electric-strength" type="number" min="-10" max="10" step="0.01" /></div>
+              <div class="field"><label for="electric-strength">Electric strength (inside charge wall)</label><input id="electric-strength" type="number" min="-10" max="10" step="0.01" /></div>
               <div class="field"><label for="channel-width">Channel width</label><input id="channel-width" type="number" min="0.5" step="0.5" /></div>
               <div class="field"><label for="playback-speed">Playback speed</label><input id="playback-speed" type="number" min="0.1" max="8" step="0.1" /></div>
               <div class="field"><label for="seed">Seed</label><input id="seed" type="number" min="0" max="4294967295" step="1" /></div>
@@ -436,8 +453,13 @@
               <p>
                 Purple is the simulated membrane potential from compartment concentrations.
                 White is the Nernst prediction for a single permeant cation:
-                <code>V = S ln(C_out / C_in) = S ln(C_right / C_left)</code>.
               </p>
+              <math display="block" aria-label="V equals S times natural log of C out over C in equals S times natural log of C right over C left">
+                <mi>V</mi><mo>=</mo><mi>S</mi><mo>ln</mo><mo>(</mo><mfrac><msub><mi>C</mi><mtext>out</mtext></msub><msub><mi>C</mi><mtext>in</mtext></msub></mfrac><mo>)</mo>
+                <mo>=</mo>
+                <mi>S</mi><mo>ln</mo><mo>(</mo><mfrac><msub><mi>C</mi><mtext>right</mtext></msub><msub><mi>C</mi><mtext>left</mtext></msub></mfrac><mo>)</mo>
+              </math>
+              <p>In this layout, inside is left and outside is right.</p>
             </div>
           </div>
         </div>
@@ -449,7 +471,7 @@
         </div>
         <div class="legend">
           <span><span class="swatch" style="background:${SIM_COLORS.ionA}"></span>Positive ion</span>
-          <span><span class="swatch" style="background:${SIM_COLORS.fieldNegative}"></span>Charge wall</span>
+          <span><span class="swatch" style="background:${SIM_COLORS.fieldNegative}"></span>Inside charge wall (far left)</span>
           <span><span class="swatch" style="background:${SIM_COLORS.totalTrace}"></span>Simulated membrane potential trace</span>
           <span><span class="swatch" style="background:${SIM_COLORS.predictionTrace}"></span>Nernst prediction trace</span>
         </div>
@@ -540,7 +562,8 @@
     electricStrength: clamp(Number(inputs.electricStrength.value) || 0, -10, 10),
     channelWidth: clamp(Number(inputs.channelWidth.value) || defaultSim.channelWidth, 0.5, boxHeight - 2),
     voltageScale: clamp(Number(inputs.voltageScale.value) || defaultSim.voltageScale, 1, 500),
-    nernstScale: clamp(Number(inputs.nernstScale.value) || defaultSim.nernstScale, 1, 500)
+    nernstScale: clamp(Number(inputs.nernstScale.value) || defaultSim.nernstScale, 1, 500),
+    chargeWallInset: simParams.chargeWallInset
   });
 }
 function readDisplayInputs(): DisplayParams {
@@ -554,8 +577,8 @@
 function updateEquationText(): void {
   const stepSd = simParams.diffusionSd * simParams.dt;
   equationBlock.innerHTML = [
-    '<span class="accent">Live update with a membrane charge-wall field term</span>',
-    'drift_x = electric_strength · (V_sim / voltage_scale)',
+    '<span class="accent">Live update with a left inside charge-wall field term</span>',
+    'drift_x = wallFieldDrift(charge_wall_x - x, electric_strength)',
     'x_new = x_old + (dxdt + drift_x) · dt',
     'y_new = reflect(y_old + dydt · dt, -H/2, H/2)',
     'dxdt, dydt ~ Normal(0, diffusionSd²)',
@@ -563,10 +586,10 @@
     '<span class="accent-2">Simulated Vm and Nernst comparison</span>',
     'V_sim = nernst_scale · ln((C_out + 0.5) / (C_in + 0.5))',
     'V_Nernst = nernst_scale · ln((C_out + 0.5) / (C_in + 0.5))',
-    'For a single positive ion, Nernst predicts the charge-wall voltage that balances the concentration gradient',
+    'For a single positive ion, Nernst predicts the balance point for diffusion vs electrical drift',
     '',
     `Per-step Brownian displacement SD = diffusionSd × dt = ${stepSd.toFixed(3)}`,
-    `Electric strength (charge wall) = ${simParams.electricStrength.toFixed(3)}`,
+    `Electric strength (inside charge wall) = ${simParams.electricStrength.toFixed(3)}`,
     `Trace window T = ${simParams.T.toFixed(0)} ms`
   ].join('\n');
 }
@@ -610,7 +633,7 @@
   const halfW = stateRef.boxWidth / 2;
   const halfH = stateRef.boxHeight / 2;
   for (let i = 0; i < stateRef.numParticles; i += 1) {
-    stateRef.x[i] = reflectIntoBounds(stateRef.x[i], -halfW, halfW);
+    stateRef.x[i] = reflectIntoBounds(stateRef.x[i], stateRef.negX, halfW);
     stateRef.y[i] = reflectIntoBounds(stateRef.y[i], -halfH, halfH);
     if (stateRef.x[i] > stateRef.leftWall && stateRef.x[i] < stateRef.rightWall && !(stateRef.y[i] >= stateRef.channelYMin && stateRef.y[i] <= stateRef.channelYMax)) {
       stateRef.x[i] = stateRef.x[i] < 0 ? stateRef.leftWall : stateRef.rightWall;
@@ -651,6 +674,7 @@
   state.boxHeight = simParams.boxHeight;
   state.leftWall = -simParams.wallThickness / 2;
   state.rightWall = simParams.wallThickness / 2;
+  state.negX = leftEdgeChargeWallX(simParams.boxWidth, simParams.chargeWallInset);
   [state.channelYMin, state.channelYMax] = centeredChannelBounds(simParams.channelWidth, simParams.boxHeight);
   enforceGeometry(state);
   trimTrace(trace, state.simTime, simParams.T);

tokens used
78,293
Implemented only the **“Nernst Equilibrium for One Ion”** requests in [src/pages/inspect_resting_potential_nernst.ts](/Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_web/src/pages/inspect_resting_potential_nernst.ts).

Changes made:
1. Replaced plain-text Nernst equation display with rendered math markup (`<math>…</math>`) in the key points and equation card.
2. Removed the middle charge-wall behavior/visual and switched to a far-left inside charge wall:
   - Added `negX` (left-edge charge wall position) to state.
   - Updated drift to use `wallFieldDrift(charge_wall_x - x, electric_strength)`.
   - Constrained x-motion to reflect from that left inside wall.
   - Drew the dashed charge wall at the far-left inside boundary instead of membrane center.
3. Updated copy/labels/legend to match the requested framing:
   - Inside = left, outside = right.
   - Electric control label and legend now refer to the inside far-left charge wall.

Validation run:
1. `npm run build` completed successfully (`tsc && vite build`).
