OpenAI Codex v0.111.0 (research preview)
--------
workdir: /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_web
model: gpt-5.3-codex
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR, /Users/mq20185996/.codex/memories]
reasoning effort: medium
reasoning summaries: none
session id: 019ccbfb-1410-7f11-93de-1665de1b4ed2
--------
user
Process only this section from change requests and make code changes in the current repository.
Source file: change_requests.md
Section title: Free Diffusion with Electrical Attraction

Requirements:
- Implement all bullet requests in this section only.
- Do not modify requests from other sections.
- Run relevant checks/build before finishing if possible.
- Provide a concise final summary of files changed and why.

Section content:
# Free Diffusion with Electrical Attraction

- Add UI controls to adjust the magnitude and direction of each electric field generater.

mcp startup: no servers
codex
I’ll locate the “Free Diffusion with Electrical Attraction” request in `change_requests.md`, identify the related UI/components, then implement only the electric field generator magnitude/direction controls and run checks.
exec
/bin/zsh -lc 'rg --files' in /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_webexec
/bin/zsh -lc 'rg -n "Free Diffusion with Electrical Attraction|electric field|generator|generater|diffusion|attraction" change_requests.md src .' in /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_webexec
/bin/zsh -lc 'ls -la' in /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_web succeeded in 51ms:
src/pages/resting_two_ion_common.ts
src/pages/inspect_nernst_one_ion.ts
src/pages/sim_shared.ts
src/pages/inspect_ap_threshold_all_or_none.ts
src/pages/inspect_resting_potential_charge_wall.ts
src/pages/action_potential_common.ts
src/pages/inspect_field_attraction.ts
src/pages/inspect_diffusion_selective_permeability_rate.ts
src/pages/inspect_diffusion_permeability.ts
src/pages/inspect_two_ion_selective_channels.ts
src/pages/inspect_diffusion_pore.ts
src/pages/inspect_ap_phases.ts
src/pages/inspect_diffusion_selective.ts
src/pages/inspect_membrane_potential_intro.ts
src/pages/inspect_diffusion_free.ts
src/pages/inspect_ap_subthreshold.ts
src/pages/inspect_nak_pump_gradients.ts
src/lessons.ts
src/index.ts
src/style.css
 succeeded in 51ms:
Total output lines: 1139

change_requests.md:20:# Free Diffusion with Electrical Attraction
change_requests.md:22:- Add UI controls to adjust the magnitude and direction of each electric field generater.
src/pages/resting_two_ion_common.ts:60:  diffusionSd: number;
src/pages/resting_two_ion_common.ts:270:    const xTrial = reflectIntoBounds(xPrev + (rng.normal(0, params.diffusionSd) + attractX) * state.dt, minX, halfW);
src/pages/resting_two_ion_common.ts:271:    const yNew = reflectIntoBounds(yPrev + (rng.normal(0, params.diffusionSd) + attractY) * state.dt, -halfH, halfH);
src/pages/resting_two_ion_common.ts:476:    diffusionSd: DEFAULT_DIFFUSION_SD,
src/pages/resting_two_ion_common.ts:513:                <div class="field"><label for="diffusion-sd">Diffusion SD</label><input id="diffusion-sd" type="number" min="0" max="20" step="0.05" /></div>
src/pages/resting_two_ion_common.ts:574:    diffusionSd: getEl<HTMLInputElement>('#diffusion-sd'),
src/pages/resting_two_ion_common.ts:626:    setNumberInput(inputs.diffusionSd, simParams.diffusionSd, 3);
src/pages/resting_two_ion_common.ts:659:      diffusionSd: clamp(Number(inputs.diffusionSd.value) || 0, 0, 20),
src/pages/resting_two_ion_common.ts:770:    const stepSd = simParams.diffusionSd * simParams.dt;
src/pages/resting_two_ion_common.ts:806:      'dxdt, dydt ~ Normal(0, diffusionSd²)',
src/pages/resting_two_ion_common.ts:815:      `Per-step Brownian displacement SD = diffusionSd × dt = ${stepSd.toFixed(3)}`,
src/pages/resting_two_ion_common.ts:828:    statusEls.stepSd.textContent = (simParams.diffusionSd * simParams.dt).toFixed(3);
src/pages/resting_two_ion_common.ts:897:    inputs.numParticles, inputs.type0Fraction, inputs.diffusionSd, inputs.electricStrength, inputs.type0Permeability, inputs.type1Permeability, inputs.type0Charge, inputs.type1Charge, inputs.seed, inputs.totalTime, inputs.dt, inputs.boxWidth, inputs.boxHeight, inputs.wallThickness, inputs.negX, inputs.negY, inputs.initialLeftFrac, ...(inputs.goldmanScale ? [inputs.goldmanScale] : [])
./src/pages/resting_two_ion_common.ts:60:  diffusionSd: number;
./src/pages/resting_two_ion_common.ts:270:    const xTrial = reflectIntoBounds(xPrev + (rng.normal(0, params.diffusionSd) + attractX) * state.dt, minX, halfW);
./src/pages/resting_two_ion_common.ts:271:    const yNew = reflectIntoBounds(yPrev + (rng.normal(0, params.diffusionSd) + attractY) * state.dt, -halfH, halfH);
 succeeded in 52ms:
total 240
drwxr-xr-x@ 31 mq20185996  staff    992 Mar  8 16:45 .
drwxr-xr-x@ 16 mq20185996  staff    512 Mar  6 21:53 ..
drwxr-xr-x@  3 mq20185996  staff     96 Mar  8 12:18 .github
-rw-r--r--@  1 mq20185996  staff     37 Feb 20 19:46 .gitignore
-rw-r--r--@  1 mq20185996  staff   1667 Mar  8 12:19 README.md
-rw-r--r--@  1 mq20185996  staff    614 Mar  8 16:45 change_requests.md
drwxr-xr-x@ 18 mq20185996  staff    576 Mar  8 16:43 dist
drwxr-xr-x@  3 mq20185996  staff     96 Mar  6 21:52 docs
-rw-r--r--@  1 mq20185996  staff    327 Feb 24 00:31 index.html
-rw-r--r--@  1 mq20185996  staff    344 Mar  8 11:53 inspect_ap_phases.html
-rw-r--r--@  1 mq20185996  staff    349 Mar  8 11:53 inspect_ap_subthreshold.html
-rw-r--r--@  1 mq20185996  staff    361 Mar  8 11:53 inspect_ap_threshold_all_or_none.html
-rw-r--r--@  1 mq20185996  staff    325 Mar  8 11:53 inspect_diffusion_free.html
-rw-r--r--@  1 mq20185996  staff    346 Mar  8 11:53 inspect_diffusion_permeability.html
-rw-r--r--@  1 mq20185996  staff    342 Mar  8 11:53 inspect_diffusion_pore.html
-rw-r--r--@  1 mq20185996  staff    336 Mar  8 11:53 inspect_diffusion_selective.html
-rw-r--r--@  1 mq20185996  staff    374 Mar  8 16:05 inspect_diffusion_selective_permeability_rate.html
-rw-r--r--@  1 mq20185996  staff    352 Mar  8 11:53 inspect_field_attraction.html
-rw-r--r--@  1 mq20185996  staff    348 Mar  8 11:53 inspect_membrane_potential_intro.html
codex
I found the target page at `src/pages/inspect_field_attraction.ts`; next I’ll inspect its current electric field model and add per-generator magnitude/direction controls without touching other sections.
exec
/bin/zsh -lc "sed -n '260,620p' src/pages/inspect_field_attraction.ts" in /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_webexec
/bin/zsh -lc "sed -n '1,120p' change_requests.md" in /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_webexec
/bin/zsh -lc "sed -n '1,260p' src/pages/inspect_field_attraction.ts" in /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_web succeeded in 51ms:
# Narrow vs Wide Pore Diffusion

- change such that the trace axes are square and the same
  size and aligned with their corresponding particle
  simulation 

# What Is a Membrane Potential?

- Change defaults so that there are 1000 ions of each type
  on each side.

# Nernst Equilibrium for One Ion

- Make default Electric field strength UI setting -0.05

# Selective Permeability (Probability-Controlled)

- Change title to: Selective Permeability by Probabilistic Gating

# Free Diffusion with Electrical Attraction
 succeeded in 51ms:
    ctx.beginPath();
    ctx.moveTo(0, y);
    ctx.lineTo(width, y);
    ctx.stroke();
  }

  ctx.strokeStyle = 'rgba(180, 220, 255, 0.28)';
  ctx.lineWidth = 1.25;
  ctx.beginPath();
  ctx.moveTo(halfW, 0);
  ctx.lineTo(halfW, height);
  ctx.moveTo(0, halfH);
  ctx.lineTo(width, halfH);
  ctx.stroke();

  ctx.strokeStyle = 'rgba(214, 236, 255, 0.42)';
  ctx.lineWidth = 1.25 * dpr;
  ctx.strokeRect(0.75 * dpr, 0.75 * dpr, width - 1.5 * dpr, height - 1.5 * dpr);

  const radius = Math.max(0.8, display.pointSize) * dpr;
 succeeded in 52ms:
import '../style.css';
import {
  DEFAULT_DIFFUSION_SD,
  DEFAULT_NUM_PARTICLES,
  MAX_PARTICLES,
  SIM_COLORS,
  fieldColor,
  pointFieldDrift
} from './sim_shared';

interface SimParams {
  T: number;
  dt: number;
  numParticles: number;
  diffusionSd: number;
  initClusterSd: number;
  electricStrength: number;
  negX: number;
  negY: number;
}
codex
I confirmed this page uses four field generators (one movable plus three fixed) but only a single shared `electricStrength`. I’m now updating `inspect_field_attraction` to give each generator its own magnitude and direction controls, wire them into drift computation, and keep defaults matching current behavior.
file update
M /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_web/src/pages/inspect_field_attraction.ts
@@ -16,3 +16,10 @@
   initClusterSd: number;
-  electricStrength: number;
+  source0Magnitude: number;
+  source0Direction: number;
+  source1Magnitude: number;
+  source1Direction: number;
+  source2Magnitude: number;
+  source2Direction: number;
+  source3Magnitude: number;
+  source3Direction: number;
   negX: number;
@@ -42,2 +49,3 @@
   y: number;
+  strength: number;
 }
@@ -85,3 +93,10 @@
   initClusterSd: 0.1,
-  electricStrength: -0.15,
+  source0Magnitude: 0.15,
+  source0Direction: -1,
+  source1Magnitude: 0.15,
+  source1Direction: -1,
+  source2Magnitude: 0.15,
+  source2Direction: -1,
+  source3Magnitude: 0.15,
+  source3Direction: -1,
   negX: -20,
@@ -180,3 +195,8 @@
 function getFieldSources(state: LiveState): FieldSource[] {
-  return [{ x: state.negX, y: state.negY }, ...EXTRA_FIELD_SOURCES];
+  return [
+    { x: state.negX, y: state.negY, strength: simParams.source0Magnitude * simParams.source0Direction },
+    { x: EXTRA_FIELD_SOURCES[0].x, y: EXTRA_FIELD_SOURCES[0].y, strength: simParams.source1Magnitude * simParams.source1Direction },
+    { x: EXTRA_FIELD_SOURCES[1].x, y: EXTRA_FIELD_SOURCES[1].y, strength: simParams.source2Magnitude * simParams.source2Direction },
+    { x: EXTRA_FIELD_SOURCES[2].x, y: EXTRA_FIELD_SOURCES[2].y, strength: simParams.source3Magnitude * simParams.source3Direction }
+  ];
 }
@@ -184,5 +204,4 @@
 function stepState(state: LiveState, params: SimParams, display: DisplayParams, rng: Rng): void {
-  const electricStrength = clamp(params.electricStrength, -10, 10);
   const chamberHalfExtent = Math.max(1, display.axisLimit);
-  const fieldSources = getFieldSources(state);
+  const fieldSources = getFieldSources(state).map((source) => ({ ...source, strength: clamp(source.strength, -10, 10) }));
   for (let i = 0; i < state.numParticles; i += 1) {
@@ -193,3 +212,3 @@
       const dy = source.y - state.y[i];
-      const [sourceDriftX, sourceDriftY] = pointFieldDrift(dx, dy, electricStrength, 1);
+      const [sourceDriftX, sourceDriftY] = pointFieldDrift(dx, dy, source.strength, 1);
       driftX += sourceDriftX;
@@ -231,3 +250,3 @@
 
-function drawFrame(canvas: HTMLCanvasElement, state: LiveState, display: DisplayParams, electricStrength: number): void {
+function drawFrame(canvas: HTMLCanvasElement, state: LiveState, display: DisplayParams): void {
   syncCanvasSize(canvas);
@@ -289,4 +308,4 @@
 
-  ctx.fillStyle = fieldColor(electricStrength);
   for (const source of getFieldSources(state)) {
+    ctx.fillStyle = fieldColor(source.strength);
     ctx.beginPath();
@@ -336,3 +355,10 @@
               <div class="field"><label for="diffusion-sd">Diffusion SD</label><input id="diffusion-sd" type="number" min="0" max="20" step="0.05" /></div>
-              <div class="field"><label for="electric-strength">Electric strength</label><input id="electric-strength" type="number" min="-10" max="10" step="0.01" /></div>
+              <div class="field"><label for="source0-magnitude">Generator 1 magnitude</label><input id="source0-magnitude" type="number" min="0" max="10" step="0.01" /></div>
+              <div class="field"><label for="source0-direction">Generator 1 direction</label><select id="source0-direction"><option value="-1">Attract</option><option value="1">Repel</option></select></div>
+              <div class="field"><label for="source1-magnitude">Generator 2 magnitude</label><input id="source1-magnitude" type="number" min="0" max="10" step="0.01" /></div>
+              <div class="field"><label for="source1-direction">Generator 2 direction</label><select id="source1-direction"><option value="-1">Attract</option><option value="1">Repel</option></select></div>
+              <div class="field"><label for="source2-magnitude">Generator 3 magnitude</label><input id="source2-magnitude" type="number" min="0" max="10" step="0.01" /></div>
+              <div class="field"><label for="source2-direction">Generator 3 direction</label><select id="source2-direction"><option value="-1">Attract</option><option value="1">Repel</option></select></div>
+              <div class="field"><label for="source3-magnitude">Generator 4 magnitude</label><input id="source3-magnitude" type="number" min="0" max="10" step="0.01" /></div>
+              <div class="field"><label for="source3-direction">Generator 4 direction</label><select id="source3-direction"><option value="-1">Attract</option><option value="1">Repel</option></select></div>
               <div class="field"><label for="playback-speed">Playback speed</label><input id="playback-speed" type="number" min="0.1" max="8" step="0.1" /></div>
@@ -397,3 +423,10 @@
   diffusionSd: getEl<HTMLInputElement>('#diffusion-sd'),
-  electricStrength: getEl<HTMLInputElement>('#electric-strength'),
+  source0Magnitude: getEl<HTMLInputElement>('#source0-magnitude'),
+  source0Direction: getEl<HTMLSelectElement>('#source0-direction'),
+  source1Magnitude: getEl<HTMLInputElement>('#source1-magnitude'),
+  source1Direction: getEl<HTMLSelectElement>('#source1-direction'),
+  source2Magnitude: getEl<HTMLInputElement>('#source2-magnitude'),
+  source2Direction: getEl<HTMLSelectElement>('#source2-direction'),
+  source3Magnitude: getEl<HTMLInputElement>('#source3-magnitude'),
+  source3Direction: getEl<HTMLSelectElement>('#source3-direction'),
   playbackSpeed: getEl<HTMLInputElement>('#playback-speed'),
@@ -441,3 +474,10 @@
   setNumberInput(inputs.diffusionSd, simParams.diffusionSd, 3);
-  setNumberInput(inputs.electricStrength, simParams.electricStrength, 3);
+  setNumberInput(inputs.source0Magnitude, simParams.source0Magnitude, 3);
+  inputs.source0Direction.value = String(simParams.source0Direction);
+  setNumberInput(inputs.source1Magnitude, simParams.source1Magnitude, 3);
+  inputs.source1Direction.value = String(simParams.source1Direction);
+  setNumberInput(inputs.source2Magnitude, simParams.source2Magnitude, 3);
+  inputs.source2Direction.value = String(simParams.source2Direction);
+  setNumberInput(inputs.source3Magnitude, simParams.source3Magnitude, 3);
+  inputs.source3Direction.value = String(simParams.source3Direction);
   setNumberInput(inputs.playbackSpeed, displayParams.playbackSpeed, 2);
@@ -461,3 +501,10 @@
     initClusterSd: clamp(Number(inputs.initClusterSd.value) || 0, 0, 20),
-    electricStrength: clamp(Number(inputs.electricStrength.value) || 0, -10, 10),
+    source0Magnitude: clamp(Number(inputs.source0Magnitude.value) || 0, 0, 10),
+    source0Direction: Number(inputs.source0Direction.value) === 1 ? 1 : -1,
+    source1Magnitude: clamp(Number(inputs.source1Magnitude.value) || 0, 0, 10),
+    source1Direction: Number(inputs.source1Direction.value) === 1 ? 1 : -1,
+    source2Magnitude: clamp(Number(inputs.source2Magnitude.value) || 0, 0, 10),
+    source2Direction: Number(inputs.source2Direction.value) === 1 ? 1 : -1,
+    source3Magnitude: clamp(Number(inputs.source3Magnitude.value) || 0, 0, 10),
+    source3Direction: Number(inputs.source3Direction.value) === 1 ? 1 : -1,
     negX: Number(inputs.negX.value) || defaultSim.negX,
@@ -490,2 +537,8 @@
   const stepSd = sigma * dt;
+  const strengths = [
+    simParams.source0Magnitude * simParams.source0Direction,
+    simParams.source1Magnitude * simParams.source1Direction,
+    simParams.source2Magnitude * simParams.source2Direction,
+    simParams.source3Magnitude * simParams.source3Direction
+  ];
   equationBlock.innerHTML = [
@@ -494,3 +547,3 @@
     'ξ_x, ξ_y ~ Normal(0, σ²),  where σ = diffusionSd',
-    'drift = -electric_strength · (field_source - pos) / (||field_source - pos|| + ε)',
+    'drift = Σ_s [ -electric_strength_s · (field_source_s - pos) / (||field_source_s - pos|| + ε) ]',
     '',
@@ -506,4 +559,4 @@
     '',
-    'Negative electric_strength attracts positive charge; positive electric_strength repels it.',
-    `Current: diffusionSd = ${sigma.toFixed(3)}, dt = ${dt.toFixed(3)}, electric strength = ${simParams.electricStrength.toFixed(3)}`,
+    'Negative electric_strength_s attracts positive charge; positive electric_strength_s repels it.',
+    `Current: diffusionSd = ${sigma.toFixed(3)}, dt = ${dt.toFixed(3)}, strengths = [${strengths.map((v) => v.toFixed(3)).join(', ')}]`,
     `Per-step displacement SD = diffusionSd × dt = ${stepSd.toFixed(3)}`
@@ -552,3 +605,3 @@
   updateStatus();
-  drawFrame(canvas, state, displayParams, simParams.electricStrength);
+  drawFrame(canvas, state, displayParams);
 }
@@ -586,3 +639,3 @@
 
-for (const key of ['numParticles', 'diffusionSd', 'electricStrength', 'seed', 'totalTime', 'dt', 'initClusterSd', 'negX', 'negY'] as const) {
+for (const key of ['numParticles', 'diffusionSd', 'source0Magnitude', 'source0Direction', 'source1Magnitude', 'source1Direction', 'source2Magnitude', 'source2Direction', 'source3Magnitude', 'source3Direction', 'seed', 'totalTime', 'dt', 'initClusterSd', 'negX', 'negY'] as const) {
   inputs[key].addEventListener('change', () => {
apply_patch(auto_approved=true) exited 0 in 99ms:
Success. Updated the following files:
M src/pages/inspect_field_attraction.ts
file update:
diff --git a/code/electrochemical_signals_progression_web/src/pages/inspect_field_attraction.ts b/code/electrochemical_signals_progression_web/src/pages/inspect_field_attraction.ts
index cbde19b0e3f80bd8bbb883115fd91de7b6996acf..98b3615af842851517165f16cb05bc4006abe08a
--- a/code/electrochemical_signals_progression_web/src/pages/inspect_field_attraction.ts
+++ b/code/electrochemical_signals_progression_web/src/pages/inspect_field_attraction.ts
@@ -14,7 +14,14 @@
   numParticles: number;
   diffusionSd: number;
   initClusterSd: number;
-  electricStrength: number;
+  source0Magnitude: number;
+  source0Direction: number;
+  source1Magnitude: number;
+  source1Direction: number;
+  source2Magnitude: number;
+  source2Direction: number;
+  source3Magnitude: number;
+  source3Direction: number;
   negX: number;
   negY: number;
 }
@@ -40,6 +47,7 @@
 interface FieldSource {
   x: number;
   y: number;
+  strength: number;
 }
 
 class Rng {
@@ -83,7 +91,14 @@
   numParticles: DEFAULT_NUM_PARTICLES,
   diffusionSd: DEFAULT_DIFFUSION_SD,
   initClusterSd: 0.1,
-  electricStrength: -0.15,
+  source0Magnitude: 0.15,
+  source0Direction: -1,
+  source1Magnitude: 0.15,
+  source1Direction: -1,
+  source2Magnitude: 0.15,
+  source2Direction: -1,
+  source3Magnitude: 0.15,
+  source3Direction: -1,
   negX: -20,
   negY: 0
 };
@@ -178,20 +193,24 @@
 }
 
 function getFieldSources(state: LiveState): FieldSource[] {
-  return [{ x: state.negX, y: state.negY }, ...EXTRA_FIELD_SOURCES];
+  return [
+    { x: state.negX, y: state.negY, strength: simParams.source0Magnitude * simParams.source0Direction },
+    { x: EXTRA_FIELD_SOURCES[0].x, y: EXTRA_FIELD_SOURCES[0].y, strength: simParams.source1Magnitude * simParams.source1Direction },
+    { x: EXTRA_FIELD_SOURCES[1].x, y: EXTRA_FIELD_SOURCES[1].y, strength: simParams.source2Magnitude * simParams.source2Direction },
+    { x: EXTRA_FIELD_SOURCES[2].x, y: EXTRA_FIELD_SOURCES[2].y, strength: simParams.source3Magnitude * simParams.source3Direction }
+  ];
 }
 
 function stepState(state: LiveState, params: SimParams, display: DisplayParams, rng: Rng): void {
-  const electricStrength = clamp(params.electricStrength, -10, 10);
   const chamberHalfExtent = Math.max(1, display.axisLimit);
-  const fieldSources = getFieldSources(state);
+  const fieldSources = getFieldSources(state).map((source) => ({ ...source, strength: clamp(source.strength, -10, 10) }));
   for (let i = 0; i < state.numParticles; i += 1) {
     let driftX = 0;
     let driftY = 0;
     for (const source of fieldSources) {
       const dx = source.x - state.x[i];
       const dy = source.y - state.y[i];
-      const [sourceDriftX, sourceDriftY] = pointFieldDrift(dx, dy, electricStrength, 1);
+      const [sourceDriftX, sourceDriftY] = pointFieldDrift(dx, dy, source.strength, 1);
       driftX += sourceDriftX;
       driftY += sourceDriftY;
     }
@@ -229,7 +248,7 @@
   }
 }
 
-function drawFrame(canvas: HTMLCanvasElement, state: LiveState, display: DisplayParams, electricStrength: number): void {
+function drawFrame(canvas: HTMLCanvasElement, state: LiveState, display: DisplayParams): void {
   syncCanvasSize(canvas);
   const ctx = canvas.getContext('2d');
   if (!ctx) return;
@@ -287,8 +306,8 @@
     ctx.fill();
   }
 
-  ctx.fillStyle = fieldColor(electricStrength);
   for (const source of getFieldSources(state)) {
+    ctx.fillStyle = fieldColor(source.strength);
     ctx.beginPath();
     ctx.arc(halfW + source.x * sx, halfH - source.y * sy, 4 * dpr, 0, Math.PI * 2);
     ctx.fill();
@@ -334,7 +353,14 @@
             <div class="control-grid">
               <div class="field"><label for="num-particles">Particles</label><input id="num-particles" type="number" min="1" max="5000" step="1" /></div>
               <div class="field"><label for="diffusion-sd">Diffusion SD</label><input id="diffusion-sd" type="number" min="0" max="20" step="0.05" /></div>
-              <div class="field"><label for="electric-strength">Electric strength</label><input id="electric-strength" type="number" min="-10" max="10" step="0.01" /></div>
+              <div class="field"><label for="source0-magnitude">Generator 1 magnitude</label><input id="source0-magnitude" type="number" min="0" max="10" step="0.01" /></div>
+              <div class="field"><label for="source0-direction">Generator 1 direction</label><select id="source0-direction"><option value="-1">Attract</option><option value="1">Repel</option></select></div>
+              <div class="field"><label for="source1-magnitude">Generator 2 magnitude</label><input id="source1-magnitude" type="number" min="0" max="10" step="0.01" /></div>
+              <div class="field"><label for="source1-direction">Generator 2 direction</label><select id="source1-direction"><option value="-1">Attract</option><option value="1">Repel</option></select></div>
+              <div class="field"><label for="source2-magnitude">Generator 3 magnitude</label><input id="source2-magnitude" type="number" min="0" max="10" step="0.01" /></div>
+              <div class="field"><label for="source2-direction">Generator 3 direction</label><select id="source2-direction"><option value="-1">Attract</option><option value="1">Repel</option></select></div>
+              <div class="field"><label for="source3-magnitude">Generator 4 magnitude</label><input id="source3-magnitude" type="number" min="0" max="10" step="0.01" /></div>
+              <div class="field"><label for="source3-direction">Generator 4 direction</label><select id="source3-direction"><option value="-1">Attract</option><option value="1">Repel</option></select></div>
               <div class="field"><label for="playback-speed">Playback speed</label><input id="playback-speed" type="number" min="0.1" max="8" step="0.1" /></div>
               <div class="field"><label for="seed">Seed</label><input id="seed" type="number" min="0" max="4294967295" step="1" /></div>
             </div>
@@ -395,7 +421,14 @@
 const inputs = {
   numParticles: getEl<HTMLInputElement>('#num-particles'),
   diffusionSd: getEl<HTMLInputElement>('#diffusion-sd'),
-  electricStrength: getEl<HTMLInputElement>('#electric-strength'),
+  source0Magnitude: getEl<HTMLInputElement>('#source0-magnitude'),
+  source0Direction: getEl<HTMLSelectElement>('#source0-direction'),
+  source1Magnitude: getEl<HTMLInputElement>('#source1-magnitude'),
+  source1Direction: getEl<HTMLSelectElement>('#source1-direction'),
+  source2Magnitude: getEl<HTMLInputElement>('#source2-magnitude'),
+  source2Direction: getEl<HTMLSelectElement>('#source2-direction'),
+  source3Magnitude: getEl<HTMLInputElement>('#source3-magnitude'),
+  source3Direction: getEl<HTMLSelectElement>('#source3-direction'),
   playbackSpeed: getEl<HTMLInputElement>('#playback-speed'),
   seed: getEl<HTMLInputElement>('#seed'),
   totalTime: getEl<HTMLInputElement>('#total-time'),
@@ -439,7 +472,14 @@
 function writeInputs(): void {
   setNumberInput(inputs.numParticles, simParams.numParticles, 0);
   setNumberInput(inputs.diffusionSd, simParams.diffusionSd, 3);
-  setNumberInput(inputs.electricStrength, simParams.electricStrength, 3);
+  setNumberInput(inputs.source0Magnitude, simParams.source0Magnitude, 3);
+  inputs.source0Direction.value = String(simParams.source0Direction);
+  setNumberInput(inputs.source1Magnitude, simParams.source1Magnitude, 3);
+  inputs.source1Direction.value = String(simParams.source1Direction);
+  setNumberInput(inputs.source2Magnitude, simParams.source2Magnitude, 3);
+  inputs.source2Direction.value = String(simParams.source2Direction);
+  setNumberInput(inputs.source3Magnitude, simParams.source3Magnitude, 3);
+  inputs.source3Direction.value = String(simParams.source3Direction);
   setNumberInput(inputs.playbackSpeed, displayParams.playbackSpeed, 2);
   setNumberInput(inputs.seed, currentSeed, 0);
   setNumberInput(inputs.totalTime, simParams.T, 0);
@@ -459,7 +499,14 @@
     numParticles: clamp(Math.round(Number(inputs.numParticles.value) || defaultSim.numParticles), 1, MAX_PARTICLES),
     diffusionSd: clamp(Number(inputs.diffusionSd.value) || 0, 0, 20),
     initClusterSd: clamp(Number(inputs.initClusterSd.value) || 0, 0, 20),
-    electricStrength: clamp(Number(inputs.electricStrength.value) || 0, -10, 10),
+    source0Magnitude: clamp(Number(inputs.source0Magnitude.value) || 0, 0, 10),
+    source0Direction: Number(inputs.source0Direction.value) === 1 ? 1 : -1,
+    source1Magnitude: clamp(Number(inputs.source1Magnitude.value) || 0, 0, 10),
+    source1Direction: Number(inputs.source1Direction.value) === 1 ? 1 : -1,
+    source2Magnitude: clamp(Number(inputs.source2Magnitude.value) || 0, 0, 10),
+    source2Direction: Number(inputs.source2Direction.value) === 1 ? 1 : -1,
+    source3Magnitude: clamp(Number(inputs.source3Magnitude.value) || 0, 0, 10),
+    source3Direction: Number(inputs.source3Direction.value) === 1 ? 1 : -1,
     negX: Number(inputs.negX.value) || defaultSim.negX,
     negY: Number(inputs.negY.value) || defaultSim.negY
   };
@@ -488,11 +535,17 @@
   const sigma = simParams.diffusionSd;
   const dt = simParams.dt;
   const stepSd = sigma * dt;
+  const strengths = [
+    simParams.source0Magnitude * simParams.source0Direction,
+    simParams.source1Magnitude * simParams.source1Direction,
+    simParams.source2Magnitude * simParams.source2Direction,
+    simParams.source3Magnitude * simParams.source3Direction
+  ];
   equationBlock.innerHTML = [
     '<span class="accent">Pedagogical stochastic-rate form</span>',
     'dx/dt = ξ_x(t) + drift_x,   dy/dt = ξ_y(t) + drift_y',
     'ξ_x, ξ_y ~ Normal(0, σ²),  where σ = diffusionSd',
-    'drift = -electric_strength · (field_source - pos) / (||field_source - pos|| + ε)',
+    'drift = Σ_s [ -electric_strength_s · (field_source_s - pos) / (||field_source_s - pos|| + ε) ]',
     '',
     '<span class="accent-2">Euler step used in this simulation</span>',
     'x_trial = x_old + (dxdt + drift_x) · dt',
@@ -504,8 +557,8 @@
     '<span class="accent-2">Reflective chamber rule</span>',
     'Particles bounce off the chamber walls instead of leaving the visible box.',
     '',
-    'Negative electric_strength attracts positive charge; positive electric_strength repels it.',
-    `Current: diffusionSd = ${sigma.toFixed(3)}, dt = ${dt.toFixed(3)}, electric strength = ${simParams.electricStrength.toFixed(3)}`,
+    'Negative electric_strength_s attracts positive charge; positive electric_strength_s repels it.',
+    `Current: diffusionSd = ${sigma.toFixed(3)}, dt = ${dt.toFixed(3)}, strengths = [${strengths.map((v) => v.toFixed(3)).join(', ')}]`,
     `Per-step displacement SD = diffusionSd × dt = ${stepSd.toFixed(3)}`
   ].join('\n');
 }
@@ -550,7 +603,7 @@
 
 function render(): void {
   updateStatus();
-  drawFrame(canvas, state, displayParams, simParams.electricStrength);
+  drawFrame(canvas, state, displayParams);
 }
 
 writeInputs();
@@ -584,7 +637,7 @@
   render();
 });
 
-for (const key of ['numParticles', 'diffusionSd', 'electricStrength', 'seed', 'totalTime', 'dt', 'initClusterSd', 'negX', 'negY'] as const) {
+for (const key of ['numParticles', 'diffusionSd', 'source0Magnitude', 'source0Direction', 'source1Magnitude', 'source1Direction', 'source2Magnitude', 'source2Direction', 'source3Magnitude', 'source3Direction', 'seed', 'totalTime', 'dt', 'initClusterSd', 'negX', 'negY'] as const) {
   inputs[key].addEventListener('change', () => {
     applyLiveSimParams();
     render();

file update:
diff --git a/code/electrochemical_signals_progression_web/src/pages/inspect_field_attraction.ts b/code/electrochemical_signals_progression_web/src/pages/inspect_field_attraction.ts
index cbde19b0e3f80bd8bbb883115fd91de7b6996acf..98b3615af842851517165f16cb05bc4006abe08a
--- a/code/electrochemical_signals_progression_web/src/pages/inspect_field_attraction.ts
+++ b/code/electrochemical_signals_progression_web/src/pages/inspect_field_attraction.ts
@@ -14,7 +14,14 @@
   numParticles: number;
   diffusionSd: number;
   initClusterSd: number;
-  electricStrength: number;
+  source0Magnitude: number;
+  source0Direction: number;
+  source1Magnitude: number;
+  source1Direction: number;
+  source2Magnitude: number;
+  source2Direction: number;
+  source3Magnitude: number;
+  source3Direction: number;
   negX: number;
   negY: number;
 }
@@ -40,6 +47,7 @@
 interface FieldSource {
   x: number;
   y: number;
+  strength: number;
 }
 
 class Rng {
@@ -83,7 +91,14 @@
   numParticles: DEFAULT_NUM_PARTICLES,
   diffusionSd: DEFAULT_DIFFUSION_SD,
   initClusterSd: 0.1,
-  electricStrength: -0.15,
+  source0Magnitude: 0.15,
+  source0Direction: -1,
+  source1Magnitude: 0.15,
+  source1Direction: -1,
+  source2Magnitude: 0.15,
+  source2Direction: -1,
+  source3Magnitude: 0.15,
+  source3Direction: -1,
   negX: -20,
   negY: 0
 };
@@ -178,20 +193,24 @@
 }
 
 function getFieldSources(state: LiveState): FieldSource[] {
-  return [{ x: state.negX, y: state.negY }, ...EXTRA_FIELD_SOURCES];
+  return [
+    { x: state.negX, y: state.negY, strength: simParams.source0Magnitude * simParams.source0Direction },
+    { x: EXTRA_FIELD_SOURCES[0].x, y: EXTRA_FIELD_SOURCES[0].y, strength: simParams.source1Magnitude * simParams.source1Direction },
+    { x: EXTRA_FIELD_SOURCES[1].x, y: EXTRA_FIELD_SOURCES[1].y, strength: simParams.source2Magnitude * simParams.source2Direction },
+    { x: EXTRA_FIELD_SOURCES[2].x, y: EXTRA_FIELD_SOURCES[2].y, strength: simParams.source3Magnitude * simParams.source3Direction }
+  ];
 }
 
 function stepState(state: LiveState, params: SimParams, display: DisplayParams, rng: Rng): void {
-  const electricStrength = clamp(params.electricStrength, -10, 10);
   const chamberHalfExtent = Math.max(1, display.axisLimit);
-  const fieldSources = getFieldSources(state);
+  const fieldSources = getFieldSources(state).map((source) => ({ ...source, strength: clamp(source.strength, -10, 10) }));
   for (let i = 0; i < state.numParticles; i += 1) {
     let driftX = 0;
     let driftY = 0;
     for (const source of fieldSources) {
       const dx = source.x - state.x[i];
       const dy = source.y - state.y[i];
-      const [sourceDriftX, sourceDriftY] = pointFieldDrift(dx, dy, electricStrength, 1);
+      const [sourceDriftX, sourceDriftY] = pointFieldDrift(dx, dy, source.strength, 1);
       driftX += sourceDriftX;
       driftY += sourceDriftY;
     }
@@ -229,7 +248,7 @@
   }
 }
 
-function drawFrame(canvas: HTMLCanvasElement, state: LiveState, display: DisplayParams, electricStrength: number): void {
+function drawFrame(canvas: HTMLCanvasElement, state: LiveState, display: DisplayParams): void {
   syncCanvasSize(canvas);
   const ctx = canvas.getContext('2d');
   if (!ctx) return;
@@ -287,8 +306,8 @@
     ctx.fill();
   }
 
-  ctx.fillStyle = fieldColor(electricStrength);
   for (const source of getFieldSources(state)) {
+    ctx.fillStyle = fieldColor(source.strength);
     ctx.beginPath();
     ctx.arc(halfW + source.x * sx, halfH - source.y * sy, 4 * dpr, 0, Math.PI * 2);
     ctx.fill();
@@ -334,7 +353,14 @@
             <div class="control-grid">
               <div class="field"><label for="num-particles">Particles</label><input id="num-particles" type="number" min="1" max="5000" step="1" /></div>
               <div class="field"><label for="diffusion-sd">Diffusion SD</label><input id="diffusion-sd" type="number" min="0" max="20" step="0.05" /></div>
-              <div class="field"><label for="electric-strength">Electric strength</label><input id="electric-strength" type="number" min="-10" max="10" step="0.01" /></div>
+              <div class="field"><label for="source0-magnitude">Generator 1 magnitude</label><input id="source0-magnitude" type="number" min="0" max="10" step="0.01" /></div>
+              <div class="field"><label for="source0-direction">Generator 1 direction</label><select id="source0-direction"><option value="-1">Attract</option><option value="1">Repel</option></select></div>
+              <div class="field"><label for="source1-magnitude">Generator 2 magnitude</label><input id="source1-magnitude" type="number" min="0" max="10" step="0.01" /></div>
+              <div class="field"><label for="source1-direction">Generator 2 direction</label><select id="source1-direction"><option value="-1">Attract</option><option value="1">Repel</option></select></div>
+              <div class="field"><label for="source2-magnitude">Generator 3 magnitude</label><input id="source2-magnitude" type="number" min="0" max="10" step="0.01" /></div>
+              <div class="field"><label for="source2-direction">Generator 3 direction</label><select id="source2-direction"><option value="-1">Attract</option><option value="1">Repel</option></select></div>
+              <div class="field"><label for="source3-magnitude">Generator 4 magnitude</label><input id="source3-magnitude" type="number" min="0" max="10" step="0.01" /></div>
+              <div class="field"><label for="source3-direction">Generator 4 direction</label><select id="source3-direction"><option value="-1">Attract</option><option value="1">Repel</option></select></div>
               <div class="field"><label for="playback-speed">Playback speed</label><input id="playback-speed" type="number" min="0.1" max="8" step="0.1" /></div>
               <div class="field"><label for="seed">Seed</label><input id="seed" type="number" min="0" max="4294967295" step="1" /></div>
             </div>
@@ -395,7 +421,14 @@
 const inputs = {
   numParticles: getEl<HTMLInputElement>('#num-particles'),
   diffusionSd: getEl<HTMLInputElement>('#diffusion-sd'),
-  electricStrength: getEl<HTMLInputElement>('#electric-strength'),
+  source0Magnitude: getEl<HTMLInputElement>('#source0-magnitude'),
+  source0Direction: getEl<HTMLSelectElement>('#source0-direction'),
+  source1Magnitude: getEl<HTMLInputElement>('#source1-magnitude'),
+  source1Direction: getEl<HTMLSelectElement>('#source1-direction'),
+  source2Magnitude: getEl<HTMLInputElement>('#source2-magnitude'),
+  source2Direction: getEl<HTMLSelectElement>('#source2-direction'),
+  source3Magnitude: getEl<HTMLInputElement>('#source3-magnitude'),
+  source3Direction: getEl<HTMLSelectElement>('#source3-direction'),
   playbackSpeed: getEl<HTMLInputElement>('#playback-speed'),
   seed: getEl<HTMLInputElement>('#seed'),
   totalTime: getEl<HTMLInputElement>('#total-time'),
@@ -439,7 +472,14 @@
 function writeInputs(): void {
   setNumberInput(inputs.numParticles, simParams.numParticles, 0);
   setNumberInput(inputs.diffusionSd, simParams.diffusionSd, 3);
-  setNumberInput(inputs.electricStrength, simParams.electricStrength, 3);
+  setNumberInput(inputs.source0Magnitude, simParams.source0Magnitude, 3);
+  inputs.source0Direction.value = String(simParams.source0Direction);
+  setNumberInput(inputs.source1Magnitude, simParams.source1Magnitude, 3);
+  inputs.source1Direction.value = String(simParams.source1Direction);
+  setNumberInput(inputs.source2Magnitude, simParams.source2Magnitude, 3);
+  inputs.source2Direction.value = String(simParams.source2Direction);
+  setNumberInput(inputs.source3Magnitude, simParams.source3Magnitude, 3);
+  inputs.source3Direction.value = String(simParams.source3Direction);
   setNumberInput(inputs.playbackSpeed, displayParams.playbackSpeed, 2);
   setNumberInput(inputs.seed, currentSeed, 0);
   setNumberInput(inputs.totalTime, simParams.T, 0);
@@ -459,7 +499,14 @@
     numParticles: clamp(Math.round(Number(inputs.numParticles.value) || defaultSim.numParticles), 1, MAX_PARTICLES),
     diffusionSd: clamp(Number(inputs.diffusionSd.value) || 0, 0, 20),
     initClusterSd: clamp(Number(inputs.initClusterSd.value) || 0, 0, 20),
-    electricStrength: clamp(Number(inputs.electricStrength.value) || 0, -10, 10),
+    source0Magnitude: clamp(Number(inputs.source0Magnitude.value) || 0, 0, 10),
+    source0Direction: Number(inputs.source0Direction.value) === 1 ? 1 : -1,
+    source1Magnitude: clamp(Number(inputs.source1Magnitude.value) || 0, 0, 10),
+    source1Direction: Number(inputs.source1Direction.value) === 1 ? 1 : -1,
+    source2Magnitude: clamp(Number(inputs.source2Magnitude.value) || 0, 0, 10),
+    source2Direction: Number(inputs.source2Direction.value) === 1 ? 1 : -1,
+    source3Magnitude: clamp(Number(inputs.source3Magnitude.value) || 0, 0, 10),
+    source3Direction: Number(inputs.source3Direction.value) === 1 ? 1 : -1,
     negX: Number(inputs.negX.value) || defaultSim.negX,
     negY: Number(inputs.negY.value) || defaultSim.negY
   };
@@ -488,11 +535,17 @@
   const sigma = simParams.diffusionSd;
   const dt = simParams.dt;
   const stepSd = sigma * dt;
+  const strengths = [
+    simParams.source0Magnitude * simParams.source0Direction,
+    simParams.source1Magnitude * simParams.source1Direction,
+    simParams.source2Magnitude * simParams.source2Direction,
+    simParams.source3Magnitude * simParams.source3Direction
+  ];
   equationBlock.innerHTML = [
     '<span class="accent">Pedagogical stochastic-rate form</span>',
     'dx/dt = ξ_x(t) + drift_x,   dy/dt = ξ_y(t) + drift_y',
     'ξ_x, ξ_y ~ Normal(0, σ²),  where σ = diffusionSd',
-    'drift = -electric_strength · (field_source - pos) / (||field_source - pos|| + ε)',
+    'drift = Σ_s [ -electric_strength_s · (field_source_s - pos) / (||field_source_s - pos|| + ε) ]',
     '',
     '<span class="accent-2">Euler step used in this simulation</span>',
     'x_trial = x_old + (dxdt + drift_x) · dt',
@@ -504,8 +557,8 @@
     '<span class="accent-2">Reflective chamber rule</span>',
     'Particles bounce off the chamber walls instead of leaving the visible box.',
     '',
-    'Negative electric_strength attracts positive charge; positive electric_strength repels it.',
-    `Current: diffusionSd = ${sigma.toFixed(3)}, dt = ${dt.toFixed(3)}, electric strength = ${simParams.electricStrength.toFixed(3)}`,
+    'Negative electric_strength_s attracts positive charge; positive electric_strength_s repels it.',
+    `Current: diffusionSd = ${sigma.toFixed(3)}, dt = ${dt.toFixed(3)}, strengths = [${strengths.map((v) => v.toFixed(3)).join(', ')}]`,
     `Per-step displacement SD = diffusionSd × dt = ${stepSd.toFixed(3)}`
   ].join('\n');
 }
@@ -550,7 +603,7 @@
 
 function render(): void {
   updateStatus();
-  drawFrame(canvas, state, displayParams, simParams.electricStrength);
+  drawFrame(canvas, state, displayParams);
 }
 
 writeInputs();
@@ -584,7 +637,7 @@
   render();
 });
 
-for (const key of ['numParticles', 'diffusionSd', 'electricStrength', 'seed', 'totalTime', 'dt', 'initClusterSd', 'negX', 'negY'] as const) {
+for (const key of ['numParticles', 'diffusionSd', 'source0Magnitude', 'source0Direction', 'source1Magnitude', 'source1Direction', 'source2Magnitude', 'source2Direction', 'source3Magnitude', 'source3Direction', 'seed', 'totalTime', 'dt', 'initClusterSd', 'negX', 'negY'] as const) {
   inputs[key].addEventListener('change', () => {
     applyLiveSimParams();
     render();

codex
I’ve applied the main UI and simulation wiring; I’m now doing a quick pass to remove any stale references and then run a build check.
exec
/bin/zsh -lc "rg -n \"electricStrength|source[0-3](Magnitude|Direction)|getFieldSources\\(|drawFrame\\(\" src/pages/inspect_field_attraction.ts" in /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_web succeeded in 51ms:
17:  source0Magnitude: number;
18:  source0Direction: number;
19:  source1Magnitude: number;
20:  source1Direction: number;
21:  source2Magnitude: number;
22:  source2Direction: number;
23:  source3Magnitude: number;
24:  source3Direction: number;
94:  source0Magnitude: 0.15,
95:  source0Direction: -1,
96:  source1Magnitude: 0.15,
97:  source1Direction: -1,
98:  source2Magnitude: 0.15,
99:  source2Direction: -1,
100:  source3Magnitude: 0.15,
101:  source3Direction: -1,
195:function getFieldSources(state: LiveState): FieldSource[] {
197:    { x: state.negX, y: state.negY, strength: simParams.source0Magnitude * simParams.source0Direction },
198:    { x: EXTRA_FIELD_SOURCES[0].x, y: EXTRA_FIELD_SOURCES[0].y, strength: simParams.source1Magnitude * simParams.source1Direction },
199:    { x: EXTRA_FIELD_SOURCES[1].x, y: EXTRA_FIELD_SOURCES[1].y, strength: simParams.source2Magnitude * simParams.source2Direction },
exec
/bin/zsh -lc 'npm run build' in /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_web exited 2 in 684ms:

> electrochemical-signals-progression-web@0.1.0 build
> tsc && vite build

src/pages/inspect_field_attraction.ts(114,3): error TS2741: Property 'strength' is missing in type '{ x: number; y: number; }' but required in type 'FieldSource'.
src/pages/inspect_field_attraction.ts(115,3): error TS2741: Property 'strength' is missing in type '{ x: number; y: number; }' but required in type 'FieldSource'.
src/pages/inspect_field_attraction.ts(116,3): error TS2741: Property 'strength' is missing in type '{ x: number; y: number; }' but required in type 'FieldSource'.
file update:
diff --git a/code/electrochemical_signals_progression_web/src/pages/inspect_field_attraction.ts b/code/electrochemical_signals_progression_web/src/pages/inspect_field_attraction.ts
index cbde19b0e3f80bd8bbb883115fd91de7b6996acf..98b3615af842851517165f16cb05bc4006abe08a
--- a/code/electrochemical_signals_progression_web/src/pages/inspect_field_attraction.ts
+++ b/code/electrochemical_signals_progression_web/src/pages/inspect_field_attraction.ts
@@ -14,7 +14,14 @@
   numParticles: number;
   diffusionSd: number;
   initClusterSd: number;
-  electricStrength: number;
+  source0Magnitude: number;
+  source0Direction: number;
+  source1Magnitude: number;
+  source1Direction: number;
+  source2Magnitude: number;
+  source2Direction: number;
+  source3Magnitude: number;
+  source3Direction: number;
   negX: number;
   negY: number;
 }
@@ -40,6 +47,7 @@
 interface FieldSource {
   x: number;
   y: number;
+  strength: number;
 }
 
 class Rng {
@@ -83,7 +91,14 @@
   numParticles: DEFAULT_NUM_PARTICLES,
   diffusionSd: DEFAULT_DIFFUSION_SD,
   initClusterSd: 0.1,
-  electricStrength: -0.15,
+  source0Magnitude: 0.15,
+  source0Direction: -1,
+  source1Magnitude: 0.15,
+  source1Direction: -1,
+  source2Magnitude: 0.15,
+  source2Direction: -1,
+  source3Magnitude: 0.15,
+  source3Direction: -1,
   negX: -20,
   negY: 0
 };
@@ -178,20 +193,24 @@
 }
 
 function getFieldSources(state: LiveState): FieldSource[] {
-  return [{ x: state.negX, y: state.negY }, ...EXTRA_FIELD_SOURCES];
+  return [
+    { x: state.negX, y: state.negY, strength: simParams.source0Magnitude * simParams.source0Direction },
+    { x: EXTRA_FIELD_SOURCES[0].x, y: EXTRA_FIELD_SOURCES[0].y, strength: simParams.source1Magnitude * simParams.source1Direction },
+    { x: EXTRA_FIELD_SOURCES[1].x, y: EXTRA_FIELD_SOURCES[1].y, strength: simParams.source2Magnitude * simParams.source2Direction },
+    { x: EXTRA_FIELD_SOURCES[2].x, y: EXTRA_FIELD_SOURCES[2].y, strength: simParams.source3Magnitude * simParams.source3Direction }
+  ];
 }
 
 function stepState(state: LiveState, params: SimParams, display: DisplayParams, rng: Rng): void {
-  const electricStrength = clamp(params.electricStrength, -10, 10);
   const chamberHalfExtent = Math.max(1, display.axisLimit);
-  const fieldSources = getFieldSources(state);
+  const fieldSources = getFieldSources(state).map((source) => ({ ...source, strength: clamp(source.strength, -10, 10) }));
   for (let i = 0; i < state.numParticles; i += 1) {
     let driftX = 0;
     let driftY = 0;
     for (const source of fieldSources) {
       const dx = source.x - state.x[i];
       const dy = source.y - state.y[i];
-      const [sourceDriftX, sourceDriftY] = pointFieldDrift(dx, dy, electricStrength, 1);
+      const [sourceDriftX, sourceDriftY] = pointFieldDrift(dx, dy, source.strength, 1);
       driftX += sourceDriftX;
       driftY += sourceDriftY;
     }
@@ -229,7 +248,7 @@
   }
 }
 
-function drawFrame(canvas: HTMLCanvasElement, state: LiveState, display: DisplayParams, electricStrength: number): void {
+function drawFrame(canvas: HTMLCanvasElement, state: LiveState, display: DisplayParams): void {
   syncCanvasSize(canvas);
   const ctx = canvas.getContext('2d');
   if (!ctx) return;
@@ -287,8 +306,8 @@
     ctx.fill();
   }
 
-  ctx.fillStyle = fieldColor(electricStrength);
   for (const source of getFieldSources(state)) {
+    ctx.fillStyle = fieldColor(source.strength);
     ctx.beginPath();
     ctx.arc(halfW + source.x * sx, halfH - source.y * sy, 4 * dpr, 0, Math.PI * 2);
     ctx.fill();
@@ -334,7 +353,14 @@
             <div class="control-grid">
               <div class="field"><label for="num-particles">Particles</label><input id="num-particles" type="number" min="1" max="5000" step="1" /></div>
               <div class="field"><label for="diffusion-sd">Diffusion SD</label><input id="diffusion-sd" type="number" min="0" max="20" step="0.05" /></div>
-              <div class="field"><label for="electric-strength">Electric strength</label><input id="electric-strength" type="number" min="-10" max="10" step="0.01" /></div>
+              <div class="field"><label for="source0-magnitude">Generator 1 magnitude</label><input id="source0-magnitude" type="number" min="0" max="10" step="0.01" /></div>
+              <div class="field"><label for="source0-direction">Generator 1 direction</label><select id="source0-direction"><option value="-1">Attract</option><option value="1">Repel</option></select></div>
+              <div class="field"><label for="source1-magnitude">Generator 2 magnitude</label><input id="source1-magnitude" type="number" min="0" max="10" step="0.01" /></div>
+              <div class="field"><label for="source1-direction">Generator 2 direction</label><select id="source1-direction"><option value="-1">Attract</option><option value="1">Repel</option></select></div>
+              <div class="field"><label for="source2-magnitude">Generator 3 magnitude</label><input id="source2-magnitude" type="number" min="0" max="10" step="0.01" /></div>
+              <div class="field"><label for="source2-direction">Generator 3 direction</label><select id="source2-direction"><option value="-1">Attract</option><option value="1">Repel</option></select></div>
+              <div class="field"><label for="source3-magnitude">Generator 4 magnitude</label><input id="source3-magnitude" type="number" min="0" max="10" step="0.01" /></div>
+              <div class="field"><label for="source3-direction">Generator 4 direction</label><select id="source3-direction"><option value="-1">Attract</option><option value="1">Repel</option></select></div>
               <div class="field"><label for="playback-speed">Playback speed</label><input id="playback-speed" type="number" min="0.1" max="8" step="0.1" /></div>
               <div class="field"><label for="seed">Seed</label><input id="seed" type="number" min="0" max="4294967295" step="1" /></div>
             </div>
@@ -395,7 +421,14 @@
 const inputs = {
   numParticles: getEl<HTMLInputElement>('#num-particles'),
   diffusionSd: getEl<HTMLInputElement>('#diffusion-sd'),
-  electricStrength: getEl<HTMLInputElement>('#electric-strength'),
+  source0Magnitude: getEl<HTMLInputElement>('#source0-magnitude'),
+  source0Direction: getEl<HTMLSelectElement>('#source0-direction'),
+  source1Magnitude: getEl<HTMLInputElement>('#source1-magnitude'),
+  source1Direction: getEl<HTMLSelectElement>('#source1-direction'),
+  source2Magnitude: getEl<HTMLInputElement>('#source2-magnitude'),
+  source2Direction: getEl<HTMLSelectElement>('#source2-direction'),
+  source3Magnitude: getEl<HTMLInputElement>('#source3-magnitude'),
+  source3Direction: getEl<HTMLSelectElement>('#source3-direction'),
   playbackSpeed: getEl<HTMLInputElement>('#playback-speed'),
   seed: getEl<HTMLInputElement>('#seed'),
   totalTime: getEl<HTMLInputElement>('#total-time'),
@@ -439,7 +472,14 @@
 function writeInputs(): void {
   setNumberInput(inputs.numParticles, simParams.numParticles, 0);
   setNumberInput(inputs.diffusionSd, simParams.diffusionSd, 3);
-  setNumberInput(inputs.electricStrength, simParams.electricStrength, 3);
+  setNumberInput(inputs.source0Magnitude, simParams.source0Magnitude, 3);
+  inputs.source0Direction.value = String(simParams.source0Direction);
+  setNumberInput(inputs.source1Magnitude, simParams.source1Magnitude, 3);
+  inputs.source1Direction.value = String(simParams.source1Direction);
+  setNumberInput(inputs.source2Magnitude, simParams.source2Magnitude, 3);
+  inputs.source2Direction.value = String(simParams.source2Direction);
+  setNumberInput(inputs.source3Magnitude, simParams.source3Magnitude, 3);
+  inputs.source3Direction.value = String(simParams.source3Direction);
   setNumberInput(inputs.playbackSpeed, displayParams.playbackSpeed, 2);
   setNumberInput(inputs.seed, currentSeed, 0);
   setNumberInput(inputs.totalTime, simParams.T, 0);
@@ -459,7 +499,14 @@
     numParticles: clamp(Math.round(Number(inputs.numParticles.value) || defaultSim.numParticles), 1, MAX_PARTICLES),
     diffusionSd: clamp(Number(inputs.diffusionSd.value) || 0, 0, 20),
     initClusterSd: clamp(Number(inputs.initClusterSd.value) || 0, 0, 20),
-    electricStrength: clamp(Number(inputs.electricStrength.value) || 0, -10, 10),
+    source0Magnitude: clamp(Number(inputs.source0Magnitude.value) || 0, 0, 10),
+    source0Direction: Number(inputs.source0Direction.value) === 1 ? 1 : -1,
+    source1Magnitude: clamp(Number(inputs.source1Magnitude.value) || 0, 0, 10),
+    source1Direction: Number(inputs.source1Direction.value) === 1 ? 1 : -1,
+    source2Magnitude: clamp(Number(inputs.source2Magnitude.value) || 0, 0, 10),
+    source2Direction: Number(inputs.source2Direction.value) === 1 ? 1 : -1,
+    source3Magnitude: clamp(Number(inputs.source3Magnitude.value) || 0, 0, 10),
+    source3Direction: Number(inputs.source3Direction.value) === 1 ? 1 : -1,
     negX: Number(inputs.negX.value) || defaultSim.negX,
     negY: Number(inputs.negY.value) || defaultSim.negY
   };
@@ -488,11 +535,17 @@
   const sigma = simParams.diffusionSd;
   const dt = simParams.dt;
   const stepSd = sigma * dt;
+  const strengths = [
+    simParams.source0Magnitude * simParams.source0Direction,
+    simParams.source1Magnitude * simParams.source1Direction,
+    simParams.source2Magnitude * simParams.source2Direction,
+    simParams.source3Magnitude * simParams.source3Direction
+  ];
   equationBlock.innerHTML = [
     '<span class="accent">Pedagogical stochastic-rate form</span>',
     'dx/dt = ξ_x(t) + drift_x,   dy/dt = ξ_y(t) + drift_y',
     'ξ_x, ξ_y ~ Normal(0, σ²),  where σ = diffusionSd',
-    'drift = -electric_strength · (field_source - pos) / (||field_source - pos|| + ε)',
+    'drift = Σ_s [ -electric_strength_s · (field_source_s - pos) / (||field_source_s - pos|| + ε) ]',
     '',
     '<span class="accent-2">Euler step used in this simulation</span>',
     'x_trial = x_old + (dxdt + drift_x) · dt',
@@ -504,8 +557,8 @@
     '<span class="accent-2">Reflective chamber rule</span>',
     'Particles bounce off the chamber walls instead of leaving the visible box.',
     '',
-    'Negative electric_strength attracts positive charge; positive electric_strength repels it.',
-    `Current: diffusionSd = ${sigma.toFixed(3)}, dt = ${dt.toFixed(3)}, electric strength = ${simParams.electricStrength.toFixed(3)}`,
+    'Negative electric_strength_s attracts positive charge; positive electric_strength_s repels it.',
+    `Current: diffusionSd = ${sigma.toFixed(3)}, dt = ${dt.toFixed(3)}, strengths = [${strengths.map((v) => v.toFixed(3)).join(', ')}]`,
     `Per-step displacement SD = diffusionSd × dt = ${stepSd.toFixed(3)}`
   ].join('\n');
 }
@@ -550,7 +603,7 @@
 
 function render(): void {
   updateStatus();
-  drawFrame(canvas, state, displayParams, simParams.electricStrength);
+  drawFrame(canvas, state, displayParams);
 }
 
 writeInputs();
@@ -584,7 +637,7 @@
   render();
 });
 
-for (const key of ['numParticles', 'diffusionSd', 'electricStrength', 'seed', 'totalTime', 'dt', 'initClusterSd', 'negX', 'negY'] as const) {
+for (const key of ['numParticles', 'diffusionSd', 'source0Magnitude', 'source0Direction', 'source1Magnitude', 'source1Direction', 'source2Magnitude', 'source2Direction', 'source3Magnitude', 'source3Direction', 'seed', 'totalTime', 'dt', 'initClusterSd', 'negX', 'negY'] as const) {
   inputs[key].addEventListener('change', () => {
     applyLiveSimParams();
     render();

codex
Build surfaced a typing issue from the fixed source list after adding per-source strength. I’ll patch the source-position type and rerun the build.
file update
M /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_web/src/pages/inspect_field_attraction.ts
@@ -52,2 +52,7 @@
 
+interface FieldSourcePosition {
+  x: number;
+  y: number;
+}
+
 class Rng {
@@ -112,3 +117,3 @@
 
-const EXTRA_FIELD_SOURCES: readonly FieldSource[] = [
+const EXTRA_FIELD_SOURCES: readonly FieldSourcePosition[] = [
   { x: 20, y: 0 },
apply_patch(auto_approved=true) exited 0 in 71ms:
Success. Updated the following files:
M src/pages/inspect_field_attraction.ts
file update:
diff --git a/code/electrochemical_signals_progression_web/src/pages/inspect_field_attraction.ts b/code/electrochemical_signals_progression_web/src/pages/inspect_field_attraction.ts
index cbde19b0e3f80bd8bbb883115fd91de7b6996acf..1b97766175097ec1c1f6c034633fa1956ef0d5dd
--- a/code/electrochemical_signals_progression_web/src/pages/inspect_field_attraction.ts
+++ b/code/electrochemical_signals_progression_web/src/pages/inspect_field_attraction.ts
@@ -14,7 +14,14 @@
   numParticles: number;
   diffusionSd: number;
   initClusterSd: number;
-  electricStrength: number;
+  source0Magnitude: number;
+  source0Direction: number;
+  source1Magnitude: number;
+  source1Direction: number;
+  source2Magnitude: number;
+  source2Direction: number;
+  source3Magnitude: number;
+  source3Direction: number;
   negX: number;
   negY: number;
 }
@@ -40,6 +47,12 @@
 interface FieldSource {
   x: number;
   y: number;
+  strength: number;
+}
+
+interface FieldSourcePosition {
+  x: number;
+  y: number;
 }
 
 class Rng {
@@ -83,7 +96,14 @@
   numParticles: DEFAULT_NUM_PARTICLES,
   diffusionSd: DEFAULT_DIFFUSION_SD,
   initClusterSd: 0.1,
-  electricStrength: -0.15,
+  source0Magnitude: 0.15,
+  source0Direction: -1,
+  source1Magnitude: 0.15,
+  source1Direction: -1,
+  source2Magnitude: 0.15,
+  source2Direction: -1,
+  source3Magnitude: 0.15,
+  source3Direction: -1,
   negX: -20,
   negY: 0
 };
@@ -95,7 +115,7 @@
   targetFps: 30
 };
 
-const EXTRA_FIELD_SOURCES: readonly FieldSource[] = [
+const EXTRA_FIELD_SOURCES: readonly FieldSourcePosition[] = [
   { x: 20, y: 0 },
   { x: 0, y: 20 },
   { x: 0, y: -20 }
@@ -178,20 +198,24 @@
 }
 
 function getFieldSources(state: LiveState): FieldSource[] {
-  return [{ x: state.negX, y: state.negY }, ...EXTRA_FIELD_SOURCES];
+  return [
+    { x: state.negX, y: state.negY, strength: simParams.source0Magnitude * simParams.source0Direction },
+    { x: EXTRA_FIELD_SOURCES[0].x, y: EXTRA_FIELD_SOURCES[0].y, strength: simParams.source1Magnitude * simParams.source1Direction },
+    { x: EXTRA_FIELD_SOURCES[1].x, y: EXTRA_FIELD_SOURCES[1].y, strength: simParams.source2Magnitude * simParams.source2Direction },
+    { x: EXTRA_FIELD_SOURCES[2].x, y: EXTRA_FIELD_SOURCES[2].y, strength: simParams.source3Magnitude * simParams.source3Direction }
+  ];
 }
 
 function stepState(state: LiveState, params: SimParams, display: DisplayParams, rng: Rng): void {
-  const electricStrength = clamp(params.electricStrength, -10, 10);
   const chamberHalfExtent = Math.max(1, display.axisLimit);
-  const fieldSources = getFieldSources(state);
+  const fieldSources = getFieldSources(state).map((source) => ({ ...source, strength: clamp(source.strength, -10, 10) }));
   for (let i = 0; i < state.numParticles; i += 1) {
     let driftX = 0;
     let driftY = 0;
     for (const source of fieldSources) {
       const dx = source.x - state.x[i];
       const dy = source.y - state.y[i];
-      const [sourceDriftX, sourceDriftY] = pointFieldDrift(dx, dy, electricStrength, 1);
+      const [sourceDriftX, sourceDriftY] = pointFieldDrift(dx, dy, source.strength, 1);
       driftX += sourceDriftX;
       driftY += sourceDriftY;
     }
@@ -229,7 +253,7 @@
   }
 }
 
-function drawFrame(canvas: HTMLCanvasElement, state: LiveState, display: DisplayParams, electricStrength: number): void {
+function drawFrame(canvas: HTMLCanvasElement, state: LiveState, display: DisplayParams): void {
   syncCanvasSize(canvas);
   const ctx = canvas.getContext('2d');
   if (!ctx) return;
@@ -287,8 +311,8 @@
     ctx.fill();
   }
 
-  ctx.fillStyle = fieldColor(electricStrength);
   for (const source of getFieldSources(state)) {
+    ctx.fillStyle = fieldColor(source.strength);
     ctx.beginPath();
     ctx.arc(halfW + source.x * sx, halfH - source.y * sy, 4 * dpr, 0, Math.PI * 2);
     ctx.fill();
@@ -334,7 +358,14 @@
             <div class="control-grid">
               <div class="field"><label for="num-particles">Particles</label><input id="num-particles" type="number" min="1" max="5000" step="1" /></div>
               <div class="field"><label for="diffusion-sd">Diffusion SD</label><input id="diffusion-sd" type="number" min="0" max="20" step="0.05" /></div>
-              <div class="field"><label for="electric-strength">Electric strength</label><input id="electric-strength" type="number" min="-10" max="10" step="0.01" /></div>
+              <div class="field"><label for="source0-magnitude">Generator 1 magnitude</label><input id="source0-magnitude" type="number" min="0" max="10" step="0.01" /></div>
+              <div class="field"><label for="source0-direction">Generator 1 direction</label><select id="source0-direction"><option value="-1">Attract</option><option value="1">Repel</option></select></div>
+              <div class="field"><label for="source1-magnitude">Generator 2 magnitude</label><input id="source1-magnitude" type="number" min="0" max="10" step="0.01" /></div>
+              <div class="field"><label for="source1-direction">Generator 2 direction</label><select id="source1-direction"><option value="-1">Attract</option><option value="1">Repel</option></select></div>
+              <div class="field"><label for="source2-magnitude">Generator 3 magnitude</label><input id="source2-magnitude" type="number" min="0" max="10" step="0.01" /></div>
+              <div class="field"><label for="source2-direction">Generator 3 direction</label><select id="source2-direction"><option value="-1">Attract</option><option value="1">Repel</option></select></div>
+              <div class="field"><label for="source3-magnitude">Generator 4 magnitude</label><input id="source3-magnitude" type="number" min="0" max="10" step="0.01" /></div>
+              <div class="field"><label for="source3-direction">Generator 4 direction</label><select id="source3-direction"><option value="-1">Attract</option><option value="1">Repel</option></select></div>
               <div class="field"><label for="playback-speed">Playback speed</label><input id="playback-speed" type="number" min="0.1" max="8" step="0.1" /></div>
               <div class="field"><label for="seed">Seed</label><input id="seed" type="number" min="0" max="4294967295" step="1" /></div>
             </div>
@@ -395,7 +426,14 @@
 const inputs = {
   numParticles: getEl<HTMLInputElement>('#num-particles'),
   diffusionSd: getEl<HTMLInputElement>('#diffusion-sd'),
-  electricStrength: getEl<HTMLInputElement>('#electric-strength'),
+  source0Magnitude: getEl<HTMLInputElement>('#source0-magnitude'),
+  source0Direction: getEl<HTMLSelectElement>('#source0-direction'),
+  source1Magnitude: getEl<HTMLInputElement>('#source1-magnitude'),
+  source1Direction: getEl<HTMLSelectElement>('#source1-direction'),
+  source2Magnitude: getEl<HTMLInputElement>('#source2-magnitude'),
+  source2Direction: getEl<HTMLSelectElement>('#source2-direction'),
+  source3Magnitude: getEl<HTMLInputElement>('#source3-magnitude'),
+  source3Direction: getEl<HTMLSelectElement>('#source3-direction'),
   playbackSpeed: getEl<HTMLInputElement>('#playback-speed'),
   seed: getEl<HTMLInputElement>('#seed'),
   totalTime: getEl<HTMLInputElement>('#total-time'),
@@ -439,7 +477,14 @@
 function writeInputs(): void {
   setNumberInput(inputs.numParticles, simParams.numParticles, 0);
   setNumberInput(inputs.diffusionSd, simParams.diffusionSd, 3);
-  setNumberInput(inputs.electricStrength, simParams.electricStrength, 3);
+  setNumberInput(inputs.source0Magnitude, simParams.source0Magnitude, 3);
+  inputs.source0Direction.value = String(simParams.source0Direction);
+  setNumberInput(inputs.source1Magnitude, simParams.source1Magnitude, 3);
+  inputs.source1Direction.value = String(simParams.source1Direction);
+  setNumberInput(inputs.source2Magnitude, simParams.source2Magnitude, 3);
+  inputs.source2Direction.value = String(simParams.source2Direction);
+  setNumberInput(inputs.source3Magnitude, simParams.source3Magnitude, 3);
+  inputs.source3Direction.value = String(simParams.source3Direction);
   setNumberInput(inputs.playbackSpeed, displayParams.playbackSpeed, 2);
   setNumberInput(inputs.seed, currentSeed, 0);
   setNumberInput(inputs.totalTime, simParams.T, 0);
@@ -459,7 +504,14 @@
     numParticles: clamp(Math.round(Number(inputs.numParticles.value) || defaultSim.numParticles), 1, MAX_PARTICLES),
     diffusionSd: clamp(Number(inputs.diffusionSd.value) || 0, 0, 20),
     initClusterSd: clamp(Number(inputs.initClusterSd.value) || 0, 0, 20),
-    electricStrength: clamp(Number(inputs.electricStrength.value) || 0, -10, 10),
+    source0Magnitude: clamp(Number(inputs.source0Magnitude.value) || 0, 0, 10),
+    source0Direction: Number(inputs.source0Direction.value) === 1 ? 1 : -1,
+    source1Magnitude: clamp(Number(inputs.source1Magnitude.value) || 0, 0, 10),
+    source1Direction: Number(inputs.source1Direction.value) === 1 ? 1 : -1,
+    source2Magnitude: clamp(Number(inputs.source2Magnitude.value) || 0, 0, 10),
+    source2Direction: Number(inputs.source2Direction.value) === 1 ? 1 : -1,
+    source3Magnitude: clamp(Number(inputs.source3Magnitude.value) || 0, 0, 10),
+    source3Direction: Number(inputs.source3Direction.value) === 1 ? 1 : -1,
     negX: Number(inputs.negX.value) || defaultSim.negX,
     negY: Number(inputs.negY.value) || defaultSim.negY
   };
@@ -488,11 +540,17 @@
   const sigma = simParams.diffusionSd;
   const dt = simParams.dt;
   const stepSd = sigma * dt;
+  const strengths = [
+    simParams.source0Magnitude * simParams.source0Direction,
+    simParams.source1Magnitude * simParams.source1Direction,
+    simParams.source2Magnitude * simParams.source2Direction,
+    simParams.source3Magnitude * simParams.source3Direction
+  ];
   equationBlock.innerHTML = [
     '<span class="accent">Pedagogical stochastic-rate form</span>',
     'dx/dt = ξ_x(t) + drift_x,   dy/dt = ξ_y(t) + drift_y',
     'ξ_x, ξ_y ~ Normal(0, σ²),  where σ = diffusionSd',
-    'drift = -electric_strength · (field_source - pos) / (||field_source - pos|| + ε)',
+    'drift = Σ_s [ -electric_strength_s · (field_source_s - pos) / (||field_source_s - pos|| + ε) ]',
     '',
     '<span class="accent-2">Euler step used in this simulation</span>',
     'x_trial = x_old + (dxdt + drift_x) · dt',
@@ -504,8 +562,8 @@
     '<span class="accent-2">Reflective chamber rule</span>',
     'Particles bounce off the chamber walls instead of leaving the visible box.',
     '',
-    'Negative electric_strength attracts positive charge; positive electric_strength repels it.',
-    `Current: diffusionSd = ${sigma.toFixed(3)}, dt = ${dt.toFixed(3)}, electric strength = ${simParams.electricStrength.toFixed(3)}`,
+    'Negative electric_strength_s attracts positive charge; positive electric_strength_s repels it.',
+    `Current: diffusionSd = ${sigma.toFixed(3)}, dt = ${dt.toFixed(3)}, strengths = [${strengths.map((v) => v.toFixed(3)).join(', ')}]`,
     `Per-step displacement SD = diffusionSd × dt = ${stepSd.toFixed(3)}`
   ].join('\n');
 }
@@ -550,7 +608,7 @@
 
 function render(): void {
   updateStatus();
-  drawFrame(canvas, state, displayParams, simParams.electricStrength);
+  drawFrame(canvas, state, displayParams);
 }
 
 writeInputs();
@@ -584,7 +642,7 @@
   render();
 });
 
-for (const key of ['numParticles', 'diffusionSd', 'electricStrength', 'seed', 'totalTime', 'dt', 'initClusterSd', 'negX', 'negY'] as const) {
+for (const key of ['numParticles', 'diffusionSd', 'source0Magnitude', 'source0Direction', 'source1Magnitude', 'source1Direction', 'source2Magnitude', 'source2Direction', 'source3Magnitude', 'source3Direction', 'seed', 'totalTime', 'dt', 'initClusterSd', 'negX', 'negY'] as const) {
   inputs[key].addEventListener('change', () => {
     applyLiveSimParams();
     render();

file update:
diff --git a/code/electrochemical_signals_progression_web/src/pages/inspect_field_attraction.ts b/code/electrochemical_signals_progression_web/src/pages/inspect_field_attraction.ts
index cbde19b0e3f80bd8bbb883115fd91de7b6996acf..1b97766175097ec1c1f6c034633fa1956ef0d5dd
--- a/code/electrochemical_signals_progression_web/src/pages/inspect_field_attraction.ts
+++ b/code/electrochemical_signals_progression_web/src/pages/inspect_field_attraction.ts
@@ -14,7 +14,14 @@
   numParticles: number;
   diffusionSd: number;
   initClusterSd: number;
-  electricStrength: number;
+  source0Magnitude: number;
+  source0Direction: number;
+  source1Magnitude: number;
+  source1Direction: number;
+  source2Magnitude: number;
+  source2Direction: number;
+  source3Magnitude: number;
+  source3Direction: number;
   negX: number;
   negY: number;
 }
@@ -40,6 +47,12 @@
 interface FieldSource {
   x: number;
   y: number;
+  strength: number;
+}
+
+interface FieldSourcePosition {
+  x: number;
+  y: number;
 }
 
 class Rng {
@@ -83,7 +96,14 @@
   numParticles: DEFAULT_NUM_PARTICLES,
   diffusionSd: DEFAULT_DIFFUSION_SD,
   initClusterSd: 0.1,
-  electricStrength: -0.15,
+  source0Magnitude: 0.15,
+  source0Direction: -1,
+  source1Magnitude: 0.15,
+  source1Direction: -1,
+  source2Magnitude: 0.15,
+  source2Direction: -1,
+  source3Magnitude: 0.15,
+  source3Direction: -1,
   negX: -20,
   negY: 0
 };
@@ -95,7 +115,7 @@
   targetFps: 30
 };
 
-const EXTRA_FIELD_SOURCES: readonly FieldSource[] = [
+const EXTRA_FIELD_SOURCES: readonly FieldSourcePosition[] = [
   { x: 20, y: 0 },
   { x: 0, y: 20 },
   { x: 0, y: -20 }
@@ -178,20 +198,24 @@
 }
 
 function getFieldSources(state: LiveState): FieldSource[] {
-  return [{ x: state.negX, y: state.negY }, ...EXTRA_FIELD_SOURCES];
+  return [
+    { x: state.negX, y: state.negY, strength: simParams.source0Magnitude * simParams.source0Direction },
+    { x: EXTRA_FIELD_SOURCES[0].x, y: EXTRA_FIELD_SOURCES[0].y, strength: simParams.source1Magnitude * simParams.source1Direction },
+    { x: EXTRA_FIELD_SOURCES[1].x, y: EXTRA_FIELD_SOURCES[1].y, strength: simParams.source2Magnitude * simParams.source2Direction },
+    { x: EXTRA_FIELD_SOURCES[2].x, y: EXTRA_FIELD_SOURCES[2].y, strength: simParams.source3Magnitude * simParams.source3Direction }
+  ];
 }
 
 function stepState(state: LiveState, params: SimParams, display: DisplayParams, rng: Rng): void {
-  const electricStrength = clamp(params.electricStrength, -10, 10);
   const chamberHalfExtent = Math.max(1, display.axisLimit);
-  const fieldSources = getFieldSources(state);
+  const fieldSources = getFieldSources(state).map((source) => ({ ...source, strength: clamp(source.strength, -10, 10) }));
   for (let i = 0; i < state.numParticles; i += 1) {
     let driftX = 0;
     let driftY = 0;
     for (const source of fieldSources) {
       const dx = source.x - state.x[i];
       const dy = source.y - state.y[i];
-      const [sourceDriftX, sourceDriftY] = pointFieldDrift(dx, dy, electricStrength, 1);
+      const [sourceDriftX, sourceDriftY] = pointFieldDrift(dx, dy, source.strength, 1);
       driftX += sourceDriftX;
       driftY += sourceDriftY;
     }
@@ -229,7 +253,7 @@
   }
 }
 
-function drawFrame(canvas: HTMLCanvasElement, state: LiveState, display: DisplayParams, electricStrength: number): void {
+function drawFrame(canvas: HTMLCanvasElement, state: LiveState, display: DisplayParams): void {
   syncCanvasSize(canvas);
   const ctx = canvas.getContext('2d');
   if (!ctx) return;
@@ -287,8 +311,8 @@
     ctx.fill();
   }
 
-  ctx.fillStyle = fieldColor(electricStrength);
   for (const source of getFieldSources(state)) {
+    ctx.fillStyle = fieldColor(source.strength);
     ctx.beginPath();
     ctx.arc(halfW + source.x * sx, halfH - source.y * sy, 4 * dpr, 0, Math.PI * 2);
     ctx.fill();
@@ -334,7 +358,14 @@
             <div class="control-grid">
               <div class="field"><label for="num-particles">Particles</label><input id="num-particles" type="number" min="1" max="5000" step="1" /></div>
               <div class="field"><label for="diffusion-sd">Diffusion SD</label><input id="diffusion-sd" type="number" min="0" max="20" step="0.05" /></div>
-              <div class="field"><label for="electric-strength">Electric strength</label><input id="electric-strength" type="number" min="-10" max="10" step="0.01" /></div>
+              <div class="field"><label for="source0-magnitude">Generator 1 magnitude</label><input id="source0-magnitude" type="number" min="0" max="10" step="0.01" /></div>
+              <div class="field"><label for="source0-direction">Generator 1 direction</label><select id="source0-direction"><option value="-1">Attract</option><option value="1">Repel</option></select></div>
+              <div class="field"><label for="source1-magnitude">Generator 2 magnitude</label><input id="source1-magnitude" type="number" min="0" max="10" step="0.01" /></div>
+              <div class="field"><label for="source1-direction">Generator 2 direction</label><select id="source1-direction"><option value="-1">Attract</option><option value="1">Repel</option></select></div>
+              <div class="field"><label for="source2-magnitude">Generator 3 magnitude</label><input id="source2-magnitude" type="number" min="0" max="10" step="0.01" /></div>
+              <div class="field"><label for="source2-direction">Generator 3 direction</label><select id="source2-direction"><option value="-1">Attract</option><option value="1">Repel</option></select></div>
+              <div class="field"><label for="source3-magnitude">Generator 4 magnitude</label><input id="source3-magnitude" type="number" min="0" max="10" step="0.01" /></div>
+              <div class="field"><label for="source3-direction">Generator 4 direction</label><select id="source3-direction"><option value="-1">Attract</option><option value="1">Repel</option></select></div>
               <div class="field"><label for="playback-speed">Playback speed</label><input id="playback-speed" type="number" min="0.1" max="8" step="0.1" /></div>
               <div class="field"><label for="seed">Seed</label><input id="seed" type="number" min="0" max="4294967295" step="1" /></div>
             </div>
@@ -395,7 +426,14 @@
 const inputs = {
   numParticles: getEl<HTMLInputElement>('#num-particles'),
   diffusionSd: getEl<HTMLInputElement>('#diffusion-sd'),
-  electricStrength: getEl<HTMLInputElement>('#electric-strength'),
+  source0Magnitude: getEl<HTMLInputElement>('#source0-magnitude'),
+  source0Direction: getEl<HTMLSelectElement>('#source0-direction'),
+  source1Magnitude: getEl<HTMLInputElement>('#source1-magnitude'),
+  source1Direction: getEl<HTMLSelectElement>('#source1-direction'),
+  source2Magnitude: getEl<HTMLInputElement>('#source2-magnitude'),
+  source2Direction: getEl<HTMLSelectElement>('#source2-direction'),
+  source3Magnitude: getEl<HTMLInputElement>('#source3-magnitude'),
+  source3Direction: getEl<HTMLSelectElement>('#source3-direction'),
   playbackSpeed: getEl<HTMLInputElement>('#playback-speed'),
   seed: getEl<HTMLInputElement>('#seed'),
   totalTime: getEl<HTMLInputElement>('#total-time'),
@@ -439,7 +477,14 @@
 function writeInputs(): void {
   setNumberInput(inputs.numParticles, simParams.numParticles, 0);
   setNumberInput(inputs.diffusionSd, simParams.diffusionSd, 3);
-  setNumberInput(inputs.electricStrength, simParams.electricStrength, 3);
+  setNumberInput(inputs.source0Magnitude, simParams.source0Magnitude, 3);
+  inputs.source0Direction.value = String(simParams.source0Direction);
+  setNumberInput(inputs.source1Magnitude, simParams.source1Magnitude, 3);
+  inputs.source1Direction.value = String(simParams.source1Direction);
+  setNumberInput(inputs.source2Magnitude, simParams.source2Magnitude, 3);
+  inputs.source2Direction.value = String(simParams.source2Direction);
+  setNumberInput(inputs.source3Magnitude, simParams.source3Magnitude, 3);
+  inputs.source3Direction.value = String(simParams.source3Direction);
   setNumberInput(inputs.playbackSpeed, displayParams.playbackSpeed, 2);
   setNumberInput(inputs.seed, currentSeed, 0);
   setNumberInput(inputs.totalTime, simParams.T, 0);
@@ -459,7 +504,14 @@
     numParticles: clamp(Math.round(Number(inputs.numParticles.value) || defaultSim.numParticles), 1, MAX_PARTICLES),
     diffusionSd: clamp(Number(inputs.diffusionSd.value) || 0, 0, 20),
     initClusterSd: clamp(Number(inputs.initClusterSd.value) || 0, 0, 20),
-    electricStrength: clamp(Number(inputs.electricStrength.value) || 0, -10, 10),
+    source0Magnitude: clamp(Number(inputs.source0Magnitude.value) || 0, 0, 10),
+    source0Direction: Number(inputs.source0Direction.value) === 1 ? 1 : -1,
+    source1Magnitude: clamp(Number(inputs.source1Magnitude.value) || 0, 0, 10),
+    source1Direction: Number(inputs.source1Direction.value) === 1 ? 1 : -1,
+    source2Magnitude: clamp(Number(inputs.source2Magnitude.value) || 0, 0, 10),
+    source2Direction: Number(inputs.source2Direction.value) === 1 ? 1 : -1,
+    source3Magnitude: clamp(Number(inputs.source3Magnitude.value) || 0, 0, 10),
+    source3Direction: Number(inputs.source3Direction.value) === 1 ? 1 : -1,
     negX: Number(inputs.negX.value) || defaultSim.negX,
     negY: Number(inputs.negY.value) || defaultSim.negY
   };
@@ -488,11 +540,17 @@
   const sigma = simParams.diffusionSd;
   const dt = simParams.dt;
   const stepSd = sigma * dt;
+  const strengths = [
+    simParams.source0Magnitude * simParams.source0Direction,
+    simParams.source1Magnitude * simParams.source1Direction,
+    simParams.source2Magnitude * simParams.source2Direction,
+    simParams.source3Magnitude * simParams.source3Direction
+  ];
   equationBlock.innerHTML = [
     '<span class="accent">Pedagogical stochastic-rate form</span>',
     'dx/dt = ξ_x(t) + drift_x,   dy/dt = ξ_y(t) + drift_y',
     'ξ_x, ξ_y ~ Normal(0, σ²),  where σ = diffusionSd',
-    'drift = -electric_strength · (field_source - pos) / (||field_source - pos|| + ε)',
+    'drift = Σ_s [ -electric_strength_s · (field_source_s - pos) / (||field_source_s - pos|| + ε) ]',
     '',
     '<span class="accent-2">Euler step used in this simulation</span>',
     'x_trial = x_old + (dxdt + drift_x) · dt',
@@ -504,8 +562,8 @@
     '<span class="accent-2">Reflective chamber rule</span>',
     'Particles bounce off the chamber walls instead of leaving the visible box.',
     '',
-    'Negative electric_strength attracts positive charge; positive electric_strength repels it.',
-    `Current: diffusionSd = ${sigma.toFixed(3)}, dt = ${dt.toFixed(3)}, electric strength = ${simParams.electricStrength.toFixed(3)}`,
+    'Negative electric_strength_s attracts positive charge; positive electric_strength_s repels it.',
+    `Current: diffusionSd = ${sigma.toFixed(3)}, dt = ${dt.toFixed(3)}, strengths = [${strengths.map((v) => v.toFixed(3)).join(', ')}]`,
     `Per-step displacement SD = diffusionSd × dt = ${stepSd.toFixed(3)}`
   ].join('\n');
 }
@@ -550,7 +608,7 @@
 
 function render(): void {
   updateStatus();
-  drawFrame(canvas, state, displayParams, simParams.electricStrength);
+  drawFrame(canvas, state, displayParams);
 }
 
 writeInputs();
@@ -584,7 +642,7 @@
   render();
 });
 
-for (const key of ['numParticles', 'diffusionSd', 'electricStrength', 'seed', 'totalTime', 'dt', 'initClusterSd', 'negX', 'negY'] as const) {
+for (const key of ['numParticles', 'diffusionSd', 'source0Magnitude', 'source0Direction', 'source1Magnitude', 'source1Direction', 'source2Magnitude', 'source2Direction', 'source3Magnitude', 'source3Direction', 'seed', 'totalTime', 'dt', 'initClusterSd', 'negX', 'negY'] as const) {
   inputs[key].addEventListener('change', () => {
     applyLiveSimParams();
     render();

exec
/bin/zsh -lc 'npm run build' in /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_web succeeded in 1.15s:

> electrochemical-signals-progression-web@0.1.0 build
> tsc && vite build

vite v6.4.1 building for production...
transforming...
✓ 37 modules transformed.
rendering chunks...
computing gzip size...
dist/index.html                                                         0.50 kB │ gzip: 0.31 kB
dist/inspect_diffusion_free.html                                        0.58 kB │ gzip: 0.32 kB
dist/inspect_diffusion_selective.html                                   0.59 kB │ gzip: 0.33 kB
dist/inspect_nernst_one_ion.html                                        0.59 kB │ gzip: 0.34 kB
dist/inspect_diffusion_pore.html                                        0.60 kB │ gzip: 0.34 kB
dist/inspect_nak_pump_gradients.html                                    0.60 kB │ gzip: 0.34 kB
dist/inspect_diffusion_permeability.html                                0.60 kB │ gzip: 0.34 kB
dist/inspect_membrane_potential_intro.html                              0.60 kB │ gzip: 0.34 kB
dist/inspect_resting_potential_charge_wall.html                         0.60 kB │ gzip: 0.34 kB
dist/inspect_field_attraction.html                                      0.61 kB │ gzip: 0.34 kB
dist/inspect_two_ion_selective_channels.html                            0.63 kB │ gzip: 0.35 kB
file update:
diff --git a/code/electrochemical_signals_progression_web/src/pages/inspect_field_attraction.ts b/code/electrochemical_signals_progression_web/src/pages/inspect_field_attraction.ts
index cbde19b0e3f80bd8bbb883115fd91de7b6996acf..1b97766175097ec1c1f6c034633fa1956ef0d5dd
--- a/code/electrochemical_signals_progression_web/src/pages/inspect_field_attraction.ts
+++ b/code/electrochemical_signals_progression_web/src/pages/inspect_field_attraction.ts
@@ -14,7 +14,14 @@
   numParticles: number;
   diffusionSd: number;
   initClusterSd: number;
-  electricStrength: number;
+  source0Magnitude: number;
+  source0Direction: number;
+  source1Magnitude: number;
+  source1Direction: number;
+  source2Magnitude: number;
+  source2Direction: number;
+  source3Magnitude: number;
+  source3Direction: number;
   negX: number;
   negY: number;
 }
@@ -40,6 +47,12 @@
 interface FieldSource {
   x: number;
   y: number;
+  strength: number;
+}
+
+interface FieldSourcePosition {
+  x: number;
+  y: number;
 }
 
 class Rng {
@@ -83,7 +96,14 @@
   numParticles: DEFAULT_NUM_PARTICLES,
   diffusionSd: DEFAULT_DIFFUSION_SD,
   initClusterSd: 0.1,
-  electricStrength: -0.15,
+  source0Magnitude: 0.15,
+  source0Direction: -1,
+  source1Magnitude: 0.15,
+  source1Direction: -1,
+  source2Magnitude: 0.15,
+  source2Direction: -1,
+  source3Magnitude: 0.15,
+  source3Direction: -1,
   negX: -20,
   negY: 0
 };
@@ -95,7 +115,7 @@
   targetFps: 30
 };
 
-const EXTRA_FIELD_SOURCES: readonly FieldSource[] = [
+const EXTRA_FIELD_SOURCES: readonly FieldSourcePosition[] = [
   { x: 20, y: 0 },
   { x: 0, y: 20 },
   { x: 0, y: -20 }
@@ -178,20 +198,24 @@
 }
 
 function getFieldSources(state: LiveState): FieldSource[] {
-  return [{ x: state.negX, y: state.negY }, ...EXTRA_FIELD_SOURCES];
+  return [
+    { x: state.negX, y: state.negY, strength: simParams.source0Magnitude * simParams.source0Direction },
+    { x: EXTRA_FIELD_SOURCES[0].x, y: EXTRA_FIELD_SOURCES[0].y, strength: simParams.source1Magnitude * simParams.source1Direction },
+    { x: EXTRA_FIELD_SOURCES[1].x, y: EXTRA_FIELD_SOURCES[1].y, strength: simParams.source2Magnitude * simParams.source2Direction },
+    { x: EXTRA_FIELD_SOURCES[2].x, y: EXTRA_FIELD_SOURCES[2].y, strength: simParams.source3Magnitude * simParams.source3Direction }
+  ];
 }
 
 function stepState(state: LiveState, params: SimParams, display: DisplayParams, rng: Rng): void {
-  const electricStrength = clamp(params.electricStrength, -10, 10);
   const chamberHalfExtent = Math.max(1, display.axisLimit);
-  const fieldSources = getFieldSources(state);
+  const fieldSources = getFieldSources(state).map((source) => ({ ...source, strength: clamp(source.strength, -10, 10) }));
   for (let i = 0; i < state.numParticles; i += 1) {
     let driftX = 0;
     let driftY = 0;
     for (const source of fieldSources) {
       const dx = source.x - state.x[i];
       const dy = source.y - state.y[i];
-      const [sourceDriftX, sourceDriftY] = pointFieldDrift(dx, dy, electricStrength, 1);
+      const [sourceDriftX, sourceDriftY] = pointFieldDrift(dx, dy, source.strength, 1);
       driftX += sourceDriftX;
       driftY += sourceDriftY;
     }
@@ -229,7 +253,7 @@
   }
 }
 
-function drawFrame(canvas: HTMLCanvasElement, state: LiveState, display: DisplayParams, electricStrength: number): void {
+function drawFrame(canvas: HTMLCanvasElement, state: LiveState, display: DisplayParams): void {
   syncCanvasSize(canvas);
   const ctx = canvas.getContext('2d');
   if (!ctx) return;
@@ -287,8 +311,8 @@
     ctx.fill();
   }
 
-  ctx.fillStyle = fieldColor(electricStrength);
   for (const source of getFieldSources(state)) {
+    ctx.fillStyle = fieldColor(source.strength);
     ctx.beginPath();
     ctx.arc(halfW + source.x * sx, halfH - source.y * sy, 4 * dpr, 0, Math.PI * 2);
     ctx.fill();
@@ -334,7 +358,14 @@
             <div class="control-grid">
               <div class="field"><label for="num-particles">Particles</label><input id="num-particles" type="number" min="1" max="5000" step="1" /></div>
               <div class="field"><label for="diffusion-sd">Diffusion SD</label><input id="diffusion-sd" type="number" min="0" max="20" step="0.05" /></div>
-              <div class="field"><label for="electric-strength">Electric strength</label><input id="electric-strength" type="number" min="-10" max="10" step="0.01" /></div>
+              <div class="field"><label for="source0-magnitude">Generator 1 magnitude</label><input id="source0-magnitude" type="number" min="0" max="10" step="0.01" /></div>
+              <div class="field"><label for="source0-direction">Generator 1 direction</label><select id="source0-direction"><option value="-1">Attract</option><option value="1">Repel</option></select></div>
+              <div class="field"><label for="source1-magnitude">Generator 2 magnitude</label><input id="source1-magnitude" type="number" min="0" max="10" step="0.01" /></div>
+              <div class="field"><label for="source1-direction">Generator 2 direction</label><select id="source1-direction"><option value="-1">Attract</option><option value="1">Repel</option></select></div>
+              <div class="field"><label for="source2-magnitude">Generator 3 magnitude</label><input id="source2-magnitude" type="number" min="0" max="10" step="0.01" /></div>
+              <div class="field"><label for="source2-direction">Generator 3 direction</label><select id="source2-direction"><option value="-1">Attract</option><option value="1">Repel</option></select></div>
+              <div class="field"><label for="source3-magnitude">Generator 4 magnitude</label><input id="source3-magnitude" type="number" min="0" max="10" step="0.01" /></div>
+              <div class="field"><label for="source3-direction">Generator 4 direction</label><select id="source3-direction"><option value="-1">Attract</option><option value="1">Repel</option></select></div>
               <div class="field"><label for="playback-speed">Playback speed</label><input id="playback-speed" type="number" min="0.1" max="8" step="0.1" /></div>
               <div class="field"><label for="seed">Seed</label><input id="seed" type="number" min="0" max="4294967295" step="1" /></div>
             </div>
@@ -395,7 +426,14 @@
 const inputs = {
   numParticles: getEl<HTMLInputElement>('#num-particles'),
   diffusionSd: getEl<HTMLInputElement>('#diffusion-sd'),
-  electricStrength: getEl<HTMLInputElement>('#electric-strength'),
+  source0Magnitude: getEl<HTMLInputElement>('#source0-magnitude'),
+  source0Direction: getEl<HTMLSelectElement>('#source0-direction'),
+  source1Magnitude: getEl<HTMLInputElement>('#source1-magnitude'),
+  source1Direction: getEl<HTMLSelectElement>('#source1-direction'),
+  source2Magnitude: getEl<HTMLInputElement>('#source2-magnitude'),
+  source2Direction: getEl<HTMLSelectElement>('#source2-direction'),
+  source3Magnitude: getEl<HTMLInputElement>('#source3-magnitude'),
+  source3Direction: getEl<HTMLSelectElement>('#source3-direction'),
   playbackSpeed: getEl<HTMLInputElement>('#playback-speed'),
   seed: getEl<HTMLInputElement>('#seed'),
   totalTime: getEl<HTMLInputElement>('#total-time'),
@@ -439,7 +477,14 @@
 function writeInputs(): void {
   setNumberInput(inputs.numParticles, simParams.numParticles, 0);
   setNumberInput(inputs.diffusionSd, simParams.diffusionSd, 3);
-  setNumberInput(inputs.electricStrength, simParams.electricStrength, 3);
+  setNumberInput(inputs.source0Magnitude, simParams.source0Magnitude, 3);
+  inputs.source0Direction.value = String(simParams.source0Direction);
+  setNumberInput(inputs.source1Magnitude, simParams.source1Magnitude, 3);
+  inputs.source1Direction.value = String(simParams.source1Direction);
+  setNumberInput(inputs.source2Magnitude, simParams.source2Magnitude, 3);
+  inputs.source2Direction.value = String(simParams.source2Direction);
+  setNumberInput(inputs.source3Magnitude, simParams.source3Magnitude, 3);
+  inputs.source3Direction.value = String(simParams.source3Direction);
   setNumberInput(inputs.playbackSpeed, displayParams.playbackSpeed, 2);
   setNumberInput(inputs.seed, currentSeed, 0);
   setNumberInput(inputs.totalTime, simParams.T, 0);
@@ -459,7 +504,14 @@
     numParticles: clamp(Math.round(Number(inputs.numParticles.value) || defaultSim.numParticles), 1, MAX_PARTICLES),
     diffusionSd: clamp(Number(inputs.diffusionSd.value) || 0, 0, 20),
     initClusterSd: clamp(Number(inputs.initClusterSd.value) || 0, 0, 20),
-    electricStrength: clamp(Number(inputs.electricStrength.value) || 0, -10, 10),
+    source0Magnitude: clamp(Number(inputs.source0Magnitude.value) || 0, 0, 10),
+    source0Direction: Number(inputs.source0Direction.value) === 1 ? 1 : -1,
+    source1Magnitude: clamp(Number(inputs.source1Magnitude.value) || 0, 0, 10),
+    source1Direction: Number(inputs.source1Direction.value) === 1 ? 1 : -1,
+    source2Magnitude: clamp(Number(inputs.source2Magnitude.value) || 0, 0, 10),
+    source2Direction: Number(inputs.source2Direction.value) === 1 ? 1 : -1,
+    source3Magnitude: clamp(Number(inputs.source3Magnitude.value) || 0, 0, 10),
+    source3Direction: Number(inputs.source3Direction.value) === 1 ? 1 : -1,
     negX: Number(inputs.negX.value) || defaultSim.negX,
     negY: Number(inputs.negY.value) || defaultSim.negY
   };
@@ -488,11 +540,17 @@
   const sigma = simParams.diffusionSd;
   const dt = simParams.dt;
   const stepSd = sigma * dt;
+  const strengths = [
+    simParams.source0Magnitude * simParams.source0Direction,
+    simParams.source1Magnitude * simParams.source1Direction,
+    simParams.source2Magnitude * simParams.source2Direction,
+    simParams.source3Magnitude * simParams.source3Direction
+  ];
   equationBlock.innerHTML = [
     '<span class="accent">Pedagogical stochastic-rate form</span>',
     'dx/dt = ξ_x(t) + drift_x,   dy/dt = ξ_y(t) + drift_y',
     'ξ_x, ξ_y ~ Normal(0, σ²),  where σ = diffusionSd',
-    'drift = -electric_strength · (field_source - pos) / (||field_source - pos|| + ε)',
+    'drift = Σ_s [ -electric_strength_s · (field_source_s - pos) / (||field_source_s - pos|| + ε) ]',
     '',
     '<span class="accent-2">Euler step used in this simulation</span>',
     'x_trial = x_old + (dxdt + drift_x) · dt',
@@ -504,8 +562,8 @@
     '<span class="accent-2">Reflective chamber rule</span>',
     'Particles bounce off the chamber walls instead of leaving the visible box.',
     '',
-    'Negative electric_strength attracts positive charge; positive electric_strength repels it.',
-    `Current: diffusionSd = ${sigma.toFixed(3)}, dt = ${dt.toFixed(3)}, electric strength = ${simParams.electricStrength.toFixed(3)}`,
+    'Negative electric_strength_s attracts positive charge; positive electric_strength_s repels it.',
+    `Current: diffusionSd = ${sigma.toFixed(3)}, dt = ${dt.toFixed(3)}, strengths = [${strengths.map((v) => v.toFixed(3)).join(', ')}]`,
     `Per-step displacement SD = diffusionSd × dt = ${stepSd.toFixed(3)}`
   ].join('\n');
 }
@@ -550,7 +608,7 @@
 
 function render(): void {
   updateStatus();
-  drawFrame(canvas, state, displayParams, simParams.electricStrength);
+  drawFrame(canvas, state, displayParams);
 }
 
 writeInputs();
@@ -584,7 +642,7 @@
   render();
 });
 
-for (const key of ['numParticles', 'diffusionSd', 'electricStrength', 'seed', 'totalTime', 'dt', 'initClusterSd', 'negX', 'negY'] as const) {
+for (const key of ['numParticles', 'diffusionSd', 'source0Magnitude', 'source0Direction', 'source1Magnitude', 'source1Direction', 'source2Magnitude', 'source2Direction', 'source3Magnitude', 'source3Direction', 'seed', 'totalTime', 'dt', 'initClusterSd', 'negX', 'negY'] as const) {
   inputs[key].addEventListener('change', () => {
     applyLiveSimParams();
     render();

exec
/bin/zsh -lc "git diff -- src/pages/inspect_field_attraction.ts | sed -n '1,260p'" in /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_web succeeded in 52ms:

file update:
diff --git a/code/electrochemical_signals_progression_web/src/pages/inspect_field_attraction.ts b/code/electrochemical_signals_progression_web/src/pages/inspect_field_attraction.ts
index cbde19b0e3f80bd8bbb883115fd91de7b6996acf..1b97766175097ec1c1f6c034633fa1956ef0d5dd
--- a/code/electrochemical_signals_progression_web/src/pages/inspect_field_attraction.ts
+++ b/code/electrochemical_signals_progression_web/src/pages/inspect_field_attraction.ts
@@ -14,7 +14,14 @@
   numParticles: number;
   diffusionSd: number;
   initClusterSd: number;
-  electricStrength: number;
+  source0Magnitude: number;
+  source0Direction: number;
+  source1Magnitude: number;
+  source1Direction: number;
+  source2Magnitude: number;
+  source2Direction: number;
+  source3Magnitude: number;
+  source3Direction: number;
   negX: number;
   negY: number;
 }
@@ -40,6 +47,12 @@
 interface FieldSource {
   x: number;
   y: number;
+  strength: number;
+}
+
+interface FieldSourcePosition {
+  x: number;
+  y: number;
 }
 
 class Rng {
@@ -83,7 +96,14 @@
   numParticles: DEFAULT_NUM_PARTICLES,
   diffusionSd: DEFAULT_DIFFUSION_SD,
   initClusterSd: 0.1,
-  electricStrength: -0.15,
+  source0Magnitude: 0.15,
+  source0Direction: -1,
+  source1Magnitude: 0.15,
+  source1Direction: -1,
+  source2Magnitude: 0.15,
+  source2Direction: -1,
+  source3Magnitude: 0.15,
+  source3Direction: -1,
   negX: -20,
   negY: 0
 };
@@ -95,7 +115,7 @@
   targetFps: 30
 };
 
-const EXTRA_FIELD_SOURCES: readonly FieldSource[] = [
+const EXTRA_FIELD_SOURCES: readonly FieldSourcePosition[] = [
   { x: 20, y: 0 },
   { x: 0, y: 20 },
   { x: 0, y: -20 }
@@ -178,20 +198,24 @@
 }
 
 function getFieldSources(state: LiveState): FieldSource[] {
-  return [{ x: state.negX, y: state.negY }, ...EXTRA_FIELD_SOURCES];
+  return [
+    { x: state.negX, y: state.negY, strength: simParams.source0Magnitude * simParams.source0Direction },
+    { x: EXTRA_FIELD_SOURCES[0].x, y: EXTRA_FIELD_SOURCES[0].y, strength: simParams.source1Magnitude * simParams.source1Direction },
+    { x: EXTRA_FIELD_SOURCES[1].x, y: EXTRA_FIELD_SOURCES[1].y, strength: simParams.source2Magnitude * simParams.source2Direction },
+    { x: EXTRA_FIELD_SOURCES[2].x, y: EXTRA_FIELD_SOURCES[2].y, strength: simParams.source3Magnitude * simParams.source3Direction }
+  ];
 }
 
 function stepState(state: LiveState, params: SimParams, display: DisplayParams, rng: Rng): void {
-  const electricStrength = clamp(params.electricStrength, -10, 10);
   const chamberHalfExtent = Math.max(1, display.axisLimit);
-  const fieldSources = getFieldSources(state);
+  const fieldSources = getFieldSources(state).map((source) => ({ ...source, strength: clamp(source.strength, -10, 10) }));
   for (let i = 0; i < state.numParticles; i += 1) {
     let driftX = 0;
     let driftY = 0;
     for (const source of fieldSources) {
       const dx = source.x - state.x[i];
       const dy = source.y - state.y[i];
-      const [sourceDriftX, sourceDriftY] = pointFieldDrift(dx, dy, electricStrength, 1);
+      const [sourceDriftX, sourceDriftY] = pointFieldDrift(dx, dy, source.strength, 1);
       driftX += sourceDriftX;
       driftY += sourceDriftY;
     }
@@ -229,7 +253,7 @@
   }
 }
 
-function drawFrame(canvas: HTMLCanvasElement, state: LiveState, display: DisplayParams, electricStrength: number): void {
+function drawFrame(canvas: HTMLCanvasElement, state: LiveState, display: DisplayParams): void {
   syncCanvasSize(canvas);
   const ctx = canvas.getContext('2d');
   if (!ctx) return;
@@ -287,8 +311,8 @@
     ctx.fill();
   }
 
-  ctx.fillStyle = fieldColor(electricStrength);
   for (const source of getFieldSources(state)) {
+    ctx.fillStyle = fieldColor(source.strength);
     ctx.beginPath();
     ctx.arc(halfW + source.x * sx, halfH - source.y * sy, 4 * dpr, 0, Math.PI * 2);
     ctx.fill();
@@ -334,7 +358,14 @@
             <div class="control-grid">
               <div class="field"><label for="num-particles">Particles</label><input id="num-particles" type="number" min="1" max="5000" step="1" /></div>
               <div class="field"><label for="diffusion-sd">Diffusion SD</label><input id="diffusion-sd" type="number" min="0" max="20" step="0.05" /></div>
-              <div class="field"><label for="electric-strength">Electric strength</label><input id="electric-strength" type="number" min="-10" max="10" step="0.01" /></div>
+              <div class="field"><label for="source0-magnitude">Generator 1 magnitude</label><input id="source0-magnitude" type="number" min="0" max="10" step="0.01" /></div>
+              <div class="field"><label for="source0-direction">Generator 1 direction</label><select id="source0-direction"><option value="-1">Attract</option><option value="1">Repel</option></select></div>
+              <div class="field"><label for="source1-magnitude">Generator 2 magnitude</label><input id="source1-magnitude" type="number" min="0" max="10" step="0.01" /></div>
+              <div class="field"><label for="source1-direction">Generator 2 direction</label><select id="source1-direction"><option value="-1">Attract</option><option value="1">Repel</option></select></div>
+              <div class="field"><label for="source2-magnitude">Generator 3 magnitude</label><input id="source2-magnitude" type="number" min="0" max="10" step="0.01" /></div>
+              <div class="field"><label for="source2-direction">Generator 3 direction</label><select id="source2-direction"><option value="-1">Attract</option><option value="1">Repel</option></select></div>
+              <div class="field"><label for="source3-magnitude">Generator 4 magnitude</label><input id="source3-magnitude" type="number" min="0" max="10" step="0.01" /></div>
+              <div class="field"><label for="source3-direction">Generator 4 direction</label><select id="source3-direction"><option value="-1">Attract</option><option value="1">Repel</option></select></div>
               <div class="field"><label for="playback-speed">Playback speed</label><input id="playback-speed" type="number" min="0.1" max="8" step="0.1" /></div>
               <div class="field"><label for="seed">Seed</label><input id="seed" type="number" min="0" max="4294967295" step="1" /></div>
             </div>
@@ -395,7 +426,14 @@
 const inputs = {
   numParticles: getEl<HTMLInputElement>('#num-particles'),
   diffusionSd: getEl<HTMLInputElement>('#diffusion-sd'),
-  electricStrength: getEl<HTMLInputElement>('#electric-strength'),
+  source0Magnitude: getEl<HTMLInputElement>('#source0-magnitude'),
+  source0Direction: getEl<HTMLSelectElement>('#source0-direction'),
+  source1Magnitude: getEl<HTMLInputElement>('#source1-magnitude'),
+  source1Direction: getEl<HTMLSelectElement>('#source1-direction'),
+  source2Magnitude: getEl<HTMLInputElement>('#source2-magnitude'),
+  source2Direction: getEl<HTMLSelectElement>('#source2-direction'),
+  source3Magnitude: getEl<HTMLInputElement>('#source3-magnitude'),
+  source3Direction: getEl<HTMLSelectElement>('#source3-direction'),
   playbackSpeed: getEl<HTMLInputElement>('#playback-speed'),
   seed: getEl<HTMLInputElement>('#seed'),
   totalTime: getEl<HTMLInputElement>('#total-time'),
@@ -439,7 +477,14 @@
 function writeInputs(): void {
   setNumberInput(inputs.numParticles, simParams.numParticles, 0);
   setNumberInput(inputs.diffusionSd, simParams.diffusionSd, 3);
-  setNumberInput(inputs.electricStrength, simParams.electricStrength, 3);
+  setNumberInput(inputs.source0Magnitude, simParams.source0Magnitude, 3);
+  inputs.source0Direction.value = String(simParams.source0Direction);
+  setNumberInput(inputs.source1Magnitude, simParams.source1Magnitude, 3);
+  inputs.source1Direction.value = String(simParams.source1Direction);
+  setNumberInput(inputs.source2Magnitude, simParams.source2Magnitude, 3);
+  inputs.source2Direction.value = String(simParams.source2Direction);
+  setNumberInput(inputs.source3Magnitude, simParams.source3Magnitude, 3);
+  inputs.source3Direction.value = String(simParams.source3Direction);
   setNumberInput(inputs.playbackSpeed, displayParams.playbackSpeed, 2);
   setNumberInput(inputs.seed, currentSeed, 0);
   setNumberInput(inputs.totalTime, simParams.T, 0);
@@ -459,7 +504,14 @@
     numParticles: clamp(Math.round(Number(inputs.numParticles.value) || defaultSim.numParticles), 1, MAX_PARTICLES),
     diffusionSd: clamp(Number(inputs.diffusionSd.value) || 0, 0, 20),
     initClusterSd: clamp(Number(inputs.initClusterSd.value) || 0, 0, 20),
-    electricStrength: clamp(Number(inputs.electricStrength.value) || 0, -10, 10),
+    source0Magnitude: clamp(Number(inputs.source0Magnitude.value) || 0, 0, 10),
+    source0Direction: Number(inputs.source0Direction.value) === 1 ? 1 : -1,
+    source1Magnitude: clamp(Number(inputs.source1Magnitude.value) || 0, 0, 10),
+    source1Direction: Number(inputs.source1Direction.value) === 1 ? 1 : -1,
+    source2Magnitude: clamp(Number(inputs.source2Magnitude.value) || 0, 0, 10),
+    source2Direction: Number(inputs.source2Direction.value) === 1 ? 1 : -1,
+    source3Magnitude: clamp(Number(inputs.source3Magnitude.value) || 0, 0, 10),
+    source3Direction: Number(inputs.source3Direction.value) === 1 ? 1 : -1,
     negX: Number(inputs.negX.value) || defaultSim.negX,
     negY: Number(inputs.negY.value) || defaultSim.negY
   };
@@ -488,11 +540,17 @@
   const sigma = simParams.diffusionSd;
   const dt = simParams.dt;
   const stepSd = sigma * dt;
+  const strengths = [
+    simParams.source0Magnitude * simParams.source0Direction,
+    simParams.source1Magnitude * simParams.source1Direction,
+    simParams.source2Magnitude * simParams.source2Direction,
+    simParams.source3Magnitude * simParams.source3Direction
+  ];
   equationBlock.innerHTML = [
     '<span class="accent">Pedagogical stochastic-rate form</span>',
     'dx/dt = ξ_x(t) + drift_x,   dy/dt = ξ_y(t) + drift_y',
     'ξ_x, ξ_y ~ Normal(0, σ²),  where σ = diffusionSd',
-    'drift = -electric_strength · (field_source - pos) / (||field_source - pos|| + ε)',
+    'drift = Σ_s [ -electric_strength_s · (field_source_s - pos) / (||field_source_s - pos|| + ε) ]',
     '',
     '<span class="accent-2">Euler step used in this simulation</span>',
     'x_trial = x_old + (dxdt + drift_x) · dt',
@@ -504,8 +562,8 @@
     '<span class="accent-2">Reflective chamber rule</span>',
     'Particles bounce off the chamber walls instead of leaving the visible box.',
     '',
-    'Negative electric_strength attracts positive charge; positive electric_strength repels it.',
-    `Current: diffusionSd = ${sigma.toFixed(3)}, dt = ${dt.toFixed(3)}, electric strength = ${simParams.electricStrength.toFixed(3)}`,
+    'Negative electric_strength_s attracts positive charge; positive electric_strength_s repels it.',
+    `Current: diffusionSd = ${sigma.toFixed(3)}, dt = ${dt.toFixed(3)}, strengths = [${strengths.map((v) => v.toFixed(3)).join(', ')}]`,
     `Per-step displacement SD = diffusionSd × dt = ${stepSd.toFixed(3)}`
   ].join('\n');
 }
@@ -550,7 +608,7 @@
 
 function render(): void {
   updateStatus();
-  drawFrame(canvas, state, displayParams, simParams.electricStrength);
+  drawFrame(canvas, state, displayParams);
 }
 
 writeInputs();
@@ -584,7 +642,7 @@
   render();
 });
 
-for (const key of ['numParticles', 'diffusionSd', 'electricStrength', 'seed', 'totalTime', 'dt', 'initClusterSd', 'negX', 'negY'] as const) {
+for (const key of ['numParticles', 'diffusionSd', 'source0Magnitude', 'source0Direction', 'source1Magnitude', 'source1Direction', 'source2Magnitude', 'source2Direction', 'source3Magnitude', 'source3Direction', 'seed', 'totalTime', 'dt', 'initClusterSd', 'negX', 'negY'] as const) {
   inputs[key].addEventListener('change', () => {
     applyLiveSimParams();
     render();

exec
/bin/zsh -lc 'git status --short' in /Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_web succeeded in 53ms:
 M README.md
 D inspect_diffusion_1.html
 D inspect_diffusion_2.html
 D inspect_diffusion_3.html
 D inspect_diffusion_4.html
 D inspect_electrochemical_1.html
 D inspect_electrochemical_2.html
 D inspect_electrochemical_3.html
 D inspect_resting_potential_1.html
 D inspect_resting_potential_2.html
 D inspect_resting_potential_3.html
 D inspect_resting_potential_4.html
 M package.json
 M src/index.ts
 D src/pages/inspect_diffusion_1.ts
 D src/pages/inspect_diffusion_2.ts
 D src/pages/inspect_diffusion_3.ts
 D src/pages/inspect_diffusion_4.ts
 D src/pages/inspect_electrochemical_1.ts
 D src/pages/inspect_electrochemical_2.ts
file update:
diff --git a/code/electrochemical_signals_progression_web/src/pages/inspect_field_attraction.ts b/code/electrochemical_signals_progression_web/src/pages/inspect_field_attraction.ts
index cbde19b0e3f80bd8bbb883115fd91de7b6996acf..1b97766175097ec1c1f6c034633fa1956ef0d5dd
--- a/code/electrochemical_signals_progression_web/src/pages/inspect_field_attraction.ts
+++ b/code/electrochemical_signals_progression_web/src/pages/inspect_field_attraction.ts
@@ -14,7 +14,14 @@
   numParticles: number;
   diffusionSd: number;
   initClusterSd: number;
-  electricStrength: number;
+  source0Magnitude: number;
+  source0Direction: number;
+  source1Magnitude: number;
+  source1Direction: number;
+  source2Magnitude: number;
+  source2Direction: number;
+  source3Magnitude: number;
+  source3Direction: number;
   negX: number;
   negY: number;
 }
@@ -40,6 +47,12 @@
 interface FieldSource {
   x: number;
   y: number;
+  strength: number;
+}
+
+interface FieldSourcePosition {
+  x: number;
+  y: number;
 }
 
 class Rng {
@@ -83,7 +96,14 @@
   numParticles: DEFAULT_NUM_PARTICLES,
   diffusionSd: DEFAULT_DIFFUSION_SD,
   initClusterSd: 0.1,
-  electricStrength: -0.15,
+  source0Magnitude: 0.15,
+  source0Direction: -1,
+  source1Magnitude: 0.15,
+  source1Direction: -1,
+  source2Magnitude: 0.15,
+  source2Direction: -1,
+  source3Magnitude: 0.15,
+  source3Direction: -1,
   negX: -20,
   negY: 0
 };
@@ -95,7 +115,7 @@
   targetFps: 30
 };
 
-const EXTRA_FIELD_SOURCES: readonly FieldSource[] = [
+const EXTRA_FIELD_SOURCES: readonly FieldSourcePosition[] = [
   { x: 20, y: 0 },
   { x: 0, y: 20 },
   { x: 0, y: -20 }
@@ -178,20 +198,24 @@
 }
 
 function getFieldSources(state: LiveState): FieldSource[] {
-  return [{ x: state.negX, y: state.negY }, ...EXTRA_FIELD_SOURCES];
+  return [
+    { x: state.negX, y: state.negY, strength: simParams.source0Magnitude * simParams.source0Direction },
+    { x: EXTRA_FIELD_SOURCES[0].x, y: EXTRA_FIELD_SOURCES[0].y, strength: simParams.source1Magnitude * simParams.source1Direction },
+    { x: EXTRA_FIELD_SOURCES[1].x, y: EXTRA_FIELD_SOURCES[1].y, strength: simParams.source2Magnitude * simParams.source2Direction },
+    { x: EXTRA_FIELD_SOURCES[2].x, y: EXTRA_FIELD_SOURCES[2].y, strength: simParams.source3Magnitude * simParams.source3Direction }
+  ];
 }
 
 function stepState(state: LiveState, params: SimParams, display: DisplayParams, rng: Rng): void {
-  const electricStrength = clamp(params.electricStrength, -10, 10);
   const chamberHalfExtent = Math.max(1, display.axisLimit);
-  const fieldSources = getFieldSources(state);
+  const fieldSources = getFieldSources(state).map((source) => ({ ...source, strength: clamp(source.strength, -10, 10) }));
   for (let i = 0; i < state.numParticles; i += 1) {
     let driftX = 0;
     let driftY = 0;
     for (const source of fieldSources) {
       const dx = source.x - state.x[i];
       const dy = source.y - state.y[i];
-      const [sourceDriftX, sourceDriftY] = pointFieldDrift(dx, dy, electricStrength, 1);
+      const [sourceDriftX, sourceDriftY] = pointFieldDrift(dx, dy, source.strength, 1);
       driftX += sourceDriftX;
       driftY += sourceDriftY;
     }
@@ -229,7 +253,7 @@
   }
 }
 
-function drawFrame(canvas: HTMLCanvasElement, state: LiveState, display: DisplayParams, electricStrength: number): void {
+function drawFrame(canvas: HTMLCanvasElement, state: LiveState, display: DisplayParams): void {
   syncCanvasSize(canvas);
   const ctx = canvas.getContext('2d');
   if (!ctx) return;
@@ -287,8 +311,8 @@
     ctx.fill();
   }
 
-  ctx.fillStyle = fieldColor(electricStrength);
   for (const source of getFieldSources(state)) {
+    ctx.fillStyle = fieldColor(source.strength);
     ctx.beginPath();
     ctx.arc(halfW + source.x * sx, halfH - source.y * sy, 4 * dpr, 0, Math.PI * 2);
     ctx.fill();
@@ -334,7 +358,14 @@
             <div class="control-grid">
               <div class="field"><label for="num-particles">Particles</label><input id="num-particles" type="number" min="1" max="5000" step="1" /></div>
               <div class="field"><label for="diffusion-sd">Diffusion SD</label><input id="diffusion-sd" type="number" min="0" max="20" step="0.05" /></div>
-              <div class="field"><label for="electric-strength">Electric strength</label><input id="electric-strength" type="number" min="-10" max="10" step="0.01" /></div>
+              <div class="field"><label for="source0-magnitude">Generator 1 magnitude</label><input id="source0-magnitude" type="number" min="0" max="10" step="0.01" /></div>
+              <div class="field"><label for="source0-direction">Generator 1 direction</label><select id="source0-direction"><option value="-1">Attract</option><option value="1">Repel</option></select></div>
+              <div class="field"><label for="source1-magnitude">Generator 2 magnitude</label><input id="source1-magnitude" type="number" min="0" max="10" step="0.01" /></div>
+              <div class="field"><label for="source1-direction">Generator 2 direction</label><select id="source1-direction"><option value="-1">Attract</option><option value="1">Repel</option></select></div>
+              <div class="field"><label for="source2-magnitude">Generator 3 magnitude</label><input id="source2-magnitude" type="number" min="0" max="10" step="0.01" /></div>
+              <div class="field"><label for="source2-direction">Generator 3 direction</label><select id="source2-direction"><option value="-1">Attract</option><option value="1">Repel</option></select></div>
+              <div class="field"><label for="source3-magnitude">Generator 4 magnitude</label><input id="source3-magnitude" type="number" min="0" max="10" step="0.01" /></div>
+              <div class="field"><label for="source3-direction">Generator 4 direction</label><select id="source3-direction"><option value="-1">Attract</option><option value="1">Repel</option></select></div>
               <div class="field"><label for="playback-speed">Playback speed</label><input id="playback-speed" type="number" min="0.1" max="8" step="0.1" /></div>
               <div class="field"><label for="seed">Seed</label><input id="seed" type="number" min="0" max="4294967295" step="1" /></div>
             </div>
@@ -395,7 +426,14 @@
 const inputs = {
   numParticles: getEl<HTMLInputElement>('#num-particles'),
   diffusionSd: getEl<HTMLInputElement>('#diffusion-sd'),
-  electricStrength: getEl<HTMLInputElement>('#electric-strength'),
+  source0Magnitude: getEl<HTMLInputElement>('#source0-magnitude'),
+  source0Direction: getEl<HTMLSelectElement>('#source0-direction'),
+  source1Magnitude: getEl<HTMLInputElement>('#source1-magnitude'),
+  source1Direction: getEl<HTMLSelectElement>('#source1-direction'),
+  source2Magnitude: getEl<HTMLInputElement>('#source2-magnitude'),
+  source2Direction: getEl<HTMLSelectElement>('#source2-direction'),
+  source3Magnitude: getEl<HTMLInputElement>('#source3-magnitude'),
+  source3Direction: getEl<HTMLSelectElement>('#source3-direction'),
   playbackSpeed: getEl<HTMLInputElement>('#playback-speed'),
   seed: getEl<HTMLInputElement>('#seed'),
   totalTime: getEl<HTMLInputElement>('#total-time'),
@@ -439,7 +477,14 @@
 function writeInputs(): void {
   setNumberInput(inputs.numParticles, simParams.numParticles, 0);
   setNumberInput(inputs.diffusionSd, simParams.diffusionSd, 3);
-  setNumberInput(inputs.electricStrength, simParams.electricStrength, 3);
+  setNumberInput(inputs.source0Magnitude, simParams.source0Magnitude, 3);
+  inputs.source0Direction.value = String(simParams.source0Direction);
+  setNumberInput(inputs.source1Magnitude, simParams.source1Magnitude, 3);
+  inputs.source1Direction.value = String(simParams.source1Direction);
+  setNumberInput(inputs.source2Magnitude, simParams.source2Magnitude, 3);
+  inputs.source2Direction.value = String(simParams.source2Direction);
+  setNumberInput(inputs.source3Magnitude, simParams.source3Magnitude, 3);
+  inputs.source3Direction.value = String(simParams.source3Direction);
   setNumberInput(inputs.playbackSpeed, displayParams.playbackSpeed, 2);
   setNumberInput(inputs.seed, currentSeed, 0);
   setNumberInput(inputs.totalTime, simParams.T, 0);
@@ -459,7 +504,14 @@
     numParticles: clamp(Math.round(Number(inputs.numParticles.value) || defaultSim.numParticles), 1, MAX_PARTICLES),
     diffusionSd: clamp(Number(inputs.diffusionSd.value) || 0, 0, 20),
     initClusterSd: clamp(Number(inputs.initClusterSd.value) || 0, 0, 20),
-    electricStrength: clamp(Number(inputs.electricStrength.value) || 0, -10, 10),
+    source0Magnitude: clamp(Number(inputs.source0Magnitude.value) || 0, 0, 10),
+    source0Direction: Number(inputs.source0Direction.value) === 1 ? 1 : -1,
+    source1Magnitude: clamp(Number(inputs.source1Magnitude.value) || 0, 0, 10),
+    source1Direction: Number(inputs.source1Direction.value) === 1 ? 1 : -1,
+    source2Magnitude: clamp(Number(inputs.source2Magnitude.value) || 0, 0, 10),
+    source2Direction: Number(inputs.source2Direction.value) === 1 ? 1 : -1,
+    source3Magnitude: clamp(Number(inputs.source3Magnitude.value) || 0, 0, 10),
+    source3Direction: Number(inputs.source3Direction.value) === 1 ? 1 : -1,
     negX: Number(inputs.negX.value) || defaultSim.negX,
     negY: Number(inputs.negY.value) || defaultSim.negY
   };
@@ -488,11 +540,17 @@
   const sigma = simParams.diffusionSd;
   const dt = simParams.dt;
   const stepSd = sigma * dt;
+  const strengths = [
+    simParams.source0Magnitude * simParams.source0Direction,
+    simParams.source1Magnitude * simParams.source1Direction,
+    simParams.source2Magnitude * simParams.source2Direction,
+    simParams.source3Magnitude * simParams.source3Direction
+  ];
   equationBlock.innerHTML = [
     '<span class="accent">Pedagogical stochastic-rate form</span>',
     'dx/dt = ξ_x(t) + drift_x,   dy/dt = ξ_y(t) + drift_y',
     'ξ_x, ξ_y ~ Normal(0, σ²),  where σ = diffusionSd',
-    'drift = -electric_strength · (field_source - pos) / (||field_source - pos|| + ε)',
+    'drift = Σ_s [ -electric_strength_s · (field_source_s - pos) / (||field_source_s - pos|| + ε) ]',
     '',
     '<span class="accent-2">Euler step used in this simulation</span>',
     'x_trial = x_old + (dxdt + drift_x) · dt',
@@ -504,8 +562,8 @@
     '<span class="accent-2">Reflective chamber rule</span>',
     'Particles bounce off the chamber walls instead of leaving the visible box.',
     '',
-    'Negative electric_strength attracts positive charge; positive electric_strength repels it.',
-    `Current: diffusionSd = ${sigma.toFixed(3)}, dt = ${dt.toFixed(3)}, electric strength = ${simParams.electricStrength.toFixed(3)}`,
+    'Negative electric_strength_s attracts positive charge; positive electric_strength_s repels it.',
+    `Current: diffusionSd = ${sigma.toFixed(3)}, dt = ${dt.toFixed(3)}, strengths = [${strengths.map((v) => v.toFixed(3)).join(', ')}]`,
     `Per-step displacement SD = diffusionSd × dt = ${stepSd.toFixed(3)}`
   ].join('\n');
 }
@@ -550,7 +608,7 @@
 
 function render(): void {
   updateStatus();
-  drawFrame(canvas, state, displayParams, simParams.electricStrength);
+  drawFrame(canvas, state, displayParams);
 }
 
 writeInputs();
@@ -584,7 +642,7 @@
   render();
 });
 
-for (const key of ['numParticles', 'diffusionSd', 'electricStrength', 'seed', 'totalTime', 'dt', 'initClusterSd', 'negX', 'negY'] as const) {
+for (const key of ['numParticles', 'diffusionSd', 'source0Magnitude', 'source0Direction', 'source1Magnitude', 'source1Direction', 'source2Magnitude', 'source2Direction', 'source3Magnitude', 'source3Direction', 'seed', 'totalTime', 'dt', 'initClusterSd', 'negX', 'negY'] as const) {
   inputs[key].addEventListener('change', () => {
     applyLiveSimParams();
     render();

codex
Implemented only the requested section (`Free Diffusion with Electrical Attraction`) in [inspect_field_attraction.ts](/Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_web/src/pages/inspect_field_attraction.ts).

Changes made:
- Replaced the single shared `Electric strength` control with per-generator controls for all 4 field generators:
  - `Generator N magnitude` (0 to 10)
  - `Generator N direction` (`Attract` / `Repel`)
- Updated simulation parameters and input parsing/writing to store per-generator magnitude and direction.
- Updated drift calculation so each generator contributes with its own signed strength (`magnitude * direction`).
- Updated field source rendering so each source color reflects its own effective strength.
- Updated equation/status text to show the multi-source formulation and current per-source strengths.

Checks run:
- `npm run build` completed successfully (`tsc && vite build`).
file update:
diff --git a/code/electrochemical_signals_progression_web/src/pages/inspect_field_attraction.ts b/code/electrochemical_signals_progression_web/src/pages/inspect_field_attraction.ts
index cbde19b0e3f80bd8bbb883115fd91de7b6996acf..1b97766175097ec1c1f6c034633fa1956ef0d5dd
--- a/code/electrochemical_signals_progression_web/src/pages/inspect_field_attraction.ts
+++ b/code/electrochemical_signals_progression_web/src/pages/inspect_field_attraction.ts
@@ -14,7 +14,14 @@
   numParticles: number;
   diffusionSd: number;
   initClusterSd: number;
-  electricStrength: number;
+  source0Magnitude: number;
+  source0Direction: number;
+  source1Magnitude: number;
+  source1Direction: number;
+  source2Magnitude: number;
+  source2Direction: number;
+  source3Magnitude: number;
+  source3Direction: number;
   negX: number;
   negY: number;
 }
@@ -40,6 +47,12 @@
 interface FieldSource {
   x: number;
   y: number;
+  strength: number;
+}
+
+interface FieldSourcePosition {
+  x: number;
+  y: number;
 }
 
 class Rng {
@@ -83,7 +96,14 @@
   numParticles: DEFAULT_NUM_PARTICLES,
   diffusionSd: DEFAULT_DIFFUSION_SD,
   initClusterSd: 0.1,
-  electricStrength: -0.15,
+  source0Magnitude: 0.15,
+  source0Direction: -1,
+  source1Magnitude: 0.15,
+  source1Direction: -1,
+  source2Magnitude: 0.15,
+  source2Direction: -1,
+  source3Magnitude: 0.15,
+  source3Direction: -1,
   negX: -20,
   negY: 0
 };
@@ -95,7 +115,7 @@
   targetFps: 30
 };
 
-const EXTRA_FIELD_SOURCES: readonly FieldSource[] = [
+const EXTRA_FIELD_SOURCES: readonly FieldSourcePosition[] = [
   { x: 20, y: 0 },
   { x: 0, y: 20 },
   { x: 0, y: -20 }
@@ -178,20 +198,24 @@
 }
 
 function getFieldSources(state: LiveState): FieldSource[] {
-  return [{ x: state.negX, y: state.negY }, ...EXTRA_FIELD_SOURCES];
+  return [
+    { x: state.negX, y: state.negY, strength: simParams.source0Magnitude * simParams.source0Direction },
+    { x: EXTRA_FIELD_SOURCES[0].x, y: EXTRA_FIELD_SOURCES[0].y, strength: simParams.source1Magnitude * simParams.source1Direction },
+    { x: EXTRA_FIELD_SOURCES[1].x, y: EXTRA_FIELD_SOURCES[1].y, strength: simParams.source2Magnitude * simParams.source2Direction },
+    { x: EXTRA_FIELD_SOURCES[2].x, y: EXTRA_FIELD_SOURCES[2].y, strength: simParams.source3Magnitude * simParams.source3Direction }
+  ];
 }
 
 function stepState(state: LiveState, params: SimParams, display: DisplayParams, rng: Rng): void {
-  const electricStrength = clamp(params.electricStrength, -10, 10);
   const chamberHalfExtent = Math.max(1, display.axisLimit);
-  const fieldSources = getFieldSources(state);
+  const fieldSources = getFieldSources(state).map((source) => ({ ...source, strength: clamp(source.strength, -10, 10) }));
   for (let i = 0; i < state.numParticles; i += 1) {
     let driftX = 0;
     let driftY = 0;
     for (const source of fieldSources) {
       const dx = source.x - state.x[i];
       const dy = source.y - state.y[i];
-      const [sourceDriftX, sourceDriftY] = pointFieldDrift(dx, dy, electricStrength, 1);
+      const [sourceDriftX, sourceDriftY] = pointFieldDrift(dx, dy, source.strength, 1);
       driftX += sourceDriftX;
       driftY += sourceDriftY;
     }
@@ -229,7 +253,7 @@
   }
 }
 
-function drawFrame(canvas: HTMLCanvasElement, state: LiveState, display: DisplayParams, electricStrength: number): void {
+function drawFrame(canvas: HTMLCanvasElement, state: LiveState, display: DisplayParams): void {
   syncCanvasSize(canvas);
   const ctx = canvas.getContext('2d');
   if (!ctx) return;
@@ -287,8 +311,8 @@
     ctx.fill();
   }
 
-  ctx.fillStyle = fieldColor(electricStrength);
   for (const source of getFieldSources(state)) {
+    ctx.fillStyle = fieldColor(source.strength);
     ctx.beginPath();
     ctx.arc(halfW + source.x * sx, halfH - source.y * sy, 4 * dpr, 0, Math.PI * 2);
     ctx.fill();
@@ -334,7 +358,14 @@
             <div class="control-grid">
               <div class="field"><label for="num-particles">Particles</label><input id="num-particles" type="number" min="1" max="5000" step="1" /></div>
               <div class="field"><label for="diffusion-sd">Diffusion SD</label><input id="diffusion-sd" type="number" min="0" max="20" step="0.05" /></div>
-              <div class="field"><label for="electric-strength">Electric strength</label><input id="electric-strength" type="number" min="-10" max="10" step="0.01" /></div>
+              <div class="field"><label for="source0-magnitude">Generator 1 magnitude</label><input id="source0-magnitude" type="number" min="0" max="10" step="0.01" /></div>
+              <div class="field"><label for="source0-direction">Generator 1 direction</label><select id="source0-direction"><option value="-1">Attract</option><option value="1">Repel</option></select></div>
+              <div class="field"><label for="source1-magnitude">Generator 2 magnitude</label><input id="source1-magnitude" type="number" min="0" max="10" step="0.01" /></div>
+              <div class="field"><label for="source1-direction">Generator 2 direction</label><select id="source1-direction"><option value="-1">Attract</option><option value="1">Repel</option></select></div>
+              <div class="field"><label for="source2-magnitude">Generator 3 magnitude</label><input id="source2-magnitude" type="number" min="0" max="10" step="0.01" /></div>
+              <div class="field"><label for="source2-direction">Generator 3 direction</label><select id="source2-direction"><option value="-1">Attract</option><option value="1">Repel</option></select></div>
+              <div class="field"><label for="source3-magnitude">Generator 4 magnitude</label><input id="source3-magnitude" type="number" min="0" max="10" step="0.01" /></div>
+              <div class="field"><label for="source3-direction">Generator 4 direction</label><select id="source3-direction"><option value="-1">Attract</option><option value="1">Repel</option></select></div>
               <div class="field"><label for="playback-speed">Playback speed</label><input id="playback-speed" type="number" min="0.1" max="8" step="0.1" /></div>
               <div class="field"><label for="seed">Seed</label><input id="seed" type="number" min="0" max="4294967295" step="1" /></div>
             </div>
@@ -395,7 +426,14 @@
 const inputs = {
   numParticles: getEl<HTMLInputElement>('#num-particles'),
   diffusionSd: getEl<HTMLInputElement>('#diffusion-sd'),
-  electricStrength: getEl<HTMLInputElement>('#electric-strength'),
+  source0Magnitude: getEl<HTMLInputElement>('#source0-magnitude'),
+  source0Direction: getEl<HTMLSelectElement>('#source0-direction'),
+  source1Magnitude: getEl<HTMLInputElement>('#source1-magnitude'),
+  source1Direction: getEl<HTMLSelectElement>('#source1-direction'),
+  source2Magnitude: getEl<HTMLInputElement>('#source2-magnitude'),
+  source2Direction: getEl<HTMLSelectElement>('#source2-direction'),
+  source3Magnitude: getEl<HTMLInputElement>('#source3-magnitude'),
+  source3Direction: getEl<HTMLSelectElement>('#source3-direction'),
   playbackSpeed: getEl<HTMLInputElement>('#playback-speed'),
   seed: getEl<HTMLInputElement>('#seed'),
   totalTime: getEl<HTMLInputElement>('#total-time'),
@@ -439,7 +477,14 @@
 function writeInputs(): void {
   setNumberInput(inputs.numParticles, simParams.numParticles, 0);
   setNumberInput(inputs.diffusionSd, simParams.diffusionSd, 3);
-  setNumberInput(inputs.electricStrength, simParams.electricStrength, 3);
+  setNumberInput(inputs.source0Magnitude, simParams.source0Magnitude, 3);
+  inputs.source0Direction.value = String(simParams.source0Direction);
+  setNumberInput(inputs.source1Magnitude, simParams.source1Magnitude, 3);
+  inputs.source1Direction.value = String(simParams.source1Direction);
+  setNumberInput(inputs.source2Magnitude, simParams.source2Magnitude, 3);
+  inputs.source2Direction.value = String(simParams.source2Direction);
+  setNumberInput(inputs.source3Magnitude, simParams.source3Magnitude, 3);
+  inputs.source3Direction.value = String(simParams.source3Direction);
   setNumberInput(inputs.playbackSpeed, displayParams.playbackSpeed, 2);
   setNumberInput(inputs.seed, currentSeed, 0);
   setNumberInput(inputs.totalTime, simParams.T, 0);
@@ -459,7 +504,14 @@
     numParticles: clamp(Math.round(Number(inputs.numParticles.value) || defaultSim.numParticles), 1, MAX_PARTICLES),
     diffusionSd: clamp(Number(inputs.diffusionSd.value) || 0, 0, 20),
     initClusterSd: clamp(Number(inputs.initClusterSd.value) || 0, 0, 20),
-    electricStrength: clamp(Number(inputs.electricStrength.value) || 0, -10, 10),
+    source0Magnitude: clamp(Number(inputs.source0Magnitude.value) || 0, 0, 10),
+    source0Direction: Number(inputs.source0Direction.value) === 1 ? 1 : -1,
+    source1Magnitude: clamp(Number(inputs.source1Magnitude.value) || 0, 0, 10),
+    source1Direction: Number(inputs.source1Direction.value) === 1 ? 1 : -1,
+    source2Magnitude: clamp(Number(inputs.source2Magnitude.value) || 0, 0, 10),
+    source2Direction: Number(inputs.source2Direction.value) === 1 ? 1 : -1,
+    source3Magnitude: clamp(Number(inputs.source3Magnitude.value) || 0, 0, 10),
+    source3Direction: Number(inputs.source3Direction.value) === 1 ? 1 : -1,
     negX: Number(inputs.negX.value) || defaultSim.negX,
     negY: Number(inputs.negY.value) || defaultSim.negY
   };
@@ -488,11 +540,17 @@
   const sigma = simParams.diffusionSd;
   const dt = simParams.dt;
   const stepSd = sigma * dt;
+  const strengths = [
+    simParams.source0Magnitude * simParams.source0Direction,
+    simParams.source1Magnitude * simParams.source1Direction,
+    simParams.source2Magnitude * simParams.source2Direction,
+    simParams.source3Magnitude * simParams.source3Direction
+  ];
   equationBlock.innerHTML = [
     '<span class="accent">Pedagogical stochastic-rate form</span>',
     'dx/dt = ξ_x(t) + drift_x,   dy/dt = ξ_y(t) + drift_y',
     'ξ_x, ξ_y ~ Normal(0, σ²),  where σ = diffusionSd',
-    'drift = -electric_strength · (field_source - pos) / (||field_source - pos|| + ε)',
+    'drift = Σ_s [ -electric_strength_s · (field_source_s - pos) / (||field_source_s - pos|| + ε) ]',
     '',
     '<span class="accent-2">Euler step used in this simulation</span>',
     'x_trial = x_old + (dxdt + drift_x) · dt',
@@ -504,8 +562,8 @@
     '<span class="accent-2">Reflective chamber rule</span>',
     'Particles bounce off the chamber walls instead of leaving the visible box.',
     '',
-    'Negative electric_strength attracts positive charge; positive electric_strength repels it.',
-    `Current: diffusionSd = ${sigma.toFixed(3)}, dt = ${dt.toFixed(3)}, electric strength = ${simParams.electricStrength.toFixed(3)}`,
+    'Negative electric_strength_s attracts positive charge; positive electric_strength_s repels it.',
+    `Current: diffusionSd = ${sigma.toFixed(3)}, dt = ${dt.toFixed(3)}, strengths = [${strengths.map((v) => v.toFixed(3)).join(', ')}]`,
     `Per-step displacement SD = diffusionSd × dt = ${stepSd.toFixed(3)}`
   ].join('\n');
 }
@@ -550,7 +608,7 @@
 
 function render(): void {
   updateStatus();
-  drawFrame(canvas, state, displayParams, simParams.electricStrength);
+  drawFrame(canvas, state, displayParams);
 }
 
 writeInputs();
@@ -584,7 +642,7 @@
   render();
 });
 
-for (const key of ['numParticles', 'diffusionSd', 'electricStrength', 'seed', 'totalTime', 'dt', 'initClusterSd', 'negX', 'negY'] as const) {
+for (const key of ['numParticles', 'diffusionSd', 'source0Magnitude', 'source0Direction', 'source1Magnitude', 'source1Direction', 'source2Magnitude', 'source2Direction', 'source3Magnitude', 'source3Direction', 'seed', 'totalTime', 'dt', 'initClusterSd', 'negX', 'negY'] as const) {
   inputs[key].addEventListener('change', () => {
     applyLiveSimParams();
     render();

tokens used
33,272
Implemented only the requested section (`Free Diffusion with Electrical Attraction`) in [inspect_field_attraction.ts](/Users/mq20185996/Dropbox/crossleylab/code/electrochemical_signals_progression_web/src/pages/inspect_field_attraction.ts).

Changes made:
- Replaced the single shared `Electric strength` control with per-generator controls for all 4 field generators:
  - `Generator N magnitude` (0 to 10)
  - `Generator N direction` (`Attract` / `Repel`)
- Updated simulation parameters and input parsing/writing to store per-generator magnitude and direction.
- Updated drift calculation so each generator contributes with its own signed strength (`magnitude * direction`).
- Updated field source rendering so each source color reflects its own effective strength.
- Updated equation/status text to show the multi-source formulation and current per-source strengths.

Checks run:
- `npm run build` completed successfully (`tsc && vite build`).
